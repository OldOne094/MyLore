# MyLore — Master Roadmap

> Phase 0 · Master roadmap · August 2026
> **This is the single source of truth for what we build and in what order.**
> Milestones first (§2), then the complete mission list (§3).
> Detailed per-mission implementation notes, files, tests and acceptance criteria stay in
> `DEVELOPMENT_PLAN.md` (the reference). Requirements in `PROJECT_REQUIREMENTS.md`.

---

## 1. Product in one line

A **local-first, offline-first, private desktop media tracker** (novels, web novels, light
novels, books, manga/manhwa/manhua, anime, TV, movies) — single binary (Tauri 2), own SQLite
database, no account, no ads, no telemetry. The internet is only a metadata tap; the library is
the source of truth. (`PROJECT_REQUIREMENTS.md`, `PHASE0_REPORT.md`.)

**Missions** are the smallest actionable units. Each mission has: ID · Title · Goal · Deps ·
Files · Tests · Acceptance Criteria · Complexity (S/M/L) · Priority · Status. When picked up, its
full checklist is created in the epic folder (`DEVELOPMENT_PLAN.md §5`).

---

## 2. Milestones

Milestones are the phases. We never build ahead of a milestone's exit criteria (spec §61).

| MS | Name | Exit criterion | Deps |
|----|------|----------------|------|
| M0 | Research & Design | This document set (Phase 0) incl. roadmap. | — |
| M1 | Foundation | Tauri 2 app builds on Win/macOS/Linux; TS strict + lint + fmt + CI green; typed IPC skeleton; empty window shell. | M0 |
| M2 | Database | SQLite via sqlx: migrations, full schema, repositories, FTS5, integrity pragmas; tests green. | M1 |
| M3 | Domain Layer | Domain entities/services in Rust with unit tests (tracking math, dedup, status transitions, stats, merge). | M2 |
| M4 | UI Foundation | Design tokens, themes, router shell, layout + nav rail, i18n (en/ar, RTL), command palette skeleton. | M1–M3 |
| M5 | Library MVP | Manual add, media CRUD, library grid/list/compact, filters + sort, media detail page, trash/undo. | M2–M4 |
| M6 | Tracking | Node trees, per-node progress (incl. novel chapter read-state), Normal/Manual mode, quick capture, status transitions, dashboard widgets. | M5 |
| M7 | Providers | Coordinator + AniList/TMDB/MangaDex/OpenLibrary (+ Jikan/Google fallback); external search; enrich; identity/dedup; optional NovelUpdates/Hardcover/Bangumi. | M3, M5 |
| M8 | Import/Export | Import pipeline + preview + reports; JSON/CSV/Markdown export; Goodreads/StoryGraph CSV + AniList/MAL imports. | M7 |
| M9 | Reviews & Collections | Reviews/notes/tags, favorites, collections + smart lists, bulk operations, content warnings. | M5 |
| M10 | Stats & Calendar | Stats service + UI (incl. reading recap), calendar, activity log polish. | M6 |
| M11 | Backup & Recovery | Backup/restore/rotation/validation, auto-backup, merge with conflict preview. | M2, M3, M6 |
| M12 | UX Polish | Shortcuts complete, command palette full, states audit, a11y pass, RTL pass, performance pass. | M5–M11 |
| M13 | Testing & Release | Integration/E2E suites, benchmarks, packaging, Alpha → Beta → Stable. | all |
| FX | Future Scope | Post-Stable, behind designed seams: cloud sync, plugins, AI (opt-in), mobile, more importers/content types. | M13 |

**Dependency spine:** M1 → M2 → M3 → M4 → M5 → M6 → M7 → M8 → M9 → M10 → M11 → M12 → M13.
**Parallel tracks after M3:** (M4 UI shell) ‖ (M5 library) ‖ (M7 provider work once M2+M3 land).

---

## 3. Mission list (everything)

Legend — **Pri:** Core (must ship) · Important (should ship) · Optional (if time, no scope creep).
**Cplx:** S (≤1 day) · M (2–5 days) · L (1–2 weeks). States:
`BACKLOG · READY · IN_PROGRESS · REVIEW · TESTING · DONE · BLOCKED · CANCELLED`.

**Mission log** (most recent first):
| Mission | Status |
|---------|--------|
| MISSION-096 Integration tests: DB, providers (recorded fixtures), import/export, backup/restore | **DONE** (2026-08-21) — the integration story audited and completed: **DB** ✓ (`domain_services.rs` — status lifecycle, stats, dedup/merge feeding progress, Arabic title variants, all over real migrated SQLite), **import/export** ✓ (`import_export.rs` — fixture detection across every source kind, profile persistence of tracking/review state, AniList mapping, JSON round-trip through the importer, CSV/Markdown exports), **providers** ✓ (recorded response fixtures for ten providers committed under `tests/fixtures/` since their missions; the dedicated offline mock harness lands with MISSION-098) — the missing fourth axis was **backup/restore**, now covered by a new `tests/backup_restore.rs` integration target exercising the full production path through public services: seed media + nodes + tracking + review + collection + cached cover → create → heavily mutate (new title, deleted node, rewritten review, dropped membership) → restore asserts the exact pre-mutation world returns (node re-parented home, review rating back, membership restored, asset repointed at its restored file, quarantined DB kept for forensics); plus retention N+monthly across sessions and mid-archive byte tampering caught by validation ✓ · gates: 3 new integration tests; 667 lib + 7 domain + 5 import/export + 3 backup/restore green ✓ |
| MISSION-095 Micro-interactions & density tiers (comfortable/compact) | **DONE** (2026-08-21) — M12 closes ✓ · **density tiers**: a new `UiDensity` preference (comfortable default / compact) persisted with the rest of the model, reflected as `data-density` on the root; `[data-density="compact"]` shrinks the control-height tokens so every component consuming `--control-height*` tightens automatically — no per-component edits · a Settings section (segmented Comfortable/Compact, mirroring the theme control) switches, reflects and persists it ✓ · validation: `parsePreferences` tolerates missing/unknown density and falls back to comfortable, round-trip covered ✓ · **micro-interactions**: audited rather than expanded — hover/focus transitions (`duration-150 ease-out`) already wrap every interactive surface from MISSION-031's tokens, and reduced-motion clamps them globally (MISSION-030); adding more motion would fight the restraint principle ✓ · tests: repository round-trip/validation with density, settings switch reflecting `data-density` + persistence, persisted-tier application on mount — **315 frontend tests**, tsc / eslint clean ✓ |
| MISSION-094 Performance pass: virtual lists, debounced search, image cache, startup timing | **DONE** (2026-08-21) — an audit found three of the four axes already built by earlier missions and closed the one real gap ✓ · **virtual lists** ✓ already: `VirtualizedLibrary` windows all three densities through TanStack Virtual against the local scroll container, and the import preview rows virtualize too — no unvirtualized large list remains · **image cache** ✓ already: `ImageService` disk cache with batched cover resolution (MISSION-062) · **debounced search**: the one real gap — QuickCapture's type-ahead fired `media_search` on *every keystroke*; a new `useDebouncedValue` primitive (200ms) collapses rapid typing into one query, and `useMediaSearchQuery` keeps previous results on screen while a new query is in flight (`placeholderData`) so type-ahead never flashes empty — unit-tested with fake timers proving five keystrokes settle into one value ✓ · **startup timing**: setup logs the database phase duration ("database opened" / "startup services ready" with ms fields) so pre-window regressions surface in the logs ✓ · **667 backend tests**, clippy clean, **314 frontend tests**, tsc / eslint clean ✓ |
| MISSION-093 Accessibility pass (WCAG AA): focus, labels, dialogs, contrast, reduced-motion | **DONE** (2026-08-21) — an audit against the five axes found the MISSION-030/037 foundation already solid: global `:focus-visible` accent ring with offset gap, skip link as first tab stop, `prefers-reduced-motion` clamping all transitions/animations, Radix dialogs with localized close labels, and form fields associating their labels — each pinned by existing tests ✓ · the real violation was **contrast**: `--text-tertiary` measured ≈3.6:1 (light) / 3.75:1 (dark) against the backgrounds — below AA for normal-size captions; retuned to `#6F6E66` / `#8A8A84` (≈4.9–5.1:1) with DESIGN_SYSTEM.md updated ✓ · new **contrast regression test** parses `tokens.css` directly and asserts every text tier ≥ 4.5:1 on both backgrounds in both themes (12 assertions), so no future token tweak can silently drop below the line ✓ · nav active link's built-in `aria-current="page"` now pinned by a test too ✓ · **312 frontend tests**, tsc / eslint clean ✓ |
| MISSION-092 RTL pass: mirror nav, flip icons, mixed-direction titles, test AR+EN | **DONE** (2026-08-21) — a full sweep found the codebase already largely RTL-correct (logical properties everywhere since MISSION-033, calendar chevrons swap via `i18n.dir()`, back arrows carry `rtl:rotate-180`) and closed the eight remaining gaps ✓ · physical → logical: `ml-auto`→`ms-auto` (Discover), `ml-1`/`ml-2`→`ms-*` (import hint, review rating), `pr-2`/`pr-1.5`→`pe-*` (node rows, tag badges), table headers `text-left`→`text-start` (import preview, enrich diff) ✓ · toast accent border `border-l-4`/`border-l-status-*` → logical `border-s-*` so the accent hugs the reading edge in both directions ✓ · keyboard direction semantics: TabsList arrow navigation and NodeTree expand/collapse arrows invert under RTL (reading-direction aware) ✓ · mixed-direction titles: media titles are data that may be Latin inside Arabic (or vice versa) — row titles and the detail h1 now carry `dir="auto"` ✓ · tests: tabs arrow inversion under `dir="rtl"`, MediaRow automatic-direction title — **299 frontend tests**, tsc / eslint clean ✓ |
| MISSION-091 States audit: empty/loading/skeleton/error/retry on every data surface | **DONE** (2026-08-21) — a systematic sweep of all 18 data surfaces (dashboard, library, detail + tracking/review tabs, search, discover, collections + detail, reviews, stats + reading recap, calendar, recap, trash, settings' providers/export/backups, recovery): **every surface already implements the full state set** — loading skeleton (`role="status"`), calm empty, error with retry — a direct payoff of the per-mission gates since 040 ✓ · the audit found the gaps were in *tests*, not components: four state paths shipped untested and are now pinned — SearchPage error → retry recovers, CollectionDetailPage load-failure error state, CalendarPage loading skeleton resolving into the grid, BackupsSection empty-folder note ✓ · ExportSection verified as an action surface (progress/failure/cancelled via its task) rather than a data list; ProvidersSection's loading uses pulse rows instead of Skeleton by design ✓ · **297 frontend tests**, tsc / eslint clean ✓ |
| MISSION-090 Complete shortcut map + command palette (add, mark complete, status, navigate, settings) | **DONE** (2026-08-21) — the MISSION-036 skeleton becomes the real thing ✓ · **complete shortcut map** in one shared source (`shortcuts/map.ts`): Mod+K palette · Mod+Enter quick capture (mark progress / complete / status on any title) · **Mod+N add title** (new) · **? shortcuts help** (new) — rendered by a new **ShortcutsDialog** so help can never drift from the bindings ✓ · **global add-title**: `AddMediaDialog` gains an optional controlled mode and `GlobalAddMedia` mounts it once in the shell (Mod+N or the palette's "Add title" command via the same window-event decoupling as quick capture; mounted only while open so provider-less shell tests keep working) ✓ · palette gains "Add title" and "Keyboard shortcuts" commands alongside the existing navigation/settings/theme/quick-capture set, whose keywords now also cover "mark complete"/"status" — marking itself flows through quick capture, the designed catch-up surface (adaptation noted: per-title status commands would need detail-page event plumbing) ✓ · i18n `shortcuts.*` + `palette.addTitle`/`palette.shortcuts` EN+AR ✓ · tests: global dialog opens via event + Mod+N and closes, help dialog lists the full map via ? and event — **293 frontend tests**, tsc / eslint / prettier clean ✓ |
| MISSION-089 Merge UI with conflict preview + restore-from-trash for merges | **DONE** (2026-08-21) — M11 closes: MISSION-028's merge policy finally reaches the database and the screen ✓ · `application/merge_service.rs` (new): the 028 policy (survivor-preferred scalars with duplicate fallback, unioned sets, differing duplicate main title → alternative, provider-unique external ids with survivor winning, self-relations dropped) implemented at the repository-`MediaRecord` level — no lossy domain↔record conversion in the path — producing a **conflict report** for every different non-empty pair ✓ · **plan** (`merge_plan`) previews conflicts + everything that will move (nodes, review/tracking when the survivor lacks one, collection memberships); **apply** (`merge_apply`) snapshots the duplicate into trash as a rich undo image (kind `merge`: full record + node ids + moved flags + collection ids) *before* touching anything, then folds metadata, re-keys nodes/review/tracking/collections onto the survivor and deletes the duplicate ✓ · **restore-from-trash**: `TrashService::restore_media` now understands kind `merge` — re-creates the duplicate exactly, pulls its nodes back from the survivor (only those still there), moves a borrowed review/tracking home, re-adds memberships additively; the Trash page lists and restores merge entries with zero UI changes ✓ · frontend: selecting exactly two titles reveals a **Merge** action in the bulk bar → **MergeDialog** (pick survivor → conflict/movement preview → apply → toast), i18n `merge.*` EN+AR; contract types `MergeConflict`/`MergePreview`/`MergeResult` + 2 commands; codegen regenerated ✓ · tests: policy unit test, plan/apply/undo end-to-end over real DBs, self-merge + missing-media rejection leaving trash untouched, dialog preview/apply flows — **667 backend tests**, clippy clean, **289 frontend tests**, tsc / eslint / prettier / codegen clean ✓ |
| MISSION-088 Backups UI (list, restore, validate, delete) + recovery from corrupt DB (prompt restore) | **DONE** (2026-08-21) — M11's engine gets its face, and a corrupt database no longer kills the app before a window shows ✓ · backend: `BackupService::list` (archives newest first from the file-name stamp; foreign files ignored), `delete_archive` (**path re-derived server-side** from the file name + canonical containment check — only real archives inside the backups folder can ever be deleted) and `start_fresh` (moves `mylore.db` + WAL sidecars into `{data_dir}/quarantine-corrupt-…`, with a short rename-retry for Windows' lingering handles); commands `backup_list` / `backup_delete` / `app_health` / `recover_start_fresh` / `recover_restore` ✓ · **recovery mode**: lib.rs now connects first and runs `integrity_check` separately — on corruption the app *launches anyway* with an `AppHealth` flag instead of dying in setup; migration stays fatal ✓ · frontend: **BackupsSection** on the Settings page — auto/interval/keep preferences saved on change (MISSION-086's prefs finally have UI), "Back up now" as a followed background task, archive rows with date + size and per-row Check (inline OK/Broken via `backup_validate`) / Restore (guarded dialog → rollback-safe task → restart note) / Delete (two-step inline confirm); **RecoveryScreen** behind a route-level **HealthGate** (`app_health`; swaps the whole shell only when `database_ok === false` is explicit, never blocking normal boot) offering restore-from-list, restore-via-native-file-dialog, or two-step start-fresh — every path ends in "close and reopen MyLore" with the quarantine name ✓ · i18n `settings.backups.*` + `recovery.*` EN+AR; contract types `BackupEntry`/`HealthStatus`/`RecoveryOutcome` + 5 commands; codegen regenerated ✓ · tests: list/delete path-safety/start-fresh quarantine (backend), section listing/check/delete/restore/create flows + recovery screen actions + HealthGate pass-through (frontend) — **663 backend tests**, clippy clean, **287 frontend tests**, tsc / eslint / prettier / codegen clean ✓ |
| MISSION-087 Pre-migration auto-backup hook | **DONE** (2026-08-21) — REQ-BACKUP-002's "no data loss from migration failure" gets a belt under the suspenders: before the schema ever moves forward, the old database is snapshotted ✓ · `db::pending_migrations(db_path)` counts embedded migrations not yet applied (missing file or no `_sqlx_migrations` table = fresh install = zero pending) ✓ · `BackupService::pre_migration_backup(db_path)` — no-op when nothing is pending, otherwise opens its own short-lived pool (the app's pool does not exist yet at that point in startup) and runs the full validated `create()` into `{data_dir}/backups` ✓ · `lib.rs` hook runs right before `db::init`: backup created → info log; nothing pending → silent; **failure → warning and startup continues** (sqlx already wraps each migration in its own transaction, so the hook is defense-in-depth, not a launch blocker) ✓ · tests: pending-count tracking (fresh/applied/forgotten-latest), hook fires only with a pending migration and produces a validatable archive, corrupt database surfaces an error — **661 backend tests**, clippy clean, 277 frontend tests, tsc / codegen clean ✓ |
| MISSION-086 Automatic backup schedule + rotation (N + monthly) in preferences | **DONE** (2026-08-21) — backups become a policy, not a chore ✓ · `BackupPrefs { auto_enabled, interval_hours, keep_count }` persisted in the `settings` table (`backup.*` keys; defaults off / 24h / 10) with `prefs()` (missing keys fall back to defaults) and `set_prefs` validating 1–8760 hours and 1–100 kept ✓ · **rotation "N + monthly"**: `rotate(keep)` sorts `{data_dir}/backups/*.mylore` by the zero-padded stamp embedded in the file name (`archive_stamp` — foreign names are never touched), keeps the newest N plus the newest of every older month, deletes the rest; wired into every `create()` so manual and automatic backups both enforce retention ✓ · **automatic schedule**: `auto_backup_if_due()` runs only when the preference is on AND the newest archive's age (`newest_backup_age_hours`, missing dir = due) exceeds the interval or no archive exists; `lib.rs` spawns a check ~20s after startup and logs the outcome without polluting the task list ✓ · IPC: `backup_prefs_get` / `backup_prefs_set` + contract type `BackupPrefs`; codegen regenerated ✓ · tests: rotation across three months with a foreign file untouched, prefs defaults/roundtrip/out-of-range rejection leaving values unchanged, auto-backup gating (disabled → skip, enabled+empty → run, fresh archive → not due) — **658 backend tests**, clippy clean, 277 frontend tests, tsc / codegen clean ✓ |
| MISSION-085 Restore: quarantine current, swap, verify, rollback-safe | **DONE** (2026-08-21) — the `.mylore` archive becomes the recovery path ✓ · `BackupService::restore(path)` (same service as MISSION-084): **validate with zero side effects first**, then stage the archive's snapshot + manifest assets under `{data_dir}/.restore-<uid>` (a `PartialGuard` deletes the staging dir on every exit), then **close the live pool** — the DB files are locked on Windows while open — and only then run the guarded file dance: quarantine the current `mylore.db` (+ WAL sidecars) and `images/` under `{data_dir}/quarantine-<stamp>-<uid>` using their original names so a rollback is a plain move back → copy the staged snapshot over the live path → place each asset at `images/<id><ext>` → repoint every manifest asset's `local_path` at its restored file via a fresh connection → verify for real (`PRAGMA integrity_check` + media count vs manifest) ✓ · **any failure after quarantine rolls back**: swapped-in files removed (file *or* directory — `remove_any` handles saboteurs) and quarantined originals moved back before the error escapes; there is deliberately **no cancellation checkpoint after validation** because a dropped future mid-swap would skip the rollback, and the whole dance is short ✓ · `RestoreReport { media_count, asset_count, quarantined_to, restart_required }` over a non-cancelable-by-design `TaskKind::Restore` task (`backup_restore`) — the managed pool is closed by the restore, so the UI must restart the app on success (wired up in MISSION-088's Backups UI) ✓ · contract type + command; codegen regenerated ✓ · tests: roundtrip restore replaces live data and repoints assets, invalid archives rejected without touching live data or creating a quarantine, planted-directory sabotage fails the swap *after* quarantine and rolls back to the intact mutated state — **655 backend tests**, clippy clean, 277 frontend tests, tsc / codegen clean ✓ |
| MISSION-084 BackupService: `VACUUM INTO` snapshot + assets + meta → `.mylore`; validate | **DONE** (2026-08-21) — M11 opens: the whole library packs into one portable `.mylore` archive (a plain zip, deflate) ✓ · `application/backup_service.rs` (new): `BackupService::create` runs SQLite's **`VACUUM INTO`** against the live WAL database for a consistent snapshot (path interpolated with quotes doubled — VACUUM takes no bind params), collects every `status='cached'` asset file under `{data_dir}/images` (missing files skipped with a warning, never fatal), and writes `meta.json` (format version 1, app version, counts, asset id → archive-path manifest for the MISSION-085 restore) + `library.db` + `assets/<id><ext>` into a **`.partial` sibling renamed into place on success** — a drop guard deletes the partial and the temp snapshot on any failure or cancellation ✓ · every archive is **re-opened and validated before success**: the manifest must parse at the current format version, the embedded snapshot must pass `PRAGMA integrity_check` via the shared `db::connect`, and its media count must match the manifest; a failed self-check deletes the archive ✓ · `TaskKind::Backup` joins the TaskManager (MISSION-070): `backup_create` spawns the task and resolves with the queued snapshot (`BackupReport` = path/size/counts as the typed result), `backup_validate(path)` validates any archive synchronously and returns the `BackupMeta` ✓ · contract types `AssetManifestEntry`/`BackupMeta`/`BackupReport` + both commands; codegen regenerated; new dependency: `zip` (deflate only, default features off) ✓ · tests: roundtrip create→validate→entry names, garbage/incomplete archives rejected, missing asset files skipped — **652 backend tests**, clippy clean, 277 frontend tests, tsc / codegen clean ✓ |
| MISSION-083 Reading recap stats: pages/chapters per month, mood/pace/format trends (StoryGraph) — local-data-only | **DONE** (2026-08-20) — REQ-STAT-001 addition: the StoryGraph-style reading recap lands under the Stats page (UX_RESEARCH line 86: "a cheap, local-data-only addition to the stats page") ✓ · migration `0012_reading.sql` — index-only (`node_progress(read_at)`) ✓ · `infrastructure/repositories/reading.rs` (new): `monthly_reading(from, to)` (node_progress JOIN content_node JOIN media, state read/watched, reading content types book/novel/web_novel/manga/manhwa/manhua — anime/tv/movie/other excluded), `taste_rows` (every review's moods JSON + pace) and `reading_formats` (tracked reading media GROUP BY format) — 2 repo tests ✓ · `application/reading_recap_service.rs` (new): `ReadingRecapService::recap(year)` (validated 1900–2100) queries a **±1-day local-year window** (same pattern as calendar/recap), buckets consumed nodes by local month — book chapters weighed by `page_count` (1 when unknown), non-book reading chapters count as chapters but 0 pages (exactly like MISSION-080's `consumed_pages`) — and counts distinct finished reading media via `activity_in_range`; mood/pace distributions folded from review metadata and the format distribution from tracked media, all all-time — 3 service tests (bucketing+totals+taste, out-of-year dropped, validation) ✓ · DTOs `ReadingRecap`/`ReadingTotals`/`MonthReading` over `reading_recap`; contract + codegen regenerated ✓ · frontend: `useReadingRecapQuery` + **ReadingSection** rendered under the Stats page charts — a year `<select>` (current → 2000), three tabular stat cards (pages / chapters / finished), two hand-rolled 12-bar month charts ("pages per month", "chapters per month"; localized `Intl` month names, RTL-safe), and three horizontal distribution charts reusing the shared `DistributionChart` (moods via `mood.*`, pace via `pace.*`, formats raw); skeleton / calm-empty / section-level error+retry; i18n `reading.*` in EN + AR; `DistributionChart` extracted from StatsPage for reuse ✓ · gates: 649 backend tests (per-target, disk-safe), clippy clean, 277 frontend tests, tsc / eslint / prettier / codegen clean ✓ |
| MISSION-082 Year-in-review style recap | **DONE** (2026-08-20) — a celebratory, whole-year recap (REQ-STAT-001 extension) assembled from the MISSION-051 activity trail, bucketed by **local** time like the calendar ✓ · migration `0011_recap.sql` — index-only (`activity(kind, created_at)`) ✓ · `infrastructure/repositories/recap.rs` (new): `completed_genres(pool, ids)` ranks genres by distinct finished media (dynamic IN clause, top 5) — 1 repo test; the activity rows reuse `calendar::activity_in_range` (same projection) ✓ · `application/recap_service.rs` (new): `RecapService::year(year)` (validated 1900–2100) queries a ±1-day local-year window and computes `RecapTotals` (distinct media added/started/completed/reviewed + progress-event count), `by_month` (completions per month), `best_month`, `top_media` (top 5 by event count), `longest_streak` (longest run of consecutive active days) and `top_genres` via the repo — 3 service tests (totals+chart+standouts, out-of-year dropped, validation) ✓ · DTO `YearRecap`/`RecapTotals`/`GenreCount`/`RecapMedia` over `recap_year`; contract + codegen regenerated ✓ · frontend: `useRecapYearQuery` + **RecapPage** replacing nothing (new `/recap` nav section, Trophy icon) — a year `<select>` (current → 2000), five tabular-numbers stat cards, best-month + longest-streak highlights (Sparkles/Flame), a hand-rolled 12-bar "finishes by month" chart (localized month names via `Intl`, best month in accent, RTL-safe), top-genre chips and a most-active list linking to `library/:id`; skeleton / calm-empty ("No activity this year") / error+retry states; i18n `recap.*` + `nav.recap`/`nav.hint_recap` in EN + AR ✓ · gates: 644 backend tests (per-target, disk-safe), clippy clean, 273 frontend tests, tsc / eslint / prettier / codegen clean ✓ |
| MISSION-081 Calendar: local air/release dates + activity; month grid + list | **DONE** (2026-08-20) — REQ-CAL-001 lands: a **month view** of when series air / books release (from `content_node.release_date`) and the user's **activity trail** (MISSION-051 events), bucketed by **local** calendar day ✓ · migration `0010_calendar.sql` — index-only (`activity(created_at)`, `content_node(release_date)`) ✓ · `infrastructure/repositories/calendar.rs` (new): `air_dates(from, to)` (release_date LEFT JOIN media → title/content_type) and `activity_in_range(from, to)` (activity JOIN media → kind/created_at/title) — 2 repo tests ✓ · `application/calendar_service.rs` (new): `CalendarService::month(year, month)` (validated 1900–2100 / 1–12) converts each RFC3339 `created_at` to `chrono::Local` and queries a **±1-day window** (`start − 1d` … `next month + 1d`) so any event whose *local* date lands in the month is captured regardless of UTC offset; air labels reuse `unit_label` → "E5"/"Ch7" — 4 service tests (bucketing, out-of-month dropped, empty Feb, validation) ✓ · DTO `CalendarMonth`/`CalendarDay`/`CalendarItem` (no `today` — the frontend computes local today) over `calendar_month`; contract + codegen regenerated ✓ · frontend: `useCalendarMonthQuery` (placeholder keeps the previous month while flipping) + **CalendarPage** replacing the `/calendar` placeholder — Sunday-start 7-column grid with `aria-pressed` day cells (accent dot = air, tertiary dot = activity), RTL-aware chevrons (`i18n.dir()`), a day-list panel (air chip in accent, activity chip `kind_*` + `HH:MM`, titles link to `library/:id`), skeleton + error/retry states; i18n `calendar.*` in EN + AR (identical key sets) ✓ · gates: 640 backend tests (per-target, disk-safe), clippy clean, 267 frontend tests, tsc / eslint / prettier / codegen clean ✓ |
| MISSION-080 StatsService endpoints + Stats page (cards + small charts, tabular numbers) | **DONE** (2026-08-20) — the MISSION-027 pure `compute_stats` finally reaches the database ✓ · Rust: `infrastructure/repositories/tracking.rs` gains `tracked_media` (tracking JOIN media LEFT JOIN review → content_type / core_status / rating / favorite / release_year for every tracked title) and `progress_stats` (batched 500-id aggregates over countable node kinds — book chapters weighed by page count, episode/unit kinds by count, `duration_min` of consumed nodes summed as minutes) — 3 new repo tests ✓ · `application/stats_service.rs` (new): `StatsView` DTO (total, status / content-type / rating / release-year distributions, avg rating, favorites, completed, completion rate, avg percent, consumed minutes / hours / pages) + `stats_summary` command; **real-data-only** — no estimated node-tree totals, `consumed_hours` derived purely from completed minutes ✓ · frontend: `useStatsSummaryQuery` + **StatsPage** replacing the `/stats` placeholder — seven tabular-numbers stat cards and four dependency-free, RTL-safe horizontal bar charts (status / content type / rating / release year) with calm empty ("No stats yet") and error+retry states; i18n `stats.*` in EN + AR ✓ · gates: 646 backend tests (per-target, disk-safe), clippy clean, 262 frontend tests, tsc / eslint / prettier / codegen clean ✓ |
| MISSION-079 Mood / pace / content-warning badges on detail page (StoryGraph); content warnings as acknowledged metadata | **DONE** (2026-08-20) — StoryGraph-style metadata lands as **user-owned review fields**, edited in the Review tab and surfaced as **badges in the detail-page hero**, with content warnings as **acknowledged-with-timestamp metadata** (never forced) ✓ · `migration 0009_review_metadata.sql` adds `review.moods`/`content_warnings` (canonical JSON arrays) + `pace TEXT CHECK (slow|medium|fast)` + `warnings_acknowledged_at` ✓ · domain (`domain/review.rs`): fixed vocabularies `Mood` (12: adventurous/dark/emotional/funny/hopeful/inspiring/informative/lighthearted/mysterious/romantic/sad/tense), `Pace` (slow/medium/fast), `ContentWarning` (14: violence/gore/sexual_content/strong_language/self_harm/suicide/drug_use/alcohol/death/abuse/bullying/animal_death/racism/transphobia) with `FromStr` + canonical `ALL` ordering; `Review` gains the four fields; `validate` now rejects duplicate moods/warnings and an acknowledgment with no warnings (or a blank stamp) — 3 new domain tests ✓ · repo: `ReviewRecord` gains the fields, `upsert`/`upsert_in_tx`/`get` read/write them (JSON ser/de via `serde_json`, `[]` for NULL), all 8 construction sites (import pipeline, export test, 4 integration tests, media-repo test) updated with empty defaults; import/export of the new fields intentionally **out of scope** (CSV/JSON user-state format unchanged) ✓ · service (`review_service.rs`): `SaveReviewInput`/`ReviewView` gain the fields; `save` **normalizes** keys (vocabulary-validated via `AppError::from(DomainError)`, deduped + sorted through `BTreeSet`), the acknowledgment is **preserved only when the warning set is unchanged** (changed/empty set clears the stamp — it's metadata of the *current* set), and an empty review is now empty only when there's also no mood/pace/warning metadata; new **`acknowledge_warnings`** stamps `warnings_acknowledged_at` now (idempotent; requires a review row with warnings) — 5 new service tests ✓ · commands: `review_save` gains `moods`/`pace`/`content_warnings`, new `review_acknowledge_warnings(media_id) → ReviewView` registered in `lib.rs` ✓ · contract + codegen: `ReviewView` shape extended, `review_save` args, `review_acknowledge_warnings`; regenerated + `codegen:check` green ✓ · frontend: `features/library/reviewMeta.ts` mirrors the vocabularies (`MOODS`/`PACES`/`CONTENT_WARNINGS` + key types — the server validates anyway); `review.ts` `SaveReviewInput` carries the fields + `useAcknowledgeWarnings` (seeds the review cache); **ReviewTab** gains three chip pickers (mood multi / pace single / content-warning multi, `aria-pressed` toggles, accent/danger selected states); **MediaDetailPage** renders the hero badges from the review row (pace neutral badge, mood accent badges, content-warning badges in danger tint) with a one-tap "I've seen them" acknowledge button → "Acknowledged ✓" (the hero reuses the shared `review.forMedia` cache via `useReviewQuery(mediaId, enabled)` — `enabled` defers the fetch until the media id resolves, so no bogus `review_get("")` fires during the skeleton) ✓ · i18n `review.*` metadata keys + `mood.*`/`pace.*`/`warning.*` vocab EN+AR (parity test green) ✓ · tests: ReviewTab picker test (chips save as `moods:["dark","mysterious"]`, `pace:"slow"`, `content_warnings:["gore"]`, toggle-off) + MediaDetailPage badge/acknowledge test + load-test chip assertions; two error-toast tests fixed to `mockImplementation` (the hero now also calls `review_get`, which was swallowing their `mockRejectedValueOnce`) ✓ · gates: **640/640 backend** (628 lib + 7 + 5), clippy `--lib -D warnings`, rustfmt on mission files (no drift this time) · **258/258 frontend**, tsc, eslint 0 errors, prettier, `codegen:check` ✓ · known limitation: `acknowledge_warnings` requires an existing review row (an empty review has no warnings to acknowledge by definition), and the badges are user-curated metadata rather than provider-derived tags — surfacing provider tags would be a MISSION-064-style enrichment follow-up |
| MISSION-078 Bulk operations on filtered selection (status/tag/list/delete) with summary | **DONE** (2026-08-19) — bulk actions now target **the whole filtered selection** (resolved server-side) and resolve with a **per-item change summary** instead of failing the whole batch on the first bad media ✓ · Rust: `application/bulk_service.rs` — `BulkFilter` (the 7 library facets, serde Deserialize), `BulkFailure{media_id,reason}` / `BulkResult{total,succeeded,failed,failures}` / `BulkDeleteResult{summary,trash_ids}`, and shared `resolve_targets(pool, filter, ids)` mapping a facet filter through the existing `media_repo::list` query (sort Title, no pagination) — when a filter is present the operation runs against the resolved set instead of the caller's ids ✓ · `set_status`/`add_tag`/`delete` are now **tolerant per media**: a repeat-guard failure, unknown id (FK error), or missing media lands in `failures` with its reason while the rest of the batch applies; `delete` returns a trash id per *successful* deletion so the group undo restores exactly what was removed ✓ · `collection_service::add_members` likewise resolves `BulkResult`, reporting per-media FK failures ✓ · commands `tracking_bulk_set_status`/`media_bulk_add_tag`/`media_bulk_delete`/`collection_bulk_add` gain `filter: Option<BulkFilter>` + the new return types (contract `BulkFilter`/`BulkFailure`/`BulkResult`/`BulkDeleteResult`, codegen regenerated) ✓ · frontend: `filters.ts` `toBulkFilter` maps `LibraryFilters` → `BulkFilter` (null when no filter); `bulk.ts` hooks accept a `filter` scope and resolve with the summary; **BulkActionBar** shows a **scope toggle** ("Selected N" / "All N matching") when active filters leave matching titles beyond the selection (the backend ignores ids when the filter is set), and every action toasts the **summary** ("Status updated — 2 of 3") plus a "N titles skipped" error toast on partial failure; delete's undo restores only the succeeded trash ids; i18n `bulk.scope*`/`statusSetSummary`/`tagAddedSummary`/`partialFailures` EN + AR (plural-aware) ✓ · tests: bulk_service 16 (5 new: partial-failure summary keeping successes, filtered selection for status/tag/delete, empty filter → no targets, unknown-media delete), collection_service 10 (+1 partial add_members); frontend 256 (4 new: summary toast, skipped-titles toast, filtered-scope invocation sending the filter, delete-undo limited to succeeded) ✓ · gotchas: the hooks always pass `ids` alongside the filter (the backend ignores ids when the filter is set) so the filtered-scope test asserts the selected ids still ride along; the library filter-bar trigger is labelled "Filter" not "Filters" ✓ · gates: **632/632 backend** (620 lib + 7 domain_services + 5 import_export), clippy `--lib -D warnings`, rustfmt on mission files (drift reverted per-file) · **256/256 frontend** (35 files), tsc, eslint 0 errors (7 pre-existing warnings), prettier, `codegen:check` ✓ |
| MISSION-077 Smart collections: save filter as collection; query builder (basic) | **DONE** (2026-08-19) — a collection can now be **computed instead of manual**: it stores a serialized `SmartFilter` in `collection.filter_def` (`is_smart=1`) and its membership is resolved **live** against the library, so it updates automatically as the library changes (REQ-MEDIA-001 "Collections" extended) ✓ · Rust: `application/collection_service.rs` gains `SmartFilter` (serde Serialize/Deserialize, mirrors the library's flat filters + sort — content_type/format/pub_status/genre/tag/year/favorite/sort/ascending, all `Option`), `create_smart(name, filter)` (validates the sort up front via `to_media_filter`, persists JSON), `update_smart_filter(id, filter)` (**smart-only**; rejects manual collections), `CollectionView` gains `is_smart: bool` + `filter: SmartFilter | null`, and `list()`/`view()` compute smart `member_count` through `media_repo::count` while manual collections keep `count_members` ✓ · **`members()` routes server-side**: a smart collection re-runs `media_repo::list` + the existing `MediaService::to_list_items` (position = index) — so the frontend reuses the same `queryKeys.collection.members(id)` with **no conditional hooks** ✓ · manual ops guarded: `add_members`/`remove_member`/`reorder` reject smart collections and `update_smart_filter` rejects manual ones (`AppError::validation`); `to_media_filter` mirrors media_service's sort/ascending resolution (unknown sort → validation error) ✓ · commands `collection_create_smart(name, filter)` / `collection_update_smart(collection_id, filter)` → `CollectionView` in `commands/collection.rs` + registered in `lib.rs` ✓ · contract: `SmartFilter` type, `CollectionView` +`is_smart`/`filter`, the 2 commands; codegen regenerated ✓ · 4 service tests (smart members + count resolved from the filter with positions, update filter changes members + the favorite flag rides through, manual-ops guard on smart collections, manual-collection + unknown-sort rejection) · gates: **625/625 backend** (613 lib + 7 domain_services + 5 import_export), clippy `--lib -D warnings`, rustfmt on mission files (cascade drift reverted per-file) · frontend: `collections/api.ts` `useCreateSmartCollection`/`useUpdateSmartFilter`; `smartFilter.ts` (`toSmartFilter` bridges `LibraryFilters`+`LibrarySort` → `SmartFilter`, `EMPTY_SMART_FILTER`); **`SmartFilterForm.tsx` — the query builder** (one nullable `<select>` per facet reusing the library's `contentType.*`/`pubStatus.*` labels, facet-driven options from `media_facets`, sort select + asc/desc toggle); **CollectionsPage** gains a "New smart collection" dialog + a **Smart badge** on cards; **LibraryFilterBar** gains **"Save as collection"** (visible only with active filters → `collection_create_smart` with the live filter+sort); **CollectionDetailPage** smart branch: computed note, no drag/reorder/remove controls, smart empty state, and an **"Edit filter"** dialog → `collection_update_smart` ✓ · i18n `collections.*` smart keys EN + AR ✓ · 7 frontend tests (create-smart via the builder, smart badge, read-only smart detail, edit filter, smart empty state, save-as-collection from the library, hidden save button without filters) · gates: **252/252 frontend**, tsc, eslint 0 errors, prettier, `codegen:check` ✓ · **also de-flaked a pre-existing MISSION-070 test**: `cancel_flips_the_flag_and_runners_can_bail` could finish `Success` before observing `cancel()` (1000-iteration spin vs. the send) — the test runner now uses `tokio::select!` on `reporter.cancelled()` so the assertion is deterministic |
| MISSION-076 Collections CRUD + drag/drop membership + bulk add | **DONE** (2026-08-19) — collections are now a **first-class, manageable surface** (REQ-MEDIA-001 sidebar "Collections"): real `/collections` grid page + `/collections/:id` detail with **ordered membership**. Rust: `infrastructure/repositories/collection.rs` gains `list_with_counts` (LEFT JOIN COUNT, ORDER BY sort_order, name), `count_members`, `members_with_media` (collection_member × media × LEFT JOIN review, ORDER BY position, media_id; `COALESCE(r.favorite,0)` rides the `MediaSummary`); new `application/collection_service.rs` — `CollectionService::{create,rename,delete,list,members,add_members,remove_member,reorder,view,require}` + `CollectionView{id,name,member_count,created_at}` / `CollectionMemberView{position,media:MediaListItem}`; `normalize_name` rejects blanks; **`reorder` validates the given media id set equals the current members** (sorted compare) then rewrites positions 0..n preserving `added_at` via repo `set_members`; `remove_member` rebuilds contiguous positions; `members` batches progress summaries through the existing `MediaService::to_list_items` so the detail rows show progress + favorite ✓ · **retired the MISSION-045 bulk collection path**: `BulkService::list_collections`/`add_to_list` + `CollectionItem` removed (3 tests dropped), the 8 `collection_*` commands now live in `commands/collection.rs` (`collection_list/create/rename/delete/members/bulk_add/remove_member/reorder`) registered in `lib.rs` ✓ · contract: `CollectionView`/`CollectionMemberView` types + the 8 commands; `collection_list → CollectionView[]`; codegen regenerated + `codegen:check` ✓ · frontend: `features/collections/api.ts` (8 hooks, `queryKeys.collection.members(id)`; detail key re-typed to string), **CollectionsPage** (card grid, create/rename dialogs, delete confirm, empty/error+retry states) + **CollectionDetailPage** (ordered `MediaRow` list reused from the library with resolved covers; **native HTML5 drag-and-drop** reorder — drag handlers never read `dataTransfer`, so jsdom tests drive them with `fireEvent.dragStart/dragOver/drop`; Up/Down buttons + Remove per row; optimistic cache writes roll back on error; back link + drag hint) ✓ · bulk picker now reads `CollectionView` rows; i18n `collections.*` EN + AR (parity kept) and `bulk.listEmpty` updated ("…create one on the Collections page.") ✓ · tests: 13 (5 CollectionsPage: grid+counts, empty, create, rename, delete-confirm, error+retry; 7 CollectionDetailPage: ordered render, move-down reorder asserting `collection_reorder` media_ids + optimistic order, drag-and-drop reorder, edge-disabled move buttons, remove, empty detail, not-found) ✓ · gotchas: React Query mutations run their `mutationFn` in a microtask so IPC assertions must `await` the success toast first, and the reorder mock must re-sort `members` so the invalidate-refetch returns the new order; the collection `memberCount` label collides with the header count in tests (two "2 titles" nodes); `useAssetViews` must be called before early returns; repo `row_to_collection(row)` moves the row so `row.get(6)` must be read first ✓ · gates: **621/621 backend** (609 lib + 7 domain_services + 5 import_export), clippy `--lib -D warnings`, rustfmt on mission files (drift reverted) · **245/245 frontend** (35 files), tsc, eslint 0 errors, prettier, `codegen:check` ✓ |
| MISSION-075 Favorites flag in grid/list + filter | **DONE** (2026-08-18) — favorites are now **visible across the library**: `MediaListItem` carries `favorite` (sourced from the `review.favorite` flag — the repo queries already selected `COALESCE(r.favorite, 0)`), so the **grid, list and compact views render a heart badge** on favorited titles via the shared `FavoriteFlag` (a non-interactive `role=img` heart, `aria-label` "Favorite", `text-danger` + `fill-current` mirroring the ReviewTab toggle; on the grid it overlays the poster's start-corner on a translucent `bg-bg-base/90` disc so it never collides with the `NextUnitButton`/select checkbox at `end-2 top-2`, and it sits inline after the row title so it survives the dense Compact tier) ✓ · the **favorite filter needed no work** — MISSION-041 already wired the `LibraryFilterBar` chip → `filtersToArgs.favorite` → `media_list` → SQL `r.favorite = 1` (with `count` LEFT-JOINing the review table), so this mission was the missing DTO + visual half ✓ · IPC contract `MediaListItem` gains `favorite: boolean`; codegen regenerated (the feature-level `MediaListItem` in `features/library/api.ts` is hand-maintained, so it gained the field separately) ✓ · **fixed a latent MISSION-074 regression**: `codegen:check` had silently broken because the `#[allow(clippy::too_many_arguments)]` attribute sat *between* `#[command]` and `pub fn review_save` — the codegen script's regex requires them adjacent — moved the allow above `#[command]` (matching `commands/media.rs`); validation now passes ✓ · tests: fixtures gained `favorite` (`listItem` factory, both `TITLES` arrays, the 300-row windowing array, the search `ROWS`) + a `LibraryPage` test asserting exactly one "Favorite" img across grid → list → compact ✓ · gates: **617/617 backend**, clippy `--lib -D warnings`, rustfmt on mission files · **232/232 frontend**, tsc, eslint 0 errors, prettier, `codegen:check` ✓ |
| MISSION-074 Review/notes UI + commands | **DONE** (2026-08-18) — a media's user-owned record (rating, review, short review, spoiler flag, favorite, notes, personal tags) is now editable from the detail page: the **Review tab** replaces the MISSION-041 placeholder, saving through new `review_get`/`review_save`/`review_delete` + `media_tags`/`media_add_tag`/`media_remove_tag` IPC commands ✓ · `application/review_service.rs` — `ReviewService::{get,save,delete}` with `ReviewView`/`SaveReviewInput`; `save` validates the media exists and the domain invariants (1–10 rating via `Rating::new`, spoiler flag requires review text), **preserves `created_at`** while stamping `updated_at` (rfc3339), treats an entirely empty review as a **delete** (clears the row, resolves with an empty view), and fires a best-effort `log_reviewed` activity row (kind `reviewed`, meta `{rating}`); `delete` is idempotent and rejects unknown media; 7 tests (create→update preserving created_at, out-of-range rating, spoiler-without-text, empty→delete, get-before-save, delete+repeat, unknown media) ✓ · **personal tags** — `MediaService::{media_tags,add_tag,remove_tag}` operate on the **personal scope only** (domain tags never surface) with resolve-or-create `tag-{uuid}` rows (reused across media) and return the updated name-ordered list; `normalize_tag` moved here and shared with the MISSION-045 bulk path; repo helpers `media_tags`/`remove_tag_from_media` + `TagLink{id,name,scope}`; 4 tests (roundtrip, tag-row reuse, personal-only via the seeded domain tag `isekai`, blank/unknown-media rejection) ✓ · IPC contract + codegen: `ReviewView`/`MediaTagView` types + 6 commands (codegen:check green) ✓ · frontend — `features/library/review.ts` (`useReviewQuery`/`useSaveReview`/`useDeleteReview` + `useMediaTagsQuery`/`useAddMediaTag`/`useRemoveMediaTag`, seeding `review.forMedia`/`media.tags` and invalidating media details + dashboard), `ReviewTab.tsx` (favorite toggle `aria-pressed`, 10-star picker that clears on re-click, review textarea + spoiler checkbox with a "Contains spoilers" badge, short review, private notes, tag chips with remove + add form, Save/Clear, "saved <date>", empty-state hint, skeleton, error+retry via `EmptyState`); `MediaDetailPage` renders it in the Review tab ✓ · i18n `review.*` EN + AR (parity kept; dead `detail.reviewPlaceholder` removed) ✓ · tests: 8 `ReviewTab` (loads values+tags, empty hint, save payload+toast, favorite/rating toggle, spoiler flag, clear via `review_delete`, tag add/remove, save-error toast) + `MediaDetailPage` tab-switch test updated ✓ · gotchas: the review textarea is queried as `getByRole("textbox", {name:"Review"})` — the section headings' `aria-labelledby` makes `getByLabelText` ambiguous; eslint's `set-state-in-effect` rejects the data→draft sync effect, so the draft resets via a **render-phase compare** (`if (data !== loadedFor)`) with no effect; the test `review_save`/`review_delete` mocks must resolve real values or the hook's `onSuccess` crashes on `view.media_id`; `#[allow(clippy::too_many_arguments)]` on `review_save` (precedent in `commands/media.rs`) ✓ · gates: **617/617 backend** (605 lib + 7 domain_services + 5 import_export), clippy `--lib -D warnings`, rustfmt on mission files · **231/231 frontend** (33 files), tsc, eslint 0 errors, prettier, `codegen:check` ✓ · ARCHITECTURE §6 updated ✓ |
| MISSION-072 Provider imports: AniList user export, Goodreads CSV, StoryGraph CSV | **DONE** (2026-08-18) — file import now brings the user's **list state** in, not just metadata (REQ-IMPORT-002). MAL-via-Jikan and Trakt were dropped from scope: Jikan has **no user-list endpoint** and Trakt user lists **require OAuth** (app is deliberately local-first; see API_PROVIDERS.md §2). ✓ `domain/import.rs` — `ParsedItem` gains `my_status`/`my_rating`/`my_review`/`progress`/`started_at`/`completed_at`/`repeat_count` (stringly); `ImportRow`/`RawFields` carry the normalized form (`CoreStatus`, 0–10 integer rating, ISO dates, counts); `parse_raw` normalizes (unknown status → warning+None, non-integer or out-of-range rating → warning+None, `repeat_count > 0` **forces `CoreStatus::Repeat`** per the tracking invariant, `completed_at` dropped for non-terminal statuses or when before `started_at`); 4 tests ✓ · `ImportPipeline::insert_row` — `write_user_state` persists a `tracking` row (status/started/finished/repeat/progress) and a `review` row (rating/review) in the same transaction via new `upsert_in_tx` helpers in the tracking/review repos; metadata-only rows write neither; 2 pipeline tests ✓ · `infrastructure/parsers/` — **`AniListParser`** (`mediaListCollection.lists[].entries[]`; media metadata + entry user state; ANIME / MANGA(+format NOVEL→novel) content types; FINISHED/RELEASING/… → `pub_status`; CURRENT/COMPLETED/PLANNING/PAUSED/DROPPED/REPEATING → `my_status`; score ÷10; anilist+mal external ids; `<br>`-stripped synopsis; entries without `media` skipped; 4 tests), **`GoodreadsParser`** (fixed headers: ISBN13 → provider `isbn`, Book Id → `goodreads`, Exclusive Shelf → status, My Rating ×2, Date Read → completed_at, Bookshelves → tags, My Review → review; 3 tests), **`StorygraphParser`** (Reading Status incl. `Did Not Finish`→dropped and `Paused`→on_hold, Start/End Date, Rating ×2, Page Number Read → progress, Genre → tags, Notes → review; 3 tests), plus `shared.rs` helpers (`sniff_csv_columns`, case-insensitive `Columns`, `rating_0_5_to_0_10`, `normalize_date`; 4 tests) ✓ · `application/import_file_service.rs` — `ImportFileKind::{Json,Csv,AniList,Goodreads,Storygraph}` with content-aware `detect` (MyLore array vs AniList collection for JSON; Goodreads/StoryGraph header sniff vs a mapped CSV), `is_profile()`, routing; 3 tests ✓ · IPC: **`import_file_detect(source) → string`** (contract + lib.rs registration + codegen) ✓ · frontend: `features/import/api.ts` — `ImportFileKind` union + `PROFILE_KINDS` + `useImportDetect`; `ImportFileDialog` sniffs the format after reading the file, shows a profile badge ("AniList export"…) + "your status, rating, and progress will be imported automatically — no mapping needed" hint, and **skips the column-mapping table for profile kinds**; i18n `import.detecting`/`profileAniList`/`profileGoodreads`/`profileStorygraph`/`profileHint` (EN + AR); 2 dialog tests (Goodreads badge + skipped mapping, AniList detect) ✓ · gates: 594/594 backend, 223/223 frontend, clippy `-D warnings`, tsc + vite build, eslint 0 errors, `codegen:check`, prettier + rustfmt on mission files ✓ · ARCHITECTURE §6 documents the profile parsers and user-state persistence ✓ |
| MISSION-073 Import/export integration tests + fixture data | **DONE** (2026-08-18) — the import/export pipeline is now covered end-to-end by **integration tests against a real migrated DB** (REQ-IMPORT-002, REQ-EXPORT-001): `tests/import_export.rs` (5 tests) drives `ImportFileService::detect`/`commit` + `ExportService::stream_to_path` through file-backed pools (`db::init`) and asserts DB state via the repos ✓ · fixtures under `tests/fixtures/import/` double as sample files for the Import dialog: `mylore.json` (2 items with user state), `anilist_export.json` (FMA: Brotherhood COMPLETED repeat=1 score=100 progress=64; Oshi no Ko CURRENT), `goodreads_export.csv` (3 rows incl. ISBN13 9780756404741), `storygraph_export.csv` (4 rows incl. Did Not Finish); the detect test proves all four kinds are sniffed, previewed, and committed with expected counts, the profile test asserts persisted tracking + review rows, and the AniList test documents the `repeat_count > 0 → Repeat` invariant (the finish date drops for the non-terminal Repeat bucket, progress stays) ✓ · **export now round-trips user state** — `ExportRow` gains `progress`/`started_at`/`completed_at`/`repeat_count` (import-aligned names; `completed_at` maps from tracking `finished_at`), `CSV_HEADERS` grows 34 → 38 columns (progress/started_at/completed_at/repeat_count after my_notes), `render_markdown` adds a "Progress: N" + "started/finished <date>" line under **My data**; the round-trip test re-imports a JSON export and proves dedup (committed 0, skipped 2) with the user state intact ✓ · gotchas: `infrastructure::test_support` is `#[cfg(test)]`-gated so integration tests define their own `migrated_pool`/`cleanup_files`/`fixture_import` over public `db::init`; the `csv` crate writes headers unquoted (QuoteStyle::Necessary), so the header assertion checks `title,title_original` without quotes ✓ · gates: 606/606 backend (594 lib + 7 domain_services + 5 import_export), clippy `--lib -D warnings`, rustfmt on mission files; frontend untouched (223/223 from MISSION-072) ✓ |
| MISSION-071 Export JSON / CSV / Markdown (streaming, dialog pick) | **DONE** (2026-08-17) — the whole library can now be **exported as a streaming background task** (REQ-EXPORT-001) with a **native save dialog** (ARCHITECTURE §6): `domain/export.rs` — `ExportFormat::{Json,Csv,Markdown}` (`FromStr`, `as_str`, `extension` json/csv/md), `ExportRow` (import-aligned field names so the JSON **round-trips through the MISSION-068 importer**; user data — `my_status`/`my_rating`/`my_review`/`my_short_review`/`my_notes`/`favorite`/`collections`/`created_at`/`updated_at` — rides as extra keys), `CSV_HEADERS` (fixed 34 columns) + `LIST_SEPARATOR` (`\|` joins alt_titles/genres/tags; external ids as `provider:value`), `row_to_json`/`row_to_csv`/`render_markdown`; 6 tests ✓ · repo helpers: `media::list_ids` (title COLLATE NOCASE order) + `collection::media_collection_names` (one join query — media → collection names) + tests ✓ · `application/export_service.rs` — `ExportService::stream_to_path(path, format, on_progress)` streams rows (JSON `[...]`, CSV via `csv` crate with a manual header row, Markdown per-title sections) ticking `(done, total)` per row; `PartialExport` guard writes `<final>.partial` and **renames into place on commit, dropping the partial on cancel/error**; `record()` assembles each row (media + person_info + genre_names + tag_info + asset cover/banner URLs + tracking core_status + review user data + collections); 6 tests (json-import-compatible, csv headers/rows, markdown sections, empty library → valid empty files, progress ticks `[(1,2),(2,2)]`, no partial left on error) ✓ · `commands/export.rs` — `export_media(format, path)` spawns `TaskKind::ExportFile` on the MISSION-070 TaskManager ("Export {format} library"), reports "Preparing export…"/"Exporting {done}/{total} titles"/"Export finished", runs inside `tokio::select!` vs `reporter.cancelled()` (`TaskError::Cancelled`), success result = `ExportReport{format,total,path}` ✓ · `tauri-plugin-dialog` wired (cargo + npm + `dialog:default` capability) ✓ · IPC: `ExportReport` type + `export_media` command; codegen regenerated ✓ · frontend: `features/tasks/api.ts` — shared `useTask(taskId, {onSuccess})` (listen `task-changed` → seed the query cache; ref-stable callback; `task_get` fallback) + `useTaskCancel` moved here; `useImportTask` refactored to delegate ✓ · `features/settings/export.ts` (`useExportMedia`, `useExportTask`, `pickExportPath` via `@tauri-apps/plugin-dialog` `save()`, default file `mylore-library.<ext>`) + `ExportSection.tsx` in the Settings page (JSON/CSV/Markdown format picker, running panel with live progress + cancel, success report "Exported N titles · Saved to <name>", failed/cancelled panels; no-op when the save dialog is cancelled) ✓ · i18n `export.*` keys (EN + AR, plural-aware) ✓ · tests: 5 `ExportSection` tests (JSON happy path, CSV format switch, save-dialog cancel → no-op, failed panel, cancelled panel); import tests stay green after the `useTask` refactor ✓ · gates: 570/570 backend, 221/221 frontend, clippy clean, tsc+vite build, eslint 0 errors, `codegen:check`, rustfmt + prettier on mission files ✓ |
| MISSION-070 Background TaskManager (progress, cancel, typed results) wired to import | **DONE** (2026-08-16) — long operations now run as **cancelable background tasks with progress, typed results and live events** (ARCHITECTURE §8): confirming an import spawns a task instead of blocking the command path ✓ · `domain/task.rs` — `TaskKind::ImportFile`, `TaskState::{Queued,Running,Success,Failed,Cancelled}` (`is_terminal`), `TaskSnapshot{id,kind,title,state,progress,message,error,result,created_at,updated_at}`, `TaskError{Cancelled,Failed}` ✓ · `application/task_service.rs` — `TaskManager` (managed as `Arc<TaskManager>`, injectable emitter via `with_emitter`), `TaskReporter` (start/progress/message/cancel backed by a watch channel), `spawn`/`get`/`list`/`cancel`; 4 tests (progress+typed-result sequence, cancel flag, failed message, list/get) ✓ · `ImportPipeline::commit_with_progress(preview, plan, on_progress)` — per-row callback `(done, total)`; `commit` delegates with a no-op; +1 test asserting ticks `[(1,3),(2,3),(3,3)]` ✓ · `commands/tasks.rs` — `task_list`/`task_get`/`task_cancel` (sync handlers over `State<Arc<TaskManager>>`) ✓ · `commands/import.rs` `import_commit` rewritten: spawns `TaskKind::ImportFile` on the manager, runs `commit_with_progress` inside `tokio::select!` against `reporter.cancelled()` (**drop-based cancellation** — dropping the commit future rolls back its transaction), resolves with the **queued `TaskSnapshot`** ✓ · `lib.rs` — TaskManager emitter forwards every change as a **`task-changed`** event (hyphen → clean `listenTaskChanged` codegen); 3 commands registered ✓ · contract: `TaskSnapshot` type (+ `result: unknown | null` — the typed outcome, e.g. the `ImportReport`), `import_commit → TaskSnapshot`, `task_*` commands, `task-changed` event; codegen regenerated ✓ · frontend: `features/import/api.ts` — `useImportFile` resolves with the snapshot (invalidations moved out), `useImportTask(taskId)` subscribes to `task-changed` per task (falls back to `task_get`), invalidates media lists/facets/dashboard on success, `useTaskCancel` ✓ · `ImportFileDialog` — running panel (backend message + progress bar + **Cancel import**), failed panel with the backend message, cancelled state, and the report read from `task.result`; resets `taskId` when reopened ✓ · i18n `importCancelled`/`cancelTask`/`cancelFailed` (EN + AR) ✓ · tests: `@tauri-apps/api/event` mocked to capture the `task-changed` handler; 7 `ImportFileDialog` tests incl. running→success report via emitted snapshots, cancel→`task_cancel`→cancelled state, and task-failure panel ✓ · gates: **556/556 backend tests** (clippy clean, rustfmt on mission files) · **216/216 frontend tests** · `tsc`+vite build · eslint 0 errors · `codegen:check` · prettier ✓ · ARCHITECTURE §6/§8 updated ✓ |
| MISSION-069 Bulk-import preview UI (per-item outcome) + confirm/cancel | **DONE** (2026-08-16) — importing now shows the **full REQ-IMPORT-003 preview before anything is written**: pick a JSON file or map a CSV Title column → the dialog **auto-runs `import_file_preview`** and renders a TanStack-Virtual per-item list (reusing the MISSION-040 virtualizer) with `New` / `In library` / `Duplicate` / `Invalid` badges + each row's issues; only `New` rows are selectable (default: all), a "select all new" checkbox + per-row toggles build the **`ImportPlan`** (`{rows}`) sent to `import_commit`, Cancel closes without writing ✓ · `features/import/api.ts` — `useImportPreview` (query keyed `preview(kind, source, mapping)`) + `useImportFile` now takes the explicit `plan` ✓ · `queryKeys.import.preview` ✓ · the **effective CSV mapping now carries the chosen `delimiter`/`separator`** into preview/commit (fixes a MISSION-068 latent bug where `mapping.delimiter` stayed `","` even after changing the delimiter select — changing it now re-analyzes the file) ✓ · `ImportFileDialog` — analyzing/error+retry states, outcome summary badges, "Nothing new to import" empty state, Import disabled until at least one new row is selected (label shows the count, e.g. "Import 2 titles") ✓ · i18n `preview*`/`outcome*`/`selectAllNew`/`importSelected` keys (EN + AR, plural-aware) ✓ · tests: 5 `ImportFileDialog` tests (JSON preview + plan `[1,2]`, CSV mapping preview, per-row deselect → plan `[2]` with invalid rows unselectable, all-deselected → disabled, commit-failure toast); `src/test/setup.ts` reports a viewport for the preview list so the virtualizer renders in jsdom ✓ · gates: 214/214 frontend tests · `tsc`+vite build · eslint 0 errors · `codegen:check` (contract unchanged — backend untouched, 551 tests) ✓ · ARCHITECTURE §6 documents the preview/confirm flow ✓ |
| MISSION-068 JSON import (app format) + CSV import (mapping UI) | **DONE** (2026-08-16) — file import is now **end-to-end usable**: JSON (MyLore app format) and CSV (with a column-mapping UI) both run through the MISSION-067 pipeline; the richer **preview + confirm screen is MISSION-069** (commit currently defaults to "import every new row", `plan: null`) ✓ · `infrastructure/parsers/` — `JsonParser` (top-level array of item objects; every field optional, counts/years accept **numbers or strings**; non-array / non-object elements → whole-file `ImportError::Parse`, content problems stay per-row) + `CsvParser` with the IPC-shared `CsvMapping` (each import field → a CSV column; `default_content_type` types every row when the file has no type column; `delimiter` = field delimiter [`,` `;` tab `|`], `separator` splits multi-value cells for `alt_titles`/`genres`/`tags`/`external_id`; external-id cells must be `provider:value`; `flexible` parsing so a ragged trailing line never aborts the batch; BOM stripped) + free `csv_headers`; 21 parser tests ✓ · `application/import_file_service.rs` — `ImportFileKind::{Json,Csv}` (`FromStr` + first-byte `detect`), `ImportFileService` routes to the right parser and reuses `ImportPipeline::preview`/`commit` (`plan: None` → `ImportPlan::all_new`); 6 service tests incl. json-commit-dedupes-skipped + csv-all-new ✓ · `domain::import::ImportPlan` now serde **round-trippable** (Serialize+Deserialize) for IPC ✓ · IPC: `import_file_preview(kind, source, mapping|null)` → `ImportPreview`, `import_commit(kind, source, mapping|null, plan|null)` → `ImportReport`, `import_csv_headers(source, delimiter)` → `string[]`; registered in `lib.rs`; contract types `CsvMapping`/`ImportPreview`/`PreviewItem`/`Issue`/`ImportPlan`/`ImportReport`/`ReportItem` + codegen regenerated ✓ · UI: `features/import/` (`api.ts` — `useCsvHeaders`/`useImportFile` invalidating media lists/facets/dashboard; `ImportFileDialog` — `<input type="file">` + FileReader in the webview (**no fs/dialog plugin added**), JSON-ready step, CSV column-mapping table with delimiter/separator controls, Import disabled until Title mapped, result report added/skipped/failed; `ImportFileDialog.test.tsx` 4 tests incl. JSON direct import + CSV mapping payload + title-gate) ✓ · **Library page** gets an **Import** button (toolbar + empty state) ✓ · i18n `import.*` (EN + AR, plural-aware) ✓ · gates: 551/551 lib tests · clippy `-D warnings` · `tsc`+vite build · 213/213 frontend tests · eslint 0 errors · `codegen:check` ✓ · ARCHITECTURE §6 documents the JSON schema + CSV mapping ✓ |
| MISSION-067 Import pipeline core (parser→validator→normalizer→dedup→preview→txn→report) | **DONE** (2026-08-16) — the **format-agnostic file-import core** (ARCHITECTURE §6 / REQ-IMPORT-002): the JSON/CSV **parsers land in MISSION-068** (they implement the new `ImportParser` trait), the confirm UI in MISSION-069 — so **no tauri command yet** ✓ · `domain/import.rs` — pure, DB-free stages: `ImportParser` trait (`parse(source) -> Result<Vec<ParsedItem>, ImportError{Parse,Unsupported}>`), `ParsedItem` (1-based `row` + stringly fields), `validate` → `Issue{severity,field,message}` (**Error** skips the row: blank title / missing or unrecognized `content_type` / non-non-negative count or year; **Warning** degrades: unknown `pub_status`→`unknown`, malformed dates or invalid language dropped, unknown person role or blank/duplicate external id dropped), `normalize` → `ImportRow` (pre-built `Title`, `ContentType`, `MediaStatus`, `DateOnly`, `LanguageCode`, people `(PersonRole, name)`, cleaned/deduped genres+tags, `ExternalId`s deduped **by provider only after a non-blank value** so a blank id never shadows the real one, `start>end` → `end_date` dropped) ✓ · `prepare` → `PreparedRow{Valid,Invalid}`; `dedup` reuses `identity::best_match` on `IdentityCandidate[]` → `RowOutcome{New,InLibrary,Duplicate}`; `preview` → `PreviewItem{source_row,title,outcome,matched_media_id,row(serde-skip),issues}` + `ImportPreview{total,valid,new,in_library,duplicates,invalid,items}`; `ImportPlan{none/all_new}`; 9 unit tests ✓ · `application/import_pipeline.rs` — `ImportPipeline{pool}`: `preview` (parse → `media_repo::identity_candidates` → dedup → preview, read-only), `commit(preview,plan)` runs **one outer transaction with `SAVEPOINT import_row` per row** — failure → `ROLLBACK TO` + `RELEASE` (that row reports `Failed`, the batch continues), success → `RELEASE`; only the plan's `New` rows are written (non-new / invalid / unselected / unknown-source rows report `Skipped`); people/genres/tags/assets resolved via the new `ensure_*_in_tx`/`insert_in_tx` helpers and the media aggregate via `media_repo::insert_in_tx` (cover/banner registered as `remote` assets per MISSION-062); `ImportReport{total,committed,skipped,failed,items}` sorted by source row; `run` = preview+commit; parse errors → `AppError::validation`, unsupported → internal; 9 async tests incl. **duplicate `(provider, ext_id)` across two rows → the second insert hits the global `media_external_id` PK and rolls back only its own savepoint** (1 committed / 1 failed; the library holds exactly the committed title) + people/genre/tag/asset/external-id linkage ✓ · `repositories`: `media.rs` `create` → `insert_in_tx` + `insert_media_row`, `ensure_person/genre/domain_tag` → `ensure_*_in_tx` (pool-level signatures unchanged); `asset.rs` `insert` → `insert_in_tx`; 1 repo test ✓ · clippy -D warnings ✓ · 523/523 lib tests ✓ |
| MISSION-064 Hardcover adapter (books/LN, free GraphQL) — optional third book provider | **DONE** (2026-08-16) — the **optional third book provider** lands on the MISSION-053 seam: a Hasura GraphQL transport + normalizer, offline-tested against hand-built fixtures (schema documented at docs.hardcover.app; API_PROVIDERS §12 updated with the **verified Bearer-token auth** — public reads now require a token from `hardcover.app/account/api`) ✓ · `infrastructure/providers/hardcover/`: `client.rs` — `HardcoverClient` (reqwest, `APP_USER_AGENT`, `graphql<T>` POSTs `{query, variables}` to `api.hardcover.app/v1/graphql`, sends `Authorization: Bearer <token>` only when set [MISSION-063 keyring wires it, never logged]; maps non-2xx via `from_http_status` [401/403→AuthRequired, 429→RateLimited, 404→NotFound, 5xx→ProviderDown], GraphQL `errors`/malformed envelopes → `InvalidResponse`; 7 wiremock tests incl. bearer-header + query/variables assertion, no-header-without-token, 401/429/503, GraphQL-error envelope, bad JSON) ✓ · `graphql.rs` — `SEARCH_QUERY` (`search(query, query_type: "Book", per_page, page)` — Typesense-backed, only `query` required, defaults match the site) + `DETAILS_QUERY` (`books(where: {id: {_eq}})` → category, cover image, contributions/authors, `cached_tags`, editions ISBNs) ✓ · `response.rs` — serde models; the search root returns **JSON blobs** (`results`), so they're parsed defensively into `SearchBook` (only fields the candidate mapping reads; `flexible_id` accepts string **or numeric** Typesense ids); details mirror the Hasura field names **verbatim** (underscores: `release_date`, `book_category_id`, `cached_tags`, `isbn_10`/`isbn_13` — no camelCase rename, the opposite gotcha from the REST adapters); `Envelope<T>` data/errors wrapper ✓ · `normalize.rs` — pure mappers (8 tests): `content_type` via `book_category_id` (1→Book, 2/10→Novel [novella/light novel], 9→WebNovel, else Book), `pub_status` (known release_date→Completed), `release_year` (field, else first-4-digit run in date), `genres` from the `cached_tags["Genre"]` bucket (Mood/Theme buckets excluded), `candidate` (slug → `https://hardcover.app/books/{slug}` URL, cover deliberately None — the search blob carries none), `media` (subtitle→alt_titles, contributions→`PersonRole::Author` deduped, `image{url}` cover, editions' `isbn_10`/`isbn_13`→isbn10/isbn13 external ids deduped) ✓ · `mod.rs` — `HardcoverProvider` (`PROVIDER_ID="hardcover"`, `hardcover_config()` = [Book, Novel, WebNovel] @ **~1 rps** [rate not published → self-throttle], caps search/details/images · nodes false [books/editions — no chapter tree] · related false · auth `Key`); provider ids are **numeric book ids** (non-numeric rejected before any request); `search` short-circuits non-book/novel/webnovel domains; 8 tests incl. type filtering + coordinator ✓ · registered in `build_adapter` (key injection via `with_api_key`) + `default_provider_entries()` + `default_provider_configs()` — the MISSION-063 settings UI picks it up automatically as a key-required row ✓ · gates: **479 Rust** (24 new), clippy `-D warnings` ✓ (lib+bins; `--all-targets` still shows only the pre-existing Behavior-enum warnings), cargo fmt ✓ · no IPC/UI changes — search/import/enrich/tests all flow through the existing seam ✓ |
| MISSION-063 Provider settings UI: enable/disable, API keys (keyring), test connection | **DONE** (2026-08-16) — the Settings page now manages **which providers are enabled**, stores their **API keys in the OS keyring** (never in the DB, JSON or the webview) and can **test each connection** live ✓ · `infrastructure/keyring.rs`: `SecretStore` trait (`get`/`set`/`delete`) with `OsKeyring` (native store, service `mylore`, idempotent delete on `NoEntry`) + `InMemoryKeyring` for tests; Cargo `keyring = "3"` ✓ · `application/providers/settings.rs`: `ProviderSettingsService` (RwLock over `configs + coordinator`), `EntryBuilder` trait + `pub type ProviderEntry = (ProviderConfig, Arc<dyn Provider>)`, `set_enabled` persisted to **`providers.json`** in the app data dir via atomic temp+rename, `set_key` → keyring only (validates existence then `AuthKind::Key`; blank clears), `test_connection` → `search_provider`; `ProviderSettingsView{provider,name,enabled,requires_key,has_key}` — **keys never cross IPC**; 10 unit tests ✓ · `infrastructure/providers/mod.rs` gains `build_adapter(id, api_key)` factory + `StdEntryBuilder` + `default_provider_configs()` ✓ · `coordinator.rs` gains `search_provider(provider_id, query, content_type, token)` — an explicit one-shot probe **not gated on `enabled`** (diagnostic ping) but requiring the `search` capability, run under the MISSION-053 policy; 2 tests ✓ · IPC: `providers_list` / `provider_set_enabled` / `provider_set_key` / `provider_test_connection` in `commands/providers.rs`; `discover`/`import`/`enrich` commands now source the coordinator via the settings service; `ProviderTestView{ok,message,results}` never rejects on provider failure (message = user-safe `ProviderError` display); contract + codegen regenerated ✓ · UI: `features/settings/providers.ts` hooks (`useProvidersQuery`/`useSetProviderEnabled`/`useSetProviderKey`/`useTestConnection`, key `settings.providers`, invalidate on mutation) + `ProvidersSection.tsx` — per-provider row: `role=switch` enable toggle, password key field (save disabled until typed, "Key saved" indicator when `has_key`), Test-connection button with inline ok/failed result, skeleton/error+retry/empty states; wired into `SettingsPage`; i18n `settings.providers.*` en/ar ✓ · gates: **455 Rust** + **209 TS** tests, clippy `-D warnings` ✓, tsc ✓, eslint 0 errors ✓, prettier ✓, codegen:check ✓ · note: `preferences.test.tsx` now wraps renders in `QueryClientProvider` + an `invoke` mock since SettingsPage mounts a real provider query ✓ |
| MISSION-062 Image pipeline: download/cache covers, broken-url handling, cache policy | **DONE** (2026-08-15) — covers are now **registered at import, downloaded once, cached to disk, and served through the Tauri asset protocol**; broken URLs flip to `missing` (permanent) and transient failures to `failed` (retried after a 1h cooldown) so the UI never shows a broken image ✓ · **import wiring**: `import_service.persist_media` now creates `asset` rows (`a-{uuid}`, kind `cover`/`banner`, status `remote`, `remote_url` from `details.cover_url`/`banner_url`) and links `cover_asset_id`/`banner_asset_id` — lazy resolve, imports never block on downloads; 2 tests (assets created + linked + status `remote`, no URL → no asset) ✓ · `infrastructure/images.rs`: `ImageClient::fetch(url)` classifies 200 → `FetchedImage{bytes, mime_type, etag}` vs 404/410 → `ImageError::NotFound` vs other/transport → transient; `ImageCache` writes atomically (temp+rename) under `app_data_dir()/images`; `ext_for_mime` (jpeg/jpg, png, webp, gif, svg, avif, bmp, fallback `img`); `From<ImageError> for ProviderError`; 5 wiremock tests (body/mime/etag, 404/410, 5xx/empty, transport, ext mapping, cache save) ✓ · `repositories/asset.rs` gains `update_fetch` (status/local_path/mime/etag/last_fetched_at) + `list_by_ids` (ordered, `IN`) + 1 test ✓ · `application/image_service.rs`: `ImageService{pool, client, cache}` + `AssetView{id, kind, status, local_path, remote_url, mime_type}`; `resolve`/`resolve_many` (batch dedupes + skips unknown ids — a stale media ref never fails the grid) with the **cache policy**: `cached`+file→serve local (no network), `cached`+file-gone→re-download, `remote`→download now, `failed`→retry only after `FAILED_RETRY_COOLDOWN` (1h from `last_fetched_at`), `missing`→never auto-retried, no `remote_url`→`missing`; 9 tests incl. cached-serves-without-network (`expect(0)` scoped mock), broken-url-not-retried, transient-retried-after-aging ✓ · `commands/images.rs` `asset_resolve(asset_id)` + `assets_resolve(asset_ids)` registered in `lib.rs` (setup `manage`s `Arc<ImageService>` with `data_dir/images`); IPC contract `AssetView` + both commands, codegen regenerated ✓ · Tauri config: `app.security.assetProtocol.enable=true` + scope `["$APPDATA/images/**"]`, `img-src` gains `http://asset.localhost` + `asset:` in both `csp` and `devCsp` ✓ · **UI**: `useAssetViews` batch hook (keyed by sorted deduped id set, disabled when empty), `CoverImage` component (`convertFileSrc(local_path)` when `cached`, else the content-type placeholder icon — failed/missing/unresolved never render a broken `<img>`), `VirtualizedLibrary` tracks the visible window via the virtualizer's `onChange` and batch-resolves one batch per visible page, `MediaCard`/`MediaRow` take a resolved `cover`, `MediaDetailPage` hero resolves its cover; tests: 4 CoverImage + 2 LibraryPage (batch `assets_resolve` called + cached img rendered, no call when no covers) — **440 Rust + 201 TS**, tsc ✓ · eslint 0 errors (7 pre-existing warnings) ✓ · prettier ✓ · clippy `-D warnings` (lib+bins) ✓ · codegen:check ✓ |
| MISSION-061 Enrich metadata (refresh provider fields only; never touch user data) + diff report | **DONE** (2026-08-15) — **Refresh from provider** on the detail page re-fetches a title's provider-owned metadata and shows a before → after **diff dialog**; user data is never touched ✓ · `application/enrich_service.rs`: `EnrichService::enrich_from_provider(media_id)` — validates the media is linked to a provider (own `(provider, provider_id)` external id), runs `coordinator.get_details` (enabled + `details`-capability gated), and diffs **provider-owned fields only** (`content_type`, `format`, `title_main`, `title_original`, `synopsis`, `pub_status`, `start_date`, `end_date`, `release_year`, `language`, `country`, `content_rating`, `pages`, `duration_min`, `ep_count`, `ch_count`, `provider_url`, `alt_titles`, `people` ["Name (role)"], `genres`, `tags` [domain only], `external_ids` ["provider:ext"]); when nothing changed only `metadata_refreshed_at` is stamped (`updated_at` untouched — a no-op refresh never churns "recently updated"); when changed the fresh record is persisted via `media_repo::update` and `EnrichView{media_id, provider, refreshed_at, changed, changes: EnrichChange[]{field, before, after}}` reports the diff — **personal tags are re-linked** via new `media_repo::personal_tag_ids` (scope `personal`) so the wholesale `media_tag` rewrite never deletes user-owned tags, and cover/banner asset ids + created_at are preserved; 6 tests (refresh updates + diff report, noop stamps only refreshed_at, personal tags + assets survive refresh, no-provider rejected, provider failure propagates, domain tags refresh but personal tags excluded from diff) via `FakeProvider`/`Behavior` ✓ · `repositories::media` gains `stamp_metadata_refreshed`, `person_info` (id → `(name, role)` labels), `genre_names`, `tag_info` (id → `(name, scope)`), `personal_tag_ids` + 1 repo test ✓ · `commands/enrich.rs` `media_enrich(media_id)` registered in `lib.rs`; IPC contract `EnrichChange`/`EnrichView` + `media_enrich`, codegen regenerated ✓ · UI: `useEnrichMedia` mutation (invalidates `media.lists()`, `media.facets()`, `dashboard.all()`), refresh button in the detail hero (only when `provider` set, spinner while pending, "Refreshed"/"already up to date"/error toasts), `EnrichDialog` (table of Field/Before/After, `noChanges` empty state) + `enrichMeta` field-label/value helpers; i18n en/ar (`enrich.*` incl. per-field labels) ✓ · tests: 4 MediaDetailPage tests (diff dialog + changed toast, unchanged toast without dialog, refresh error toast, button hidden when no provider) — **423 Rust** + **195 TS** ✓ · tsc ✓ · eslint 0 errors (7 pre-existing warnings) ✓ · prettier ✓ · clippy `-D warnings` ✓ · codegen:check ✓ |
| MISSION-060 Import-from-provider flow (search → details → identity check → add) | **DONE** (2026-08-15) — clicking **Add to library** on a discover hit runs the full **details → identity check → add** pipeline and imports the content-node tree so tracking works immediately ✓ · `ProviderCoordinator` gains `get_details(provider, id, token)` / `get_nodes(provider, id, token)` — enabled + capability-gated (`details`/`nodes`) and policy-run (timeout/retry/backoff/rate-limit/cancel from MISSION-053); 5 tests (runs when enabled+capable, gated on missing capability, gated on disabled, nodes) ✓ · `repositories::media` gains find-or-create helpers `ensure_person(name, role)`, `ensure_genre(name)` (NOCASE lookup so provider names reuse seed slugs like `fantasy`), `ensure_domain_tag(name)` (scope `domain` only — personal tags are user-owned) ✓ · `application/import_service.rs`: `ImportService::import_from_provider(provider, provider_id)` — fetches details, re-runs `identity::best_match` against `identity_candidates` with the own `(provider, provider_id)` appended as an `ExternalId` (so re-imports match **exactly**), returns the existing `media_id` when matched (`created: false`, kind `in_library`/`duplicate`) or persists a new `MediaRecord` (`created: true`, `new`) — people/genre/tag links resolved via the ensure-* helpers, external ids stored (own + provider-surfaced, deduped by provider), then **best-effort** node-tree import (iterative pre-order stack — no async recursion/boxing; `ProviderNode.id` kept as `content_node.external_id`; provider without the `nodes` capability → no-op; transport errors warn + keep the media); `ProviderImportView{media_id, created, identity_kind, title, content_type}`; 4 tests (new media + links + volume→chapter tree roundtrip, same-provider re-import → in_library, title duplicate → duplicate, details failure propagates) ✓ · `commands/import.rs` `import_provider(provider, provider_id)` registered in `lib.rs`; `From<ProviderError> for AppError` added (ProviderError Display is already user-safe) ✓ · IPC contract `types.ProviderImportView` + `import_provider` — codegen regenerated ✓ · UI: DiscoverPage rows for non-`in_library` hits get an **Add to library** button (pending state "Importing…", disabled while a mutation is in flight); success → toast (`Added “{{title}}”` / already-in-library / duplicate-candidate) + `navigate("/library/{media_id}")`; error toast on failure; `useImportProvider` invalidates `media.lists`/`facets`/`details` + `dashboard.all`; i18n `discover.import*` en/ar (parity kept) ✓ · tests: DiscoverPage import flow (new hit imports + toast + navigates to `/library/m-42`; in-library hit has no add button) — **416 Rust + 191 TS**, tsc ✓ · eslint 0 errors ✓ · prettier ✓ · clippy `-D warnings` ✓ · codegen:check ✓ · notes: metadata is imported as-is from the provider (image pipeline + provider-settings wiring land MISSION-062/063); node-tree failures never fail the import — the media row is the primary contract and a tree can be re-fetched via the MISSION-061 enrich |
| MISSION-066 Bangumi adapter (CN ACGN, open API) + fixtures | **DONE** (2026-08-16) — the **CN ACGN provider** (anime/manga/light novels/web novels/games) on the MISSION-053 seam: anonymous reads need **no key** → registered `AuthKind::None` (the MISSION-063 settings UI shows it without a key row) ✓ · `infrastructure/providers/bangumi/`: `client.rs` — `BangumiClient` (reqwest, `APP_USER_AGENT`, no token; `search_subjects` JSON **POSTs** `/v0/search/subjects` with the documented `{keyword, sort:"match", filter:{type:[1,2], nsfw:false}}` body and `limit`/`offset` as query params — the experimental search API ignores unknown fields, so no legacy `filter_type` shape is sent; `get_subject`/`get_episodes`/`get_related` GETs; `read_json` maps non-2xx via `from_http_status` [404→NotFound, 429→RateLimited, 5xx→ProviderDown] and malformed JSON → `InvalidResponse`; **the episodes route is the flat `/v0/episodes?subject_id=`** — `/v0/subjects/{id}/episodes` 404s (verified live); 8 wiremock tests incl. UA + filter-body + query-param assertions) ✓ · `response.rs` — **camelCase** OpenAPI models (the opposite gotcha from TMDB/OpenLibrary's snake_case): `PagedSubject`/`SlimSubject` (search rows carry `short_summary` + `platform` so book sub-types resolve at search time), `Subject` (detail w/ `WikiV0` `infobox` — values are a string **or** a `[{v}]` list, deserialized via an `untagged` enum → `.text()` flattens with `、`), `PagedEpisode`/`Episode` (`ep` within-subject number, `sort` all-seasons, both fractional; type 0 main · 1 SP · 2 OP · 3 ED), `RelatedSubject`; only read fields declared ✓ · `normalize.rs` — pure mappers (14 tests): `content_type` (1 book → `漫画`→Manga · `网络小说`→WebNovel · `小说`→Novel · else Book · 2 anime→Anime · else Other), `title` (**`name_cn` preferred for display**, `name` kept as `title_original`), `pub_status` (known `date`→Completed — Bangumi exposes no airing-status field), `release_year` from `YYYY-MM-DD`, `candidate`/`media` (cover `images.large`, `format`=`platform`, `ep_count` for anime / `ch_count` for books from `total_episodes`, `bgm.tv/subject/{id}` url), `people` from infobox wiki keys (**`作者`→Author · `插图`→Artist · `导演`→Director · `动画制作`→Studio**, list values split on `、`/`,`/`/`, deduped), `genres`/`tags` — **no genre taxonomy**, so `genres` are a best-effort top-tags subset (sorted by vote count, structural noise filtered: years/formats/countries/adaptation markers/`漫画`/`小说` labels) and `tags` keep the top 10 raw; `nodes` — main + SP rows kept, OP/ED/PV dropped, sorted by position, `kind` follows the content type (Episode for anime, Chapter for books), `is_special` for SP, duration from `duration_seconds`; `relations` (`前传`→Prequel · `续集`→Sequel · `原作`/`动画`/`漫画`/`小说`/`书籍`→Adaptation · else Other) ✓ · `mod.rs` — `BangumiProvider` (`PROVIDER_ID="bangumi"`, `bangumi_config()` = [Anime, Manga, Novel, WebNovel, Book] @ **1 rps** [documented 1 rps / 15 per 60 s / 80 per 10 min → cache heavily], caps search/details/**nodes**/related/images · auth None); provider ids are numeric subject ids (validated; `a/b`/`-1`/blank → `InvalidResponse`); `get_nodes` fetches the subject (kind) then `/v0/episodes`, `get_related` maps the relation edges; `get_external_ids` stays `Unsupported` (the v0 payload carries no cross-provider ids — AniList links *to* Bangumi, not the reverse); 7 tests incl. domain short-circuit, invalid ids, node tree, relation mapping, coordinator ✓ · fixtures `tests/fixtures/bangumi/{search_subjects,subject_detail,episodes,relations}.json` **hand-built from verified live responses** (real 3-gatsu book/anime rows, detail, 4-episode feed incl. an SP + OP, relation edges) — API_PROVIDERS §13 updated with the verified auth/search-body/episodes-route + adapter notes ✓ · 25 new tests, full gate: 504 lib tests · clippy `-D warnings` clean (bangumi files produce zero warnings even under `--all-targets`; committed-file rustfmt churn reverted as usual) |
| MISSION-065 NovelUpdates adapter (HTML scrape, WN/LN) + fixtures | **DONE** (2026-08-15) — the **web/light-novel provider**: NovelUpdates is server-rendered HTML (no JSON metadata API), so this adapter follows the **LNReader `novelupdates` plugin** (maintained by an NU moderator — authoritative selectors, API_PROVIDERS §14) at a conservative **~1 req/s** on the MISSION-053 seam ✓ · `infrastructure/providers/novelupdates/`: `response.rs` — `scraper`-based HTML extraction mirroring the plugin: `parse_search_rows` (`div.search_main_box_nu` → `.search_title > a` href/text + `img` cover, slug = `/series/{slug}/` tail), `parse_series_page` (`.seriestitlenu` title, `.wpb_wrapper img` cover, `#authtag` authors [split on `,`], `#seriesgenre a` genres, `#editstatus` status, `#showtype` type, `#editdescription` synopsis [HTML-stripped], `input#mypostid` → the chapters key; `None` when the page isn't a series page), `parse_chapter_labels` (`li.sp_li_chp`, newest-first as served) + `is_captcha_page` (`<title>` in the plugin's blocked-title list — "Just a moment…"/"bot verification"/etc.); 5 fixture-driven tests ✓ · `client.rs` — `NovelUpdatesClient` (reqwest, `APP_USER_AGENT`, `get(path, params)` for HTML + `post_form` for the chapter feed [added reqwest **`form`** feature]; maps transport→`Transport`, HTTP→`from_http_status` [404→NotFound, 429→RateLimited, 5xx→ProviderDown], and an anti-bot challenge → non-retryable `InvalidResponse` — NU serves captchas as 200s with a recognizable title; 7 wiremock tests incl. UA header, urlencoded body assertion, captcha/429/404/503) ✓ · `normalize.rs` — pure mappers (8 tests): `content_type` (`#showtype` contains "Web Novel"→WebNovel, else Novel [Light/Published]), `pub_status` (Ongoing/Hiatus/Cancelled/Dropped/Completed from `#editstatus`), `parse_label`/`chapter_title` (**`v1c1part1` → Volume 1 Chapter 1 Part 1**, `c3`, `ss1`/`s12` side-stories → specials; hand-rolled digit scanner, no regex dep), `candidate`/`media` (authors→`PersonRole::Author`, genres, synopsis, canonical `page_url`; `ch_count` deliberately None — lives in the feed), `nodes` (feed **reversed to chronological**, Volume parents + top-level specials) ✓ · `mod.rs` — `NovelUpdatesProvider` (`PROVIDER_ID="novelupdates"`, `novelupdates_config()` = [Novel, WebNovel] @ **1 rps**, caps search/details/**nodes**/images · related false · auth None); `search` via `/series-finder/?sf=1&sh=…&sort=srank&order=asc&pg=1` with non-novel domains short-circuited; `get_details` = series page → media; `get_nodes` resolves `input#mypostid` from the series page, then POSTs `action=nd_getchapters&mygrr=0&mypostid=…` to `/wp-admin/admin-ajax.php` → node tree (2 requests, within coordinator rate limit); `get_external_ids`/`get_related` stay `Unsupported`; tests incl. coordinator ✓ · registered in `default_provider_entries()` (after OpenLibrary) + `test_support::novelupdates_fixture` (fixtures **hand-built** from the plugin's selectors — NU's Cloudflare layer blocks live recording) ✓ · CSP `img-src` extended with `https://cdn.novelupdates.com` (covers) ✓ · **caveat**: live calls may hit Cloudflare; if the challenge persists the provider reports a clear non-retryable error and the fallback fan-out (MISSION-058) still serves the query · clippy `-D warnings` clean · full suite 407 tests pass · MISSION-066 (Bangumi) still open. |
| MISSION-059 External search UI (grouped by provider, "in library"/duplicate flags) | **DONE** (2026-08-15) — the combined **`{local, external}`** search (ARCHITECTURE §5) behind a single `search_external(query, content_type)` IPC command ✓ · `repositories::media::identity_candidates(pool)` loads the library identity index (id, main/original titles, alt titles, external ids) defensively — a row that fails `MediaId`/`Title`/`ExternalId`/`ProviderId` validation is skipped, never fatal; 1 test ✓ · `application/search_service.rs`: `SearchService{pool, coordinator: Arc<ProviderCoordinator>}` fans out to **all enabled providers** in parallel (the coordinator's retry/backoff/rate-limit policy from MISSION-053 applies), groups hits per provider, and maps each hit through `identity::best_match` with the hit's own `(provider, provider_id)` appended as an `ExternalId` — so previously-imported titles from the same provider match **exactly** ✓ · DTOs: `ExternalSearchView{local: MediaListItem[], groups: ProviderGroup[], failures: ProviderFailureView[]}`, `ExternalHit{provider, provider_id, title, content_type, release_year, cover_url, synopsis, url, identity: IdentityFlagView}`, `IdentityFlagView{kind: "in_library"|"duplicate"|"new", media_id?, score?}` — `Exact`→**in_library** (links to `/library/{id}`), `TitleExact`/`Fuzzy`→**duplicate candidate**, `None`→**new**; blank query returns an empty view without hitting providers; per-provider failures surface as notices instead of failing the whole search; 6 tests (blank, exact, duplicate, new, failures, local+groups) via the `FakeProvider`/`Behavior` pattern ✓ · `commands/discover.rs` `search_external` (registered in `lib.rs` invoke_handler, which now also builds `Arc<ProviderCoordinator>` from `default_provider_entries()` and `app.manage`s it) ✓ · IPC contract types `ExternalSearchView`/`ExternalProviderGroup`/`ExternalHit`/`ExternalIdentityFlag`/`ExternalProviderFailure` + `search_external` — codegen regenerated ✓ · UI: real `DiscoverPage` replaces the placeholder — query input + content-type `<select>` (10 types), local hits section, per-provider group cards (cover thumbnail, title, year, synopsis, provider badge), `IdentityBadge` (in_library=accent/duplicate=planned/new=neutral), failure notices, skeleton/empty/error+retry states; `useDiscoverSearchQuery` (key `queryKeys.search.external`, stale 60s, disabled on blank) ✓ · i18n `discover.*` en/ar ✓ · CSP `img-src` widened to the provider CDNs (s4.anilist.co, image.tmdb.org, uploads.mangadex.org, covers.openlibrary.org, cdn.myanimelist.net, books.google.com, storage.googleapis.com) for remote covers ✓ · tests: `DiscoverPage.test.tsx` (5 — initial prompt, submit+invoke args, local+in-library links `/library/m-1`, provider failure notice, no-results) — **189 TS** (28 files) + **381 Rust**, tsc ✓, eslint 0 ✓, prettier ✓, cargo fmt ✓, clippy `-D warnings` ✓, codegen:check ✓ |
| MISSION-058 Fallback providers: Jikan + Google Books | **DONE** (2026-08-15) — the **fallback pair**: Jikan (anime/MAL mirror) + Google Books (books) join the seam so every domain now has ≥2 independent providers; the MISSION-053 coordinator's parallel fan-out *is* the fallback mechanism (one provider failing never fails a search — verified by the existing coordinator tests) ✓ · `infrastructure/providers/jikan/`: `client.rs` — keyless `JikanClient` (5 wiremock tests incl. 429/404/503 + invalid JSON) ✓ · `response.rs` — snake_case v4 models (Anime w/ `r#type`, images.jpg large, aired from/to, duration string, rating, genres/themes/studios; Episode without an explicit number — Jikan omits it, so nodes use list ordinal) ✓ · `normalize.rs` — `content_type` (Movie→Movie, else Anime — mirrors AniList), `pub_status` (Finished Airing→Completed, Currently Airing→Ongoing, Not yet aired→Announced), `"24 min per ep"`→duration, ISO from/to→start/end dates, studios→`Studio` people, MAL CDN cover (large preferred), genres/themes→genres/tags, `mal` external id always attached (MAL-native ids must be stored regardless of provider — API_PROVIDERS §2); 1 fixture-driven test batch ✓ · `mod.rs` — `JikanProvider` (`PROVIDER_ID="jikan"`, `jikan_config()` = [Anime, Movie] @ **2 rps** [~3 rps, 60 req/min documented], caps search/details/nodes/images · auth None); provider ids are numeric MAL ids (validated), `get_nodes` via `/anime/{id}/episodes?limit=100`, `get_external_ids` returns the `mal` id; tests incl. type filtering + coordinator ✓ · `infrastructure/providers/googlebooks/`: `client.rs` — `GoogleBooksClient` (optional `api_key` query param like TMDB, MISSION-063 wires the real key; 5 wiremock tests incl. api_key assertion + 401→AuthRequired) ✓ · `response.rs` — **camelCase** models (`volumeInfo`, `industryIdentifiers`, `publishedDate`, `imageLinks`) with `rename_all="camelCase"` on every nested struct ✓ · `normalize.rs` — free-form publishedDate (`"1965"`/`"1965-08-01"`/`"0000-00-00"`→None), subtitle→alt_titles, categories→genres (capped 8), pageCount→pages, authors→Author credits, thumbnail cover, industryIdentifiers (ISBN_10/ISBN_13 only)→isbn10/isbn13 external ids ✓ · `mod.rs` — `GoogleBooksProvider` (`PROVIDER_ID="googlebooks"`, config = [Book] @ **1 rps** [~100 qpm quota], caps search/details/images · auth Key); volume ids validated (no `/`); search adds `printType=books`; tests incl. coordinator ✓ · `default_provider_entries()` now registers all **six** adapters ✓ · fixtures: jikan `search_anime.json` (HxH TV 11061 + GITS Movie 43), `details_anime.json` (studio Madhouse, duration "24 min per ep", aired ISO), `episodes.json` (2 eps, no explicit number); googlebooks `search_volumes.json` + `volume_details.json` (Dune, ISBN_10/13, pageCount 704) ✓ · **374 Rust** tests ✓ · cargo fmt ✓ · clippy `-D warnings` ✓ · gotchas: Jikan episodes carry **no episode number** (ordinal only — verified against the fixture, not assumed); Google Books **camelCase at every level** (missed rename on the outer `Volume`/`VolumesResponse` at first compile, caught by tests); `pub_status` treats Google's `"0000-00-00"` unknown-date as not-yet-published ✓ |
| MISSION-057 OpenLibrary adapter + fixtures | **DONE** (2026-08-15) — the **book provider**: free keyless REST, ~1 req/s, on the MISSION-053 seam · `infrastructure/providers/openlibrary/`: `client.rs` — `OpenLibraryClient` (reqwest, always sends `APP_USER_AGENT` with contact → earns the 3 req/s tier; `get<T>(path, params)`, `from_http_status` mapping [404→NotFound, 429→RateLimited, 5xx→ProviderDown], bad JSON → `InvalidResponse`; wiremock tests incl. a `user-agent` header matcher) ✓ · `response.rs` — **snake_case end-to-end**, plain field names (no rename_all — the fourth serde case after AniList/TMDB/MangaDex); `SearchResponse`/`SearchDoc` (work key, title, first_publish_year, cover_i), `WorkResponse` (`description` as a **`#[serde(untagged)]` enum** — plain string or `{value,…}` object), `WorkAuthor`/`AuthorRef`, `AuthorResponse`, `EditionsResponse`/`Edition` (isbn_10/isbn_13/lccn/oclc_numbers); only read fields declared ✓ · `normalize.rs` — pure mappers (7 tests): `work_id` (full `/works/OL89650W` key → bare `OL89650W` provider id), `content_type`→Book, `pub_status` (known first_publish_date→Completed), `year_from_date` (free-form `"1965"`/`"May 1989"`/`"1989-05-15"` → first 4-digit run), description string/object→stripped synopsis, `candidate` (cover via `covers.openlibrary.org/b/id/{cover_i}-M.jpg`, release_year), `media` (title, subjects→genres capped at 8, authors→`PersonRole::Author` credits, `pages` deliberately None — page counts live on editions not works), `external_ids` (first edition ISBN-10/ISBN-13/LCCN/OCLC → id-only ExternalIds — dedup payload), HTML sanitizer kept as a local copy (module-visibility keeps adapters self-contained) ✓ · `mod.rs` — `OpenLibraryProvider` (`PROVIDER_ID="openlibrary"`, `openlibrary_config()` = [Book] @ **1 rps**, caps search/details/images · nodes false [books have no chapter tree] · related false · auth None); `search` uses `/search.json?q=&limit=20&fields=…` and short-circuits non-book domains; `get_details` = work + **per-author resolution** (`/authors/{key}.json`, capped at 5, keys validated `OL*`); `get_external_ids` via `/works/{id}/editions.json?limit=1&fields=…`; provider ids are bare work keys (`OL89650W`); invalid ids → `InvalidResponse`; tests incl. coordinator fan-out + 404 mapping ✓ · `default_provider_entries()` registers all **four** adapters ✓ · fixtures: `search_books.json` (Dune → `/works/OL89650W`, cover 68486), `work.json` (`{value}` description + 10 subjects + `/authors/OL26523A`), `author.json` (Frank Herbert), `editions.json` (isbn10/13 + lccn + oclc) ✓ · **343 Rust** tests ✓ · cargo fmt ✓ · clippy `-D warnings` ✓ · gotchas: OpenLibrary is snake_case and free-form (dates/descriptions vary — the untagged enum + 4-digit-year extraction absorb it); author names are **not** in the work payload, requiring N+1 author calls; search rows carry no description so candidate synopsis stays None ✓ |
| MISSION-056 MangaDex adapter + fixtures | **DONE** (2026-08-15) — the **manga/manhwa/manhua/novel provider**: pure keyless REST transport + normalizer on the MISSION-053 seam, offline-tested against recorded fixtures, all policy coordinator-side ✓ · `infrastructure/providers/mangadex/`: `client.rs` — `MangaDexClient` (reqwest, `APP_USER_AGENT`, `get<T>(path, params)` preserving repeated query keys like `contentRating[]`/`includes[]`; maps non-2xx via `from_http_status` [404→NotFound, 429→RateLimited, 5xx→ProviderDown], bad JSON → `InvalidResponse`; wiremock tests: repeated-param assertion, 429/404/503, invalid JSON) ✓ · `response.rs` — serde models for MangaDex v5: **camelCase multi-word attributes** (`altTitles`, `originalLanguage`, `publishAt`, `fileName`) with `rename_all="camelCase"` on attributes structs (top-level `data`/`result`/`type` stay default); `Manga`/`MangaAttributes` (localized title/altTitles/description maps, originalLanguage, year, status, format, tags w/ `group` genre|theme, contentRating, links)/`Relationship` (author/artist name, cover_art fileName)/`Chapter` (volume, chapter, title, publishAt, pages); only read fields declared ✓ · `normalize.rs` — pure mappers (6 tests): `content_type` (format manga/one_shot/doujinshi→Manga · manhwa→Manhwa · manhua→Manhua · **novel→Novel** [LNs/web novels hosted on MangaDex]), `pub_status` (ongoing→Ongoing, completed/published→Completed, hiatus→Hiatus, cancelled→Cancelled), localized-title resolution (preferred `en` → original language → any), description HTML strip (shared sanitizer reused from the AniList adapter), cover via MangaDex CDN `covers/{id}/{file}.256.jpg`, `candidate`/`media` (release_year, language, content_rating, genres=genre-tags, tags=theme-tags, people=author/artist relationships deduped per `(role,name)`, `ch_count` deliberately None — `/manga` carries no count), `nodes` (feed → **Volume parents with Chapter children**, volume-less chapters top-level; chapter `position` by feed order, `number` keeps the raw `"1.5"` string, `release_date` from `publishAt`[..10], `page_count`), `external_ids` (links map → mal/anilist/kitsu/anime_planet/bookwalker/novelupdates/mangaupdates/amazon, mal+anilist+kitsu get canonical URLs, unknown sites dropped) ✓ · `mod.rs` — `MangaDexProvider` (`PROVIDER_ID="mangadex"`, `mangadex_config()` = content_types [Manga, Manhwa, Manhua, Novel] @ **4 rps** (~5 req/s documented), caps search/details/nodes/images · related false [no sequel/prequel edges] · auth None; UUID provider ids — no kind prefix needed); `search` adds `format[]` filter + filters hits by content_type, short-circuits non-comic domains to empty; `get_nodes` via `/manga/{id}/feed?translatedLanguage[]=en&limit=500&order[volume]=asc&order[chapter]=asc`; `get_external_ids` from details `links`; provider tests incl. a coordinator fan-out ✓ · `default_provider_entries()` now registers all three adapters ✓ · fixtures: `search_manga.json` (Berserk manga + Solo Leveling manhwa + Overlord novel), `details_manga.json` (Berserk: <p>-tagged description, HTML-stripped, ja original title, genre/theme tags, author+artist, 7 external links, cover), `chapter_feed.json` (vol 1×2 + vol 2×1 + volume-less side story) ✓ · **323 Rust** tests ✓ · cargo fmt ✓ · clippy `-D warnings` ✓ · gotchas: MangaDex uses camelCase multi-word attribute keys but snake for nothing else — exactly opposite pattern from TMDB, caught by fixture assertions; the search result set must be filtered client-side (the `format[]` param is best-effort); chapter numbers are strings that can be `"1.5"`/`null`, so `position` is the feed ordinal while `number` preserves the raw value ✓ · attribution note: MangaDex credit line + scanlation-group credits tracked for the UI ✓ |
| MISSION-055 TMDB adapter + fixtures | **DONE** (2026-08-15) — the **movies+TV provider** on the MISSION-053 seam: a pure REST transport + normalizer, offline-tested against recorded fixtures, all policy still coordinator-side ✓ · `infrastructure/providers/tmdb/`: `client.rs` — `TmdbClient` (reqwest, `APP_USER_AGENT`, `get<T>(path, params)` appends `api_key` when set [settings land MISSION-063 via keyring, never logged]; maps non-2xx via `ProviderError::from_http_status` [404→NotFound, 401/403→AuthRequired, 429→RateLimited, 5xx→ProviderDown], malformed JSON → `InvalidResponse`; wiremock tests: api_key query-param assertion, 429/401/404/503, invalid JSON) ✓ · `response.rs` — serde models mirroring TMDB's **snake_case** responses exactly (no `rename_all` — the opposite gotcha from AniList's camelCase GraphQL); `MediaDetails` shared by `/movie/{id}`/`/tv/{id}` with per-kind fields as `Option`; only read fields declared (unknown keys ignored, no dead-code warnings) ✓ · `normalize.rs` — pure mappers (9 tests): `content_type` (movie→Movie, tv→Tv, else Other), `pub_status` (Released/Ended→Completed · Returning Series/In Production/Post Production→Ongoing · Planned/Rumored→Announced · Canceled→Cancelled), image URLs via `image.tmdb.org/t/p/w500` posters + `original` backdrops, title selection (localized `title`/`name` → original-language), year from `YYYY-MM-DD`, `candidate`/`media` (start/end dates, movie runtime vs `episode_run_time[0]`, `number_of_episodes`→ep_count, country from `production_countries[0].iso_3166_1`, language, genres, homepage fallback to site url), `people` (crew Director→Director, Writer/Screenplay/Story/Teleplay→Author, Art/Design→Artist; production companies→Studio; TV networks→Network; **cast deliberately skipped** — no Actor role in the domain; dedup per `(role,name)`; Producer skipped), `season_tree` (Season→Episode nodes with air dates/runtimes/overviews; ids `tv-{id}-s{n}`/`tv-{id}-s{n}e{m}`), `external_ids` (imdb_id→IMDb w/ canonical URL, tvdb/tvrage/wikidata id-only) ✓ · `mod.rs` — `TmdbProvider` (`PROVIDER_ID="tmdb"`, `tmdb_config()` = content_types [Movie, Tv] @ **4 rps** (~40 req/10s), caps search/details/nodes/images · related **false** [TMDB exposes no sequel/prequel/adaptation edges, only recommendations → trait default `Unsupported`] · auth Key); **kind-prefixed provider ids** `movie-<id>`/`tv-<id>` since TMDB ids are per-kind (movie 603 ≠ tv 603); `get_nodes` = details (seasons) + one `/tv/{id}/season/{n}` per season ≥1 (specials skipped), movies → empty without any request; `get_external_ids` via the dedicated `/movie|tv/{id}/external_ids` endpoints; provider tests incl. a coordinator fan-out ✓ · `default_provider_entries()` now registers both AniList and TMDB (keyless until MISSION-063) ✓ · fixtures: `search_multi.json` (movie 603 + tv 1396 + sparse movie 42), `details_movie.json` (The Matrix: credits w/ 2 directors + writer + producer, 2 studios, external ids), `details_tv.json` (Breaking Bad: 62 eps, AMC network, Sony studio, IMDb/TVDB/TVrage/Wikidata ids, one season entry), `season_1.json` (Pilot + Cat's in the Bag), `external_ids_{movie,tv}.json` ✓ · **304 Rust** tests ✓ · cargo fmt ✓ · clippy `-D warnings` ✓ · gotchas: TMDB responses are snake_case (adding `rename_all="camelCase"` silently `None`s every field — caught by fixture assertions); reqwest 0.13 gates `.query()` behind a `query` feature (added to Cargo.toml); the season endpoint uses `season_number`/`air_date` snake while some third-party docs show camelCase ✓ · attribution note: TMDB logo/link required on displayed data/images — tracked for the UI work ✓ |
| MISSION-054 AniList adapter + fixtures | **DONE** (2026-08-15) — the first real provider: a **pure GraphQL transport + normalizer** behind the MISSION-053 seam, fully offline-tested against recorded fixtures, no policy code (that all lives in the coordinator) ✓ · `infrastructure/providers/anilist/`: `client.rs` — `AniListClient` (reqwest, `APP_USER_AGENT`, `graphql<T>` POSTs `{query, variables}` to `https://graphql.anilist.co`, maps non-2xx via `ProviderError::from_http_status` [429→RateLimited, 5xx→ProviderDown], GraphQL `errors`/missing `data` → `InvalidResponse`; wiremock tests: body-partial-json query/variables, 429, 503, GraphQL-error envelope) ✓ · `graphql.rs` — trimmed `SEARCH_QUERY` (`id/type/format/countryOfOrigin/title/coverImage/description/startDate/siteUrl`, no over-fetch) + `DETAILS_QUERY` (`…bannerImage/endDate/status/episodes/chapters/duration/genres/tags/studios/staff/relations/externalLinks`, one query serves details/nodes/related/external-ids) ✓ · `response.rs` — serde models with `rename_all = "camelCase"` + `Envelope<T>` (data/errors) + `Page`/`Media` renames; `normalize.rs` — pure mappers (14 tests): `content_type` (ANIME TV/TV_SHORT→Anime · MOVIE→Movie; MANGA NOVEL→**Novel** [LNs], country KR→Manhwa / CN→Manhua, else Manga; fallback Other), `pub_status` (FINISHED→Completed, RELEASING→Ongoing, NOT_YET_RELEASED→Announced, CANCELLED, HIATUS), `strip_html` on descriptions, `date_string` (missing month/day → earliest), title selection (romaji→english→native), `candidate`, `media` (start/end, ep/ch count, duration, genres+tags, banner/cover, main studio [isMain], people via `map_staff_role` Author/Artist/Director/Studio), `nodes` (flat episode list when no seasons, `get_nodes` only when ep/ch count present), `relations` (SEQUEL/PREQUEL→Sequels/Prequels, ADAPTATION→BasedOn), `external_ids` (allowlist: MAL/AniDB/IMDb/TMDB with numeric id; site→`ProviderId`; url-only links skipped) ✓ · `mod.rs` — `AniListProvider` (`PROVIDER_ID="anilist"`, `anilist_config()` = content_types [Anime, Manga, Manhwa, Manhua, Novel] @ 1.5 rps, caps search/details/nodes/related/images · auth None; `get_details` → NotFound on null `Media`), provider-level wiremock tests incl. a coordinator fan-out test ✓ · `providers/mod.rs` wires `default_provider_entries()` + `test_support::anilist_fixture` (reads `tests/fixtures/anilist/{name}`) and `infrastructure/mod.rs` registers `pub mod providers;` ✓ · fixtures: `search_anime.json` (Cowboy Bebop id=1 + One Piece id=21), `details_anime.json` (full Cowboy Bebop: dates, 26 eps, Sunrise main studio, staff, SEQUEL→30013 + ADAPTATION→manga 2, MAL/AniDB/IMDb/TMDB external ids), `details_ln.json` (Classroom of the Elite id=106626 MANGA/NOVEL, 16 volumes, MAL id=85653) — captured as faithful full `{data:…}` envelopes ✓ · **280 Rust** tests ✓ · cargo fmt ✓ · clippy `-D warnings` ✓ · gotchas: serde needs `rename_all = "camelCase"` on every model (a missing rename silently `None`s the field — surfaced only by the fixture-driven assertions); fixtures parse as `Envelope<T>` (full responses), not bare payloads ✓ · policy still coordinator-side: a live hit is un-tested by design (MISSION-058+ adds network-integration gates) ✓ |
| MISSION-053 Provider trait + capabilities + Coordinator | **DONE** (2026-08-14) — the **capability-based provider seam** (ARCHITECTURE §4, ADR-004) is in place and the entire policy engine is HTTP-agnostic and unit-tested with fake adapters + paused time; no transport code yet (adapters land MISSION-054+) ✓ · `domain/provider/`: `Provider` trait (`search`/`get_details`/`get_nodes`/`get_related`/`get_external_ids` — optional ops default to `Unsupported`, `Send + Sync` via `async-trait`), `ProviderCapabilities` (search/details/nodes/related/reviews/images/seasonal + `AuthKind` None/Key/OAuth, all off by default so an adapter must declare what it implements), and the normalized metadata types (`ProviderCandidate`, `ProviderMedia`, `ProviderNode` — recursive, mirrors `content_node` —, `ProviderRelation`, `ProviderPerson`) that mirror the local aggregates so import/enrich is a straight copy and provider JSON never leaks past the adapter ✓ · `ProviderError` map: `RateLimited{retry_after}`/`Timeout`/`ProviderDown`/`NotFound`/`AuthRequired`/`Http`/`InvalidResponse`/`Canceled`/`Unsupported`/`Transport` with `from_http_status` (429→RateLimited, 401/403→AuthRequired, 404→NotFound, 5xx→ProviderDown), `is_retryable` (rate-limit/timeout/down/transport = retry; auth/404/invalid/cancel/unsupported = never) and `retry_after` carrying the server hint — 8 tests ✓ · `application/providers/`: `ProviderConfig` (enabled, `content_types` routing, api_key — never logged, timeout, max_retries, backoff_base/max, `RateLimitConfig` rps), `RateLimiter` (serialized request-start spacing `1/rps`, one shared limiter per provider so parallel fan-out respects AniList ~90/min · Jikan 3 rps · OpenLibrary 1 rps; `reserve()` is the synchronous slot math so the Mutex guard never crosses an `await`; 4 tests incl. paused-clock sleep verification) ✓ · `ProviderCoordinator`: `execute()` applies policy in order — cancel check → rate-limit → per-attempt `timeout` → retry with **exponential backoff + jitter** (25%, deterministic xorshift, capped at `backoff_max`, honoring server `Retry-After`) — re-invoking the op closure so a dropped/timed-out request is never retried with a stale future; `search_all()` fans out over enabled + capable providers (empty `content_types` = domain-agnostic) via `JoinSet`, aggregates **partial hits + per-provider failures** (one bad provider never fails the search), re-emits in registration order, and `providers()` exposes the capability snapshot for the MISSION-063 settings UI ✓ · cooperative `CancellationToken` (`Arc<AtomicBool>` + `tokio::sync::Notify`, no `tokio-util`) — dropping the caller's future cancels in-flight work; a missed notify is harmless because every attempt boundary re-checks the flag ✓ · Cargo: `tokio` promoted to a main dependency (`macros/rt-multi-thread/time/sync`) + `async-trait`; `tokio` dev-deps add `test-util` for deterministic time ✓ · tests: retry-then-succeed (3 attempts), no-retry-on-404 (1 call), timeout→`Timeout`, cancel-wakes-backoff→`Canceled`, `search_all` aggregates success+failure, disabled/unsupported providers skipped by routing, backoff growth/cap/Retry-After, jitter determinism, parallel `execute` shares the limiter — **261 Rust** ✓ · cargo fmt ✓ · clippy `-D warnings` ✓ · no IPC/UI changes — external search + settings land with MISSION-059/063 ✓ |
| MISSION-052 Novel/web-novel tracking UX | **DONE** (2026-08-14) — the per-media **Normal (autoTrack) vs Manual** mode and **DNF-with-progress** for dropped titles, on top of the already-shipped chapter-list read-state rows (MISSION-046/047 NodeTree: per-chapter state + range marks + next-chapter) ✓ · migration `0008_tracking_mode.sql`: `tracking.auto_track INTEGER NOT NULL DEFAULT 1` (Normal = the MISSION-048 auto-complete rule stays on; Manual opts out) ✓ · `domain::tracking::Tracking.auto_track` + repo read/write roundtrip; `TrackingService::sync_auto_status` now returns `None` when `auto_track` is off — **Manual mode means progress marks never change the status** (the application-layer gate the status engine's design docs describe) ✓ · new `set_auto_track(media_id, enabled)` (+ IPC `tracking_set_auto_track`) toggles the mode, creates a planned row when untracked, and **turning Normal back on re-syncs** the status to the current progress so the change is immediately visible ✓ · DNF-with-progress: `TrackingView` now carries `progress` (percent + completed/total + `next_label` via the batched `progress_summaries`), so the Tracking tab shows **"Stopped at Ch3 · 66%"** for dropped titles — the derived node states are the source of truth, nothing re-stored (REQ-TRACK-004) ✓ · Tracking tab gains the **Normal/Manual segmented toggle** (lucide Zap/Hand, `aria-pressed`, toast on failure) and the stopped-at MetaItem; i18n en/ar (`modeTitle/modeHint/modeNormal/modeManual/dnfTitle/setModeErrorToast`) ✓ · tests: repo roundtrip, `manual_mode_blocks_auto_complete_until_normal_resumes`, `set_auto_track_toggles_mode_and_survives_transitions`, `view_includes_derived_progress_for_dnf` + 6 TrackingTab tests (toggle default/untracked/switch/error, stopped-at with/without progress) — **238 Rust** + **184 TS** ✓ · tsc ✓ · eslint 0 errors ✓ · prettier ✓ · clippy `-D warnings` ✓ · codegen:check ✓ · note: research's "Defaults to Manual" is adapted — since the release-feed Normal (MISSION-107 calendar) isn't built yet, Normal maps to the shipped auto-complete rule and is the default; Manual is the explicit per-media opt-out ✓ |
| MISSION-051 Activity log writes on all tracking actions | **DONE** (2026-08-14) — every tracking action now appends to the `activity` table (MISSION-017 schema + MISSION-019 repo), giving the calendar/stats/undo consumers a real event trail ✓ · `application/activity_service.rs`: `log_tracking` (mints `a-{uuid}` + RFC3339 `created_at`, always sets `media_id`, optional `node_id`, JSON `meta`) is **best-effort** — a failed write is `tracing::warn!`-logged and swallowed so the user's action never fails or rolls back ✓ · `log_status_transition` maps the resulting status to an activity kind (`in_progress`→`started`, `completed`→`completed`, `repeat`→`repeat`; on_hold/dropped/wishlist/planned have no kind → nothing written) with meta `{"from":…,"to":…}`; `log_progress` writes a per-node `progress` entry with meta `{"state":…}` ✓ · wired into `TrackingService::set_status` and `sync_auto_status` (so the auto-complete rule's planned→in_progress→completed lifecycle is recorded), `BulkService::set_status` (per media, same path), and `ProgressService::{set_node_progress, set_range_progress, mark_next_unit}` (one `progress` entry per written node) ✓ · tests: `set_status_logs_activity_for_tracked_statuses` (started/completed/repeat newest-first + from→to meta, on_hold skipped), `auto_complete_logs_started_and_completed_activity`, `set_node_progress_logs_progress_activity`, `range_logs_progress_per_written_node`, `mark_next_unit_logs_progress_activity`, `set_status_logs_activity_per_media` (bulk) — **235 Rust** ✓ · cargo fmt ✓ · clippy `-D warnings` ✓ · no IPC/UI changes — reads arrive with the calendar/stats missions (REQ-STAT-001, REQ-CAL-001) ✓ |
| MISSION-050 Dashboard widgets: Continue Reading/Watching, Recently Added/Completed, Quick Actions | **DONE** (2026-08-14) — the app now lands on a calm, empty-by-default **Dashboard** (REQ-DASH-001): **Dashboard is `NAV_ITEMS[0]`** and the root redirects to `/dashboard` (UX research lists the Dashboard first in the left rail); the TopBar's h1 owns the page title, so the page is a labeled `<section>` with no duplicate heading ✓ · Rust data: `tracking` repo gains `recent_media_by_status(pool, statuses, order, limit)` — joins media↔tracking onto the `MediaSummary` row shape, `RecentOrder::{UpdatedAt, FinishedAt}` (updated_at DESC / finished_at DESC with NULLs last), empty-statuses guard, 3 repo tests ✓ · `application/dashboard_service.rs`: `DashboardService::summary(limit)` clamps 1..=20 and resolves **continue** (core_status in_progress/repeat, updated desc), **recently_completed** (completed, finished desc), **recently_added** (`media::list` created_at desc), all mapped through `MediaService::to_list_items` (made `pub(crate)`) so every widget row carries its MISSION-049 progress summary + next-unit control ✓ · IPC: `dashboard_summary(limit|null) → DashboardSummary` in `commands/dashboard.rs` (registered in `lib.rs`); contract gains `types.MediaListItem` + `types.DashboardSummary` (media_list/search returns refactored to `MediaListItem[]`), codegen regenerated ✓ · UI: `features/dashboard/api.ts` (`useDashboardSummaryQuery`, key `["dashboard","summary"]`) + `DashboardPage` widget grid — a full-width Quick Actions card (Add title via the reusable `AddMediaDialog`, Quick capture by dispatching `mylore:open-quick-capture`, Search → navigate `/search`) plus Continue/Recently-completed/Recently-added cards that reuse `MediaRow` (progress bar + next-unit overlay included); each widget shows a calm one-line empty hint; skeletons + error/retry mirror the library ✓ · cache wiring: `queryKeys.dashboard.all()` invalidated from every progress/status/add/delete path — `invalidateProgress` (progress.ts), `useAddMedia`, `useSetStatus`, `useBulkSetStatus`, `useBulkDelete`, `useDeleteMedia`, `useRestoreTrashItem` ✓ · i18n `dashboard.*` + `nav.dashboard`/`nav.hint_dashboard` en/ar, parity test green ✓ · gotchas: the page's own `<h1>` duplicated the TopBar h1 (removed); the root-redirect test raced the TopBar title vs widget content (assert the widget empty text); adding Dashboard first changed the palette arrow-nav order, so that test now starts at `/dashboard` and arrows into Library ✓ · tests: `DashboardPage.test.tsx` (widget grid rows per section, per-widget empty states, add-dialog open, quick-capture event dispatch, search navigation, error+retry — 6) + app.test/palette.test updates — **178 TS (27 files)** ✓ · **229 Rust** ✓ · tsc ✓ · eslint 0 errors ✓ · prettier ✓ · codegen check ✓ · cargo fmt ✓ · clippy `-D warnings` ✓ |
| MISSION-049 Quick capture popover (global hotkey) + in-grid quick controls | **DONE** (2026-08-14) — two delivery paths for one-tap progress: a global **Quick capture** popover (Mod+Enter or the palette's new "Quick capture" action, which dispatches the `mylore:open-quick-capture` window event so the palette stays decoupled) and **in-grid quick controls** on every library card/row ✓ · Rust data: `tracking` repo gains `progress_summaries(pool, ids)` — batched in chunks of 500 with two windowed queries per chunk (aggregate per-media weight totals over `UNIT_KINDS` episode/chapter/node with book chapters weighted by `COALESCE(page_count,1)`, plus a `ROW_NUMBER()` first-unread window for the next node) — plus `next_unread_unit` (skipped nodes remain candidates) + 4 repo tests ✓ · `application/progress_service.rs`: `ProgressSummary`/`NodeProgressNextView` DTOs (percent/completed/total/next_label/next_node_id), `ProgressService::mark_next_unit` (mint-timestamped write through the media's `ProgressTemplate::consuming_state`, then `sync_auto_status`, then a fresh summary; `Ok(None)` when nothing is left to mark) + module-level `summary_dto`/`unit_label` (E{n}/Ch{n}/#{n} with display-position fallback) + 4 service tests ✓ · `MediaListItem` (list + search) now carries `progress` attached in constant query count via the batched repo call ✓ · IPC: `node_progress_next(media_id) → NodeProgressNextView | null` in `commands/node.rs` (registered in `lib.rs`); contract gains `types.ProgressSummary` + `types.NodeProgressNextView` and `progress` on the `media_list`/`media_search` returns, codegen regenerated ✓ · UI: `features/library/progress.ts` (`consumingStateFor` watched/read, `unreadUnits` tree flatten, `nodeUnitLabel`, `useMarkNextUnit`, `useMarkRange` — all invalidating `media.all()`/`tracking.all()`/`search.all()`/`media.nodes(id)`) + `ProgressBar.tsx` (thin accent bar, hidden until >0) + `NextUnitButton` (labeled pill over the card poster / icon-only on rows; rendered as an **absolute sibling of the card/row `<Link>`** so no nested interactive elements; `pe-10` reserves row space; hidden in select mode and when `next_node_id` is null) + `QuickCapture` (Radix Dialog split into a shell owning open-state/shortcut and a panel that mounts only while open — so the shell survives renderers without a QueryClientProvider, cf. the preferences tests; left type-ahead via `useMediaSearchQuery`, right panel marks next or up-to-N via `node_progress_range` from the first unread unit, all-caught-up state, loading/error/retry states, per-action success toasts and an info toast on the null next) mounted in AppShell ✓ · i18n `quick.*` en/ar (ar ICU plurals `rangeDone_zero/two/few/many`), parity test green ✓ · gotchas: `mark_next_unit`'s `Ok(None)` vs a *still-has-next* summary tripped two early tests (fixed by seeding a 3rd episode / asserting the true next label), `unit_label`'s `&row.next_kind` was `&Option<String>` not `&str` (deref it), and `summaries.get(&row.id)` borrowed a moved `row` (capture inside the closure); QuickCapture's initial query hooks crashed the provider-less preferences tests until the shell/panel split ✓ · tests: `progress.test.ts` (unread flatten/order, consuming-state map, labels), `NextUnitButton.test.tsx` (pill + row icon, mark-next IPC, null→info toast, hidden when done / in select mode), `QuickCapture.test.tsx` (palette-event + Mod+Enter open, type-ahead pick, mark next, range 1..N, all-caught-up) — **172 TS (26 files)** ✓ · **226 Rust** tests ✓ · tsc ✓ · eslint 0 errors ✓ · prettier ✓ · codegen check ✓ · cargo fmt ✓ · clippy `-D warnings` ✓ |
| MISSION-048 Status transitions + auto-complete rule; repeat counter | **DONE** (2026-08-13) — the media detail **Tracking** tab is now a real status engine UI: exclusive picker over the 7 core statuses (colored pills per the status palette, `role=group` + `aria-pressed`, active pill gets a tinted fill + ring + Activity icon) that drives `tracking_set_status(media_id, core_status)` → domain `apply_transition` (started_at stamped on active, finished_at on terminal, Repeat guard = must have consumed before, repeat_count increments entering Repeat / resets leaving it) ✓ · Rust: `application/tracking_service.rs` — `TrackingService::{get, set_status, sync_auto_status}` + `TrackingView` (media_id/core_status/custom_status_id/started_at/finished_at/repeat_count/updated_at) + shared `existing_to_domain`/`domain_to_record` helpers (moved out of `bulk_service`, which now imports them — no duplication); media-existence + `CoreStatus::from_str` validation, failed transitions write nothing ✓ · auto-complete rule wired into `ProgressService` (`set_node_progress` + `set_range_progress`): after any progress write, `sync_auto_status` runs the MISSION-024 `suggest_auto_status` → `apply_transition` — but only when the current status is engine-owned (planned/in_progress/completed); manual statuses (on_hold/dropped/wishlist/repeat) are never auto-overridden; fully reversible (un-marking completed → in_progress → planned); a sync failure is `tracing::warn!`-logged and swallowed so the UI never rolls back a persisted change; media with no node data → no suggestion → no row ✓ · `tracking` repo gains `node_ticks_for_media` (LEFT JOIN node_progress, `COALESCE(np.state,'unread')`, ordered by position, id) so the aggregate counts unmarked nodes as unread ✓ · IPC: `tracking_get(media_id) → TrackingView | null`, `tracking_set_status(media_id, core_status) → TrackingView` in `commands/tracking.rs` (contract `types.TrackingView` + codegen regenerated) ✓ · UI: `features/library/tracking.ts` (`useTrackingQuery` keyed `tracking.detail(mediaId)` — `queryKeys.tracking.detail` retyped from number to string, the dead `.node` fan-out removed; `useSetStatus` seeds the detail cache with the returned row) ✓ · `useNodeProgress` (api.ts) invalidates `queryKeys.tracking.all()` after both node and range writes so the tab reflects auto-completes ✓ · `TrackingTab.tsx`: skeleton, error+retry (`EmptyState`), untracked hint, status pills, and a meta grid (Started/Finished dates via `toLocaleDateString`, Re-read run `#N` badge only while core_status=repeat) ✓ · i18n `tracking.*` en/ar (parity kept; dead `detail.trackingPlaceholder` removed) ✓ · gotcha: the manual-repeat UI test initially tried `on_hold → repeat`, which the transition matrix correctly rejects — a repeat run must start from a consumed state (completed first) ✓ · tests: `tracking_service` Rust suite (create+stamps, repeat increments/resets, Repeat guard writes nothing, unknown status/media reject, get None/after-write, auto-complete partial→in_progress→completed+finished_at, reversibility to planned, manual statuses never auto-overridden, no node data → no row — 9) + `TrackingTab.test.tsx` (pills + current selection, untracked hint, apply+reflect, repeat counter, dates, error+retry, rejected-transition toast — 7) + MediaDetailPage tracking-tab block updated; 218 Rust tests, cargo fmt ✓, clippy ✓, 153 TS tests, tsc ✓, eslint 0 errors ✓, prettier ✓, codegen check ✓ |
| MISSION-047 Per-node progress commands + optimistic UI | **DONE** (2026-08-13) — the content tree rows now toggle progress end to end: `node_progress_set(node_id, node_state)` marks one node read/watched/skipped/unread and `node_progress_range(media_id, from_id, to_id, node_state)` marks every node between two rows in the media's display order, resolving with the affected node ids for optimistic reconciliation ✓ · Rust data: `tracking` repo gains `set_node_progress` (upsert + `read_at` stamped only on completed states) and `set_range_progress` (single `INSERT OR IGNORE` across the pre-order slice, aggregated per node to mixed/read/watched so the UPDATE-or-IGNORE ambiguity never hits the upsert's UNIQUE constraint, strict count validation guards id drift) ✓ · `application/progress_service.rs`: `ProgressService::set_node_progress` (state parsed through `ProgressState` → invalid enum rejects) + `set_range_progress` (anchors → `NodeService::preorder` slice → affected ids) + 6 tests ✓ · `NodeService::tree_for_media` attaches per-node `state`; `ContentNode` gains a required `state: string | null` field (contract `types` + codegen regenerated) ✓ · UI: `useNodeProgress(mediaId)` optimistic hook in `features/library/api.ts` — commits the new state to the `["media","nodes",id]` cache immediately, rolls the cache back if the IPC write rejects (error toast), and for ranges reconciles the cache with the server-returned affected ids after success ✓ · `NodeTree` renders a per-row progress checkbox (`role=checkbox`, `aria-checked` incl. `mixed` for partial, `aria-label` carries the mark/unmark verb — watched for anime/tv/movie, read otherwise), a hover-revealed skip button (skip ↔ unread), shift-click range marking over the pre-order regardless of which rows are expanded, Shift+Enter on the row as the keyboard equivalent, and a range-hint caption ✓ · i18n `progress.*` en/ar ✓ · gotchas: the checkbox label built as `` t(`progress.toggle${complete}`) `` produced the lowercase key `progress.togglewatched` that never matched — switched to an explicit watched/read branch ✓ · `userEvent.click` has no `shiftKey` option, so the shift-click range test uses `fireEvent.click(el, { shiftKey: true })` ✓ · the generated `ContentNode.state` is required, so both NodeTree/MediaDetailPage test fixtures gained `state: null` on every literal ✓ · tests: `NodeTree.test.tsx` grows to 11 (single mark/unmark via checkbox, shift-click range + affected-id reconcile, skip toggle, anime→watched consuming state, pre-applied completed state rendering); 146 TS (22 files) + 208 Rust tests, tsc ✓, eslint 0 errors ✓, prettier ✓, codegen check ✓, cargo fmt ✓, clippy ✓ |
| MISSION-046 Node tree endpoint + UI (seasons→episodes, volumes→chapters; expand/collapse) | **DONE** (2026-08-13) — the media detail **Details** tab now renders the content tree: new `media_nodes(id)` IPC returns the nested tree (roots and every sibling group ordered by position, then id; unknown media → empty) built by `NodeService::tree_for_media` (`application/node_service.rs`) from the existing node repo rows — group by parent + recurse ✓ · IPC contract gains a `types` section (named interfaces for recursion) and the codegen script emits them as `export interface` before the command wrappers (`ContentNode` self-referencing `children: ContentNode[]`) ✓ · UI: `NodeTree` component (`features/library/NodeTree.tsx`) with full WAI-ARIA tree semantics — `role=tree`/`treeitem`, `aria-level`/`aria-posinset`/`aria-setsize`/`aria-expanded`, roving tabindex, Arrow ↑/↓ moves focus between visible rows, Arrow →/← expands/collapses; roots start expanded (seeded once), per-node chevron toggle, leaf rows render a spacer instead of a toggle ✓ · row labels compose kind + number + title ("Episode 12 · The Beginning"), `is_special` rows get a Special badge, duration/pages counts render with ICU plurals ✓ · i18n `nodes.*`/`nodeKind.*` en/ar (full Arabic plural forms zero/two/few/many/other for duration + pages, parity test green) ✓ · gotcha: seeding "expand all roots" in the tree must skip while the query is pending (data still empty) and seed when rows arrive — the first-seed guard used to run on the empty `data ?? []` and never re-seed ✓ · per-node progress markers are deliberately out of scope (MISSION-047) ✓ · tests: `NodeTree.test.tsx` (tree semantics + labels, page counts + special badge, collapse/expand toggle, empty, error+retry, arrow-key nav — 6) + Details-tab block in `MediaDetailPage.test.tsx` + `node_service` Rust suite (nested ordering, flat roots, position→id tiebreak, field mapping, unknown media — 5); 141 TS (22 files) + 201 Rust tests, tsc ✓, eslint 0 errors ✓, prettier ✓, codegen check ✓, cargo fmt ✓, clippy ✓ |
| MISSION-045 Bulk-select mode + action bar (status, tag, list, delete, export later) | **DONE** (2026-08-13) — library rows/cards enter a select mode (Select / Select-all / Clear / Exit toolbar controls; `selectable` + `selected` + `aria-pressed` toggle buttons with a corner checkbox) and an action bar offers: set status, add tag, add to list (collection), delete with group undo, and a disabled Export button ✓ · Rust `BulkService` (`set_status` via the domain `apply_transition` engine — missing tracking rows start from `Planned`, Repeat-guard applies, per-id transition failures are wrapped with the offending id and write nothing; `add_tag` resolves-or-creates personal-scope tags by exact name with trim/collapse-whitespace normalization + `INSERT OR IGNORE` links; `delete` returns `Vec<String>` trash ids for the single group-undo toast; `list_collections` + `add_to_list` minimal add-to-collection) with repo helpers `resolve_personal_tag`/`create_personal_tag`/`add_tag_to_many` + 11 tests ✓ · IPC: `tracking_bulk_set_status`, `media_bulk_add_tag`, `media_bulk_delete`, `collection_list`, `collection_bulk_add` in `commands/bulk.rs` (contract + codegen regenerated) ✓ · i18n `bulk.*`/`coreStatus.*`/`library.select*` en/ar with ICU plurals (ar zero/one/two/few/many/other for the delete-undo toast) ✓ · gotchas: React Query v5 per-call callbacks die on unmount, so the bulk delete-undo restore runs through `Promise.all(mutateAsync(trash_restore))`; a fresh tracking row had an empty `updated_at` that failed the validation inside `apply_transition` until `updated_at` was fed into `existing_to_domain` ✓ · **export intentionally deferred** (button disabled with a tooltip) and collection **creation** stays with MISSION-073; delete-to-trash + undo reuses the MISSION-044 `trash_restore` path ✓ · tests: `LibraryPage.bulk.test.tsx` (enter/exit select mode, select-all, status menu, tag dialog, list picker, delete + undo toast, export disabled — 8) + Rust bulk-service suite; 134 TS (21 files) + 196 Rust tests, tsc ✓, eslint 0 errors (8 pre-existing warnings) ✓, prettier ✓, codegen check ✓, cargo fmt ✓, clippy ✓ |
| MISSION-044 Trash/restore UI + undo toast for deletes | **DONE** (2026-08-13) — soft-delete end to end (REQ-MEDIA-007): delete archives a JSON before-image and cascades the row away ✓ · Rust data layer: `trash` repository (`insert/get/list/mark_restored/purge`; rows from migration 0006 — `id TEXT PK` minted `t-{uuid}`, `kind CHECK ('media','merge','bulk')`, `payload` full `MediaRecord` before-image, `deleted_at`, `restored 0/1`) + `TrashService` (`delete_media` = snapshot then `media::delete`, `restore_media` = deserialize + `media::create` + `mark_restored`, `list_trash` filters `restored=0` with the title parsed from payload, `purge`) + 7 tests ✓ · IPC: `media_delete(id)→trash_id` (string the undo toast rides on), `trash_list()→TrashItem[]`, `trash_restore(id)→void`, `trash_purge(id)→void` — contract in `scripts/ipc-contract.json`, codegen regenerated (`MediaRecord`/`AltTitle`/`ExternalId`/`MediaRelation` now also `Deserialize`) ✓ · UI: delete button on the media-detail hero offers an undo toast whose Action calls `trash_restore`; real Trash page at `/trash` (Trash nav item appended last with Trash2, keeps command-palette arrow order) with Restore / Delete-forever (confirm dialog; dialog button carries an `aria-label` so row vs dialog buttons stay distinguishable in tests) + `useTrashListQuery`/`useDeleteMedia`/`useRestoreTrashItem`/`usePurgeTrashItem` (`features/trash/api.ts`, new `queryKeys.trash` scope) + i18n `trash.*`/`nav.trash` en/ar with full ICU plurals (ar zero/one/two/few/many/other) ✓ · React Query v5 gotcha: per-call `mutate(vars, { onSuccess })` callbacks only run while the mutation's observer has listeners, so the undo restore-toast silently vanished after `navigate("/library")` unmounted the page — the delete toast (pushed while still mounted) worked, the restore one (pushed after unmount) didn't; fixed by using `mutateAsync().then()` which resolves regardless of listeners ✓ · also `toast.tsx` switched `Date.now()` ids to a `useRef` counter and `test/setup.ts` gained pointer-capture polyfills (Radix Toast needs `hasPointerCapture` in jsdom) ✓ · tests: `TrashPage.test.tsx` (empty/list/restore-toast/purge-confirm/retry — 5) + delete→undo and delete-error blocks in `MediaDetailPage.test.tsx`; 126 TS + 182/7 Rust tests, tsc ✓, eslint 0 errors ✓, prettier ✓, codegen check ✓, cargo fmt ✓, clippy ✓ |
| MISSION-043 Local search (FTS) in header + results page (local-first) | **DONE** (2026-08-12) — header search box (`HeaderSearch` in TopBar, `role="search"`, focus-within accent) navigates to `/search?q=` and mirrors the URL query while on the search page (input remounted via `key` on the URL query — no state-sync effect) ✓ · real `SearchPage` replaces the placeholder: reads `q` from the URL, `useMediaSearchQuery(query)` hook (keyed `queryKeys.search.local(trimmed)`, `enabled` only for non-blank trimmed queries) → skeleton → result-count header + `MediaRow` list (reused from the library) → no-results / error+retry / initial prompt states ✓ · data layer: `media_search` IPC command — Rust `MediaService::search_media` (trims + normalizes, blank → empty vec) wraps the existing repo `media::search` (FTS via unicode61 prefix + trigram fallback) and maps rows through `media_list_item` ✓ · contract entry added to `scripts/ipc-contract.json` + codegen regenerated (`ipc.generated.ts`) ✓ · i18n `search.*` en/ar keys with parity + ICU plurals (`resultsCount`, ar zero/one/two/few/many/other) ✓ · tests: `SearchPage.test.tsx` (initial prompt, query results, no-results, multi-row — 4) + header-search block in `shell/a11y.test.tsx` (submit → `/search?q=`, blank query no-navigate — 2) + server `search_media_returns_matching_summaries` + `search_media_blank_query_resolves_empty`; command-palette nav test updated to the real SearchPage initial hint ✓ · 119 TS + 172 Rust tests, tsc ✓, eslint 0 errors ✓, prettier ✓, codegen check ✓, cargo fmt ✓, clippy ✓ | `MediaDetailPage` (/library/:id): hero with `Link` back-to-library, poster placeholder (2:3, type icon), title + original title, content-type/status badges + release year, synopsis ✓ · detail navigation: framework-free `Tabs` primitive (Radix-free, `useId` wiring, `role=tablist` + `aria-selected`/`aria-controls`/`aria-labelledby`, full keyboard nav ←/→ wrap + Home/End focus-move without changing selection, only the active `role=tabpanel` renders) ✓ · tabbed sections: **Overview** renders the full aggregate facts (format, status, year, language, country, pages/episodes/chapters/duration, genres as badges) via `MetaCell`; **Details / Tracking / Review** are placeholders wired to MISSION-046 / MISSION-074 ✓ · loading skeleton (hero + tab strip + body), error state with Retry (`refetch`), not-found state for unknown ids ✓ · data layer: `media_get` IPC command (Rust service `get_media` + repo `media::get` returns the full `MediaRecord` with alt titles/people/genres/tags/external ids/relations; `to_record` maps the domain aggregate; `MediaRecord`/`AltTitle`/`ExternalId`/`MediaRelation` now `Serialize`) ✓ · `useMediaDetailQuery(id)` + `queryKeys.media.detail(id)` re-keyed from number to string ✓ · grid/compact cards + list rows are now `<Link>`s to the detail page (aria-label = title; library tests query cards by role link) ✓ · route table (MISSION-032) gains `library/:id` ✓ · i18n `detail.*` en/ar keys with parity kept ✓ · tests: `MediaDetailPage.test.tsx` (hero/badges/back-link/invoke args, overview facts + genre badges, tab keyboard switch + shell placeholders, error→retry, not-found — 5) + `Tabs` primitive tests (semantics, click select, arrow-key nav — 3); server tests `get_media_returns_full_aggregate` + `get_media_returns_none_for_unknown_id`; 113 TS + 170 Rust tests, tsc ✓, eslint 0 errors ✓, prettier ✓, codegen check ✓, cargo fmt ✓, clippy ✓ |
| MISSION-041 Library filter panel + sort menu + group-by + facets endpoint | **DONE** (2026-08-12) — filter panel: `LibraryFilterBar` (type/format/pub-status/genre/tag/year/favorite) + sort menu (title/last-updated/release-year, asc+desc) + group-by (none/content-type/pub-status/year) ✓ · facets stay true to data: new `media_facets` command returns distinct `formats`/`genres`/`tags`/`years` present in the library (repo `facets()`, genre/tag rows carry id+name) so options never drift from rows ✓ · `media_list` gains `format` + `year` filters (IPC contract + codegen regenerated) ✓ · `filters.ts`: `LibraryFilters` → typed `MediaListArgs` (`filtersToArgs`), `activeFilterCount`; `useMediaListQuery` now keys on args so filtering re-queries ✓ · `grouping.ts`: `buildLibraryRows` turns the sorted list into virtualized rows interleaving group-header rows with item rows — enum-ordered groups (content type 10-kind schema order, pub status 6-kind), year descending, `null` release year bucketed last as "Unknown"; `VirtualizedLibrary` virtualizes headers + items at 36px vs density row sizes ✓ · no-results state with clear-filters when a filter matches nothing (library empty + no filters still shows the add-first EmptyLibrary) ✓ · tests rewritten around route-typed `mockLibrary` (11, incl. filter/sort rewriting `media_list` args) + 6 `grouping.test.ts` unit tests; 105 TS, 168 Rust, tsc ✓, eslint 0 errors ✓, prettier ✓, codegen check ✓ |
| M4 · foundation review fixes (type scale, switcher a11y, dead keys, shared labels) | **DONE** (2026-08-12) — post-037 review-fix pass ✓ · Tailwind `@theme` type scale now mirrors `tokens.css` (§3): added the missing `--text-md:1rem` so the TopBar `<h1>` and EmptyState `<h2>` render at 16px (were silently body-sized via the dead `text-md` class) and aligned `text-xs/sm/base` to 12/13/14 (was 13/14/16) ✓ · a11y: added `role="group"` to the TopBar theme switcher and `LanguageSwitcher` wrappers so their `aria-label`s are actually exposed to assistive tech (Settings already had it) ✓ · i18n: removed the dead `page.status_bar` keys (en/ar) — `StatusBar` uses `shell.status.*` ✓ · DRY: extracted `LANGUAGE_SHORT_LABELS` into `i18n/index.ts`; `LanguageSwitcher` + `SettingsPage` now share it instead of two identical per-file maps ✓ · token-scale compliance: Settings sections `rounded-xl p-5` → `rounded-md p-6`, segmented pickers `px-3.5 py-1.5` → `px-3 py-1` (match TopBar), EmptyState `py-16` → `py-12` ✓ · tests: `preferences.test.tsx` settings groups now scoped to `within(main)` (TopBar + Settings both expose Theme/Language groups) ✓ · 92/92 TS tests, tsc ✓, eslint 0 errors ✓, prettier ✓ |
| MISSION-037 A11y baseline: focus ring, reduced-motion, semantic shell, screen-reader labels | **DONE** (2026-08-11) — `SkipLink` (first tab stop) slides into view on `:focus-visible` (new `.skip-link` styles on tokens; slides via transform + ease-out, safe under reduced-motion) and moves real focus to `main#main-content` (`tabIndex=-1`, `outline-none`) on activation ✓ · semantic shell audited: nav landmark gets a proper translated `a11y.navTitle` label (was the page label), `banner`/`main`/`contentinfo` landmarks verified, headings per page ✓ · screen-reader pass: TopBar theme group `a11y.theme`, LanguageSwitcher `a11y.language` (was hardcoded English), Dialog close button gains `closeLabel` prop (translated `a11y.close`, passed by the palette) ✓ · new `a11y.*` en/ar keys (parity kept) ✓ · tests (`components/shell/a11y.test.tsx`): landmark roles + main id, skip link is first tab stop + moves focus to main on Enter, control labels (Light/EN/ع, Language/Theme groups), palette combobox + Close names, Arabic landmark translation; focus ring + reduced-motion were already in `global.css` (MISSION-030) ✓ · 73 TS tests, lint 0 errors, build, prettier, codegen check ✓ |
| MISSION-036 Command palette skeleton (Ctrl/Cmd+K) + shortcut registry | **DONE** (2026-08-11) — `shortcuts/keys.ts`: framework-free combo primitives — platform detection, `parseKeyCombo` ("Mod+K" = Cmd on macOS / Ctrl elsewhere), strict `matchesKeyCombo` against KeyboardEvent modifiers, `formatKeyCombo` hints (⌘K / Ctrl+K, Esc labels) ✓ · `shortcuts/useShortcuts.ts`: global window keydown registry with `enabled` gates and editable-target guard (inputs/textareas/contenteditable swallow bare-letter combos unless a modifier is held) ✓ · `features/command-palette/commands.tsx`: typed `PaletteCommand` registry + `buildPaletteCommands` (nav to every section, theme light/dark/system) + substring `filterPaletteCommands` (label + keywords) ✓ · `CommandPalette.tsx`: Radix Dialog overlay, Combobox input with ↑/↓ wrap nav + Enter + Escape, mouse-hover sync, grouped options (Navigation/Actions) with scroll-to-active, `Mod+K` toggle, Ctrl+K kbd hint, i18n `palette.*` keys en/ar (parity kept) ✓ · theme actions write through `usePreferences().setTheme` (single persistence path); `DialogContent` gained a `noPadding` option for the flush layout ✓ · mounted in AppShell; app test harness now includes `PreferencesProvider` ✓ · tests: combo parse/match/format matrix, palette opens/filters/navigates/runs-theme/navigates-to-settings; 66 TS tests, lint 0 errors, build, prettier, codegen check ✓ |
| MISSION-035 TanStack Query client + typed command wrappers (`api.ts`) + query keys | **DONE** (2026-08-11) — `@tanstack/react-query` v5 installed; `api/queryClient.ts`: `createQueryClient()` factory with local-first defaults (`retry:false`, `staleTime:60s`, `gcTime:5m`, `refetchOnWindowFocus:false`) + app singleton ✓ · `api/queryKeys.ts`: typed readonly-tuple key factory with scope fan-out (`all`/`lists`/`list(filters)`/`detail(id)` dashboards) for media, tracking, review, collection, stats, search, settings, task + system (greet) ✓ · `api/api.ts`: typed command wrapper object (`api`) + React Query hooks (`useGreetQuery` caching under `system.greeting(name)`, `useGreetMutation` writing its result into the cache) — features never touch `invoke` or raw keys ✓ · `api/index.ts` re-exports the whole boundary; `QueryClientProvider` mounted in main.tsx (contexts compose: Query → Theme → Preferences → Toasts) ✓ · tests: client defaults, key factory shapes/fan-out, wrapper hooks resolve+cache and mutation seeds cache via mocked `invoke`; 55 TS tests, lint 0 errors, build, prettier, codegen check ✓ |
| MISSION-034 Settings store: preferences model + persistence | **DONE** (2026-08-11) — `tauri-plugin-store` 2.4 wired: Rust dep, `tauri_plugin_store::Builder` registered, `store:default` capability added ✓ · `preferences/types.ts` (`Preferences { theme, language }` + defaults) ✓ · `preferences/repository.ts`: single `PreferencesRepository` with runtime backend selection — Tauri plugin `Store` (`settings.json`) in the desktop shell, localStorage (`mylore.preferences`) elsewhere; tolerant `parsePreferences` (corrupt/partial storage → defaults); cached repo ✓ · `PreferencesProvider`: renders with boot values (no flash), reconciles with the persisted store on load, and every change persists + mirrors the boot-cache keys (`mylore.theme`/`mylore.lang`) so `initTheme`/`initI18n` stay in sync; `usePreferences()` hook (context split into `PreferencesContext.ts`/`usePreferences.ts` per `ThemeContext`/`useTheme` convention) ✓ · real Settings page replaces the placeholder — theme (light/dark/system) + language (EN/ع) segmented controls (`role="group"` aria-labelled), i18n keys added (en/ar parity kept) ✓ · provider mounted in main.tsx ✓ · TS tests: repo round-trip/null/corrupt, settings persistence (theme→`data-theme` + store, language→rtl/lang + store), apply-on-mount; 50 TS + 165 Rust tests, lint 0 errors, build, prettier, cargo check/test ✓ |
| MISSION-033 i18n: i18next en/ar, ICU, RTL wiring (`dir`, logical props), locale switcher | **DONE** (2026-08-11) — i18next + react-i18next (bundler, `useSuspense: false`) ✓ · `i18n/locales.ts` en/ar resource trees (nav, shell/status, theme + full page hints) with ICU plural categories via Intl (`counts_one/other` en · `zero/one/two/few/many/other` ar); parity is enforced by a locale test (exact match for non-plural keys, Arabic superset for plural forms, non-empty values) ✓ · `i18n/index.ts`: bootstrap with `LOCALE_STORAGE_KEY` persistence, browser-language fallback, `applyLanguage` sets `lang` + `dir` (ar→rtl) on `<html>`, `setLanguage` persists + re-renders, `initI18n()` pre-paint in main.tsx (no FOUC), `useLanguage` hook ✓ · RTL made logical: NavRail `border-e`, Toast viewport `end-0`, Dialog close `end-4` (flex layout already mirrors) ✓ · `LanguageSwitcher` (EN/ع segmented control) in the TopBar next to the theme switcher; whole shell translated (TopBar title, NavRail labels, StatusBar counts + version, empty-state titles/hints) ✓ · 43 tests, lint 0 problems, tsc+vite build, prettier, codegen check ✓ |
| MISSION-032 Router + app shell: nav rail, topbar, status bar; empty-state pages | **DONE** (2026-08-11) — React Router (hash router for the Tauri webview scheme; `createHashRouter`, no server rewrites on reload) ✓ · `navigation.ts` is the single source of path/label/icon for rail + top bar + routes (8 sections: Library, Search, Discover, Collections, Reviews, Stats, Calendar, Settings) ✓ · `components/shell/`: **AppShell** (nav rail + top bar + status bar around `<Outlet>`, h-screen flex, mirrors in RTL), **NavRail** (icon+text NavLinks, active = accent-soft, `end` for the index section), **TopBar** (page title from route + app-wide theme switcher; `THEME_CHOICES` moved to `themes/preferences.ts`), **StatusBar** (v0.1.0 / "0 titles" placeholder) ✓ · `EmptyState` primitive (icon + title + hint + action) consumed by 8 placeholder feature pages (`features/index.tsx`) ✓ · `routes.tsx` route table shared with tests via `createMemoryRouter`; root redirects to Library ✓ · primitives showcase App removed; `main.tsx` renders ThemeProvider → ToastProvider → RouterProvider ✓ · 32 tests (shell: redirect, nav navigation, active highlight, status bar, theme switch), lint 0 problems, tsc+vite build, prettier, codegen check ✓ |
| MISSION-031 Tailwind + design-system primitives (Button, Input, Dialog, Popover, Toast, Skeleton…) on Radix | **DONE** (2026-08-11) — Tailwind v4 via `@tailwindcss/vite` + `@theme` mapping the MISSION-030 tokens onto utilities (`bg-bg-surface`, `text-text-primary`, `rounded-md`, `shadow-sm`, status palette; aliased `--elevation-*` so Tailwind's shadow keys dereference without self-collision) ✓ · primitives in `src/components/ui/` (barrel `index.ts`, `cn()` helper): **Button** (primary/secondary/ghost/danger, sm/md, `asChild` via `@radix-ui/react-slot`, default `type=button`) ✓ · **InputField + TextareaField** (Radix Label, required label, inline error slot with `role=alert` + `aria-invalid`) ✓ · **Badge** (status palette + neutral) ✓ · **Dialog/Popover** (Radix; focus trap, Esc, dir-aware, overlay + raised surface + elevation) ✓ · **Toast** (Provider+useToast: success/error/info, undo action, auto-dismiss, swipe) ✓ · **Skeleton** (pulse, reduced-motion-safe) ✓ · App switch to a primitives showcase (themes switcher retained); `lucide-react` icons added ✓ · test setup gains a `ResizeObserver` polyfill (jsdom/Radix) ✓ · 32 tests (20→32), lint 0 problems (eslint override scoping fast-refresh off for ui modules), tsc+vite build, prettier, codegen check ✓ |
| MISSION-030 Design tokens (colors/type/spacing/radius/elevation), light+dark themes, `data-theme` | **DONE** (2026-08-11) — `src/design-tokens/tokens.css` as the single source of truth (DESIGN_SYSTEM.md): full palette both themes on `:root`/`:root[data-theme=dark]` (bg-base/-surface/-raised/-hover, border-subtle/-strong, text-primary/-secondary/-tertiary, accent/-hover/-soft, ok, warn, danger, info) + `color-scheme` ✓ · tokenized status→color mapping (`status-planned/-inprogress/-completed/-onhold/-dropped/-repeat`, AA pairs) ✓ · type scale (12–36rem) + tabular-nums, spacing 4px unit (4–48), radius 6/10/16/999, elevation shadow-sm/lg, focus ring 2px accent w/ 3px gap, motion 120–200ms ease-out, control heights ✓ · `src/design-tokens/index.ts` mirrors values for JS/TS (charts, inline styles) ✓ · `themes/theme.ts` framework-free controller: `ThemePreference light|dark|system` (system default), `matchSystemTheme()` (prefers-color-scheme), `applyTheme`/`createThemeSystem` setting `data-theme` on `<html>`, `localStorage` persistence (`mylore.theme`), graceful fallbacks when storage/matchMedia unavailable ✓ · `ThemeProvider` + `ThemeContext` + `useTheme`; system-preference live-follow (matchMedia change listener, derived-state render so no state-in-effect churn) ✓ · `initTheme()` runs in `main.tsx` pre-render (no FOUC) ✓ · App scaffold converted to token-driven classes + working light/dark/system switcher ✓ · global.css: base styles, AA focus ring, reduced-motion, selection ✓ · 12 (+3 updated App/theme) tests, lint 0 problems, tsc+vite build, prettier, codegen check ✓ |
| MISSION-025 Title normalization (case/unicode/diacritic fold, script-aware) + title matching | **DONE** (2026-08-11) — `domain/normalize` (pure, no I/O) ✓ · `fold_title`: NFC compose → full Unicode lowercase → width normalize (fullwidth ASCII → halfwidth, fullwidth space, halfwidth katakana → fullwidth via table) → NFD + drop combining marks (**except kana voicing U+3099/U+309A**: パン stays distinct from ハン) → Arabic consonant variants (أ/إ/آ/ٱ → ا, ى → ي, ة → ه) + × → x → script-aware filter (Han/kana/hangul: drop spaces+punctuation; spaced scripts: collapse whitespace + separators -,_,/,·,–,— to one space, drop other punctuation) ✓ · `title_matches` (fold equality), `title_contains` (fold substring) for the identity stage ✓ · `unicode-normalization` 0.1.25 added as a direct dependency (already vendored transitively) ✓ · 12 new unit tests (129/129 total) ✓ · clippy -D warnings ✓ · ROADMAP ✓ |
| MISSION-026 IdentityService: exact (provider, ext_id) + fuzzy scoring + candidate ranking | **DONE** (2026-08-11) — `domain/identity` (pure, no I/O) ✓ · `IdentityCandidate { media_id, titles, external_ids }` + `IdentityKind` (Exact / TitleExact / Fuzzy / None) ✓ · `exact_external_id`: same (provider, value) on file → definitive (score 1.0) ✓ · `titles_exact` (fold-equal any title incl. original/alternatives → 0.95, TITLE_EXACT_SCORE) ✓ · `title_similarity`: token Jaccard + containment + bigram Jaccard on MISSION-025 folds, bigram only refines pairs with token/containment overlap so stray bigrams ("on" in One Piece/Attack on Titan) give no false positives ✓ · `best_title_similarity` across all title pairs ✓ · `score_candidate` + `rank_candidates` (score desc, media_id tie-break for determinism) + `best_match` (skips kind None) ✓ · 9 new unit tests (138/138 total) ✓ · clippy -D warnings ✓ · ROADMAP ✓ |
| MISSION-027 StatsService: pure computations (counts, hours, completion, avg rating, distributions) | **DONE** (2026-08-11) — `domain/stats` (pure, no I/O) ✓ · `MediaStatsRow { media_id, content_type, core_status, rating, favorite, release_year, progress }` projection + `compute_stats(&[MediaStatsRow]) -> StatsSummary` ✓ · counts per status and per content type (schema ALL order) ✓ · rating distribution 1..=10 + `avg_rating` (mean of non-null) ✓ · completion: `completed_media`, `completion_rate` (completed/total, None when empty), `avg_percent` (mean of aggregate percents) ✓ · time from real data only: `consumed_minutes` (sum of node-level completed minutes) + `consumed_hours()`; reading reported as `consumed_pages` (Pages-weight aggregates) — no invented pages→time conversion (product decision) ✓ · `year_counts` distribution (ascending) ✓ · `favorites` ✓ · 7 new unit tests (145/145 total) ✓ · clippy -D warnings ✓ · ROADMAP ✓ |
| MISSION-028 MergeService: merge plan, conflict report, re-parenting, before-image | **DONE** (2026-08-11) — `domain/merge` (pure, no I/O) ✓ · `plan_merge(survivor, duplicate, duplicate_nodes, duplicate_collection_ids, survivor_has_review, survivor_has_tracking) -> MergePlan` ✓ · merged metadata: survivor identity kept, scalars prefer survivor and fall back to duplicate, sets (titles/genres/tags/people/relations/external_ids) unioned + deduped; duplicate main title becomes an alternative when it differs; duplicate original title is a fallback ✓ · `conflicts: Vec<FieldConflict>` only for *different non-empty* values (scalars, content_type, title via fold, external_id provider collisions) ✓ · re-parenting: duplicate nodes re-keyed to survivor id, parent links and node ids preserved ✓ · moves: review/tracking only when survivor lacks one; collection memberships always re-keyed ✓ · `BeforeImage { survivor, duplicate }` for undo ✓ · self-relations to the survivor dropped, external ids stay provider-unique, merged record re-validated ✓ · 13 new unit tests (158/158 total) ✓ · clippy -D warnings ✓ · ROADMAP ✓ |
| MISSION-029 Service unit tests: progress math, status, dedup, stats, merge | **DONE** (2026-08-11) — `tests/domain_services.rs` integration crate over `mylore_lib::domain::*` (pure, in-memory) ✓ · tracking lifecycle: aggregate → `suggest_auto_status` (in_progress → completed → repeat) with `apply_transition` stamping started_at/finished_at, repeat_count increment + finished_at clear, reversible regression ✓ · stats combining watch time/reading/ratings across content types (`consumed_minutes` from durations-only, pages by book weight, avg percent = mean of integer percents) ✓ · dedup → merge → re-parenting: exact external-id match on the family, duplicate collection memberships moved, genre/title sets unioned, duplicate nodes re-keyed to survivor, before-image present ✓ · title-variant dedup via fold (`タイトル`, case, ×/x) → TitleExact, merged main survives, no alternative duplication ✓ · Arabic variant dedup (anime title vs Arabic alt) + external-id union ✓ · merged media's node set aggregates to the same progress as the original ✓ · custom-status bucket sets status count, Repeat-guard rejects entering from Planned ✓ · 7 new integration tests (158 + 7 = 165/165 total) ✓ · fmt ✓ · clippy -D warnings ✓ · ROADMAP ✓ |
| MISSION-024 Status engine: core statuses, custom statuses, auto-transition rules (reversible) | **DONE** (2026-08-11) — `domain/status` (pure, no I/O) ✓ · `CoreStatus` classification: `is_terminal` (completed/dropped), `is_active` (in_progress/repeat), `is_not_started` (planned/wishlist) ✓ · explicit transition matrix (`can_transition`): permissive like MAL/AniList with one rule — **Repeat requires prior consumption** (in_progress/completed/dropped/repeat only) ✓ · `apply_transition(tracking, to, today)` (clock-free) stamps started_at (active) / finished_at (terminal, only when absent), clears finished_at when leaving terminal (reversible), increments repeat_count on entering Repeat and resets on exit; result re-validated so it can never be invalid ✓ · `CustomStatus { id, name, bucket, sort_order }` + `effective_status` (custom bucket overrides core status) ✓ · `suggest_auto_status(&ProgressAggregate)` auto-transition rule: all nodes consumed → completed; some → in_progress; none → planned; **None when no node data**; explicit + reversible (un-marking moves completed → in_progress → planned) ✓ · 11 new unit tests (117/117 total) ✓ · clippy -D warnings ✓ · ROADMAP ✓ |
| MISSION-023 Progress engine: per-contentType templates, aggregates (pages/chapters/episodes); unit tests | **DONE** (2026-08-11) — `domain/progress` (pure, no I/O) ✓ · `ProgressTemplate::for_content_type` covers all 10 content types: anime/tv/movie → Episode/Count/Watched; manga/manhwa/manhua/novel/web_novel → Chapter/Count/Read; book → Chapter/**Pages**/Read; other → Node/Count/Read ✓ · `unit_label()` for UI ("episodes"/"chapters"/"pages") ✓ · `aggregate()` folds `NodeTick`s: only template-unit nodes count, weight = 1 or `page_count.unwrap_or(1)`, only consuming state counts, `percent` = `saturating_mul(100)/total` (None when tree empty), minutes summed only when a node carries duration ✓ · `estimated_total_units(content_type, &MediaRuntime)` → pages/ep_count/ch_count (book uses pages, not chapter count) ✓ · `with_estimate` fills totals only when the node tree contributed 0 ✓ · 10 new unit tests (106/106 total) ✓ · clippy -D warnings ✓ · ROADMAP ✓ |
| MISSION-022 Domain types: `Media`, `ContentNode`, `Tracking`, `Review`, value objects; invariant guards | **DONE** (2026-08-11) — `domain/{enums,value_objects,media,content_node,tracking,review,error}` (pure, no I/O) ✓ · `string_enum!` macro mirrors every SQL CHECK (ContentType, MediaStatus, NodeKind, NodeProgressState, CoreStatus, PersonRole, MediaRelationKind) with `as_str`/`FromStr` — enum-valid ⇒ CHECK-valid ✓ · value objects: `MediaId`, `Rating(1..10)`, `DateOnly(YYYY-MM-DD)`, `LanguageCode`, `ProviderId`, `Title` (≥1 title, dedup incl. original), `ExternalId` ✓ · invariant guards: media (unique provider ext-ids, no self-relation, start≤end, timestamps), ContentNode (1-based position), NodeProgress (read/watched ⇒ read_at; unread/skipped ⇒ no read_at), Tracking (repeat_count only on Repeat, finished ⇒ terminal status, finish≥start), Review (timestamp order, spoiler requires text) ✓ · `DomainError` + `From<DomainError> for AppError` ✓ · 26 new unit tests (96/96 total) ✓ · clippy -D warnings ✓ |
| MISSION-021 Benchmarks: insert 1k/10k, search 10k/50k/100k rows, bulk import timing | **DONE** (2026-08-11) — `src-tauri/benches/database.rs` (criterion, release) ✓ · fresh in-memory migrated DB per insert sample (results independent of prior samples) ✓ · insert repo path (`media::create` per-row tx) 1k ≈ 349 ms (~0.35 ms/row), 10k ≈ 3.66 s ✓ · insert bulk raw one-tx 1k ≈ 184 ms, 10k ≈ 1.90 s (~2× faster) ✓ · search FTS5 **flat at ~110 µs** across 10k/50k/100k rows (selective ~1% hit + no-match) — index-bound, not data-bound ✓ · bulk import: raw one-tx 10k ≈ 1.90 s vs repo loop ≈ 3.76 s → import pipeline (MISSION-067) must batch in one tx ✓ · DATABASE.md §5.2 ✓ · clippy -D warnings ✓ · 70/70 tests ✓ |
| MISSION-020 DB integration tests: CRUD, FKs, cascade, FTS query, transaction rollback | **DONE** (2026-08-11) — `infrastructure::integration_tests` (cfg(test)) over the fully-migrated schema ✓ · full lifecycle: create media + nodes/tracking/progress/review/collection/activity → delete media → every aggregate cascades, FTS empties, unrelated media + collection + person survive ✓ · manual tx: failed statement → whole tx rolls back (incl. FTS), commit persists ✓ · repo-internal atomicity: failing link rolls back `media::create` AND `media::update` (previous aggregate untouched) ✓ · FTS follows review body edits (index add + clear + delete) ✓ · FK rejection at repo boundary for every aggregate ✓ · 70/70 tests ✓ · clippy -D warnings ✓ · ROADMAP ✓ |
| MISSION-019 Repositories: media, node, tracking, review, collection, asset, activity (sqlx typed) | **DONE** (2026-08-11) — `infrastructure::repositories/{media,node,tracking,review,collection,asset,activity}` ✓ · media: full-aggregate create/get/update (row + links in one tx), delete, `find_by_external_id`, `list` (content_type/pub_status/genre/tag/favorite filters, sort, pagination) + `count`, FTS `search` (unicode61 + trigram, `fts::normalize_query` Arabic fold) ✓ · node: create/reparent/update/delete/children with `content_node` validators (cross-media + cycle rejection) ✓ · tracking: row upsert + `node_progress` upsert/read + `count_nodes_in_state` ✓ · review/collection/asset/activity CRUD ✓ · repos are clock-free, never write FTS tables ✓ · 64/64 tests ✓ · clippy -D warnings ✓ · DATABASE.md §5.1 ✓ |
| MISSION-018 FTS5 `media_fts` + triggers + rebuild; multilingual tokenization (unicode61 + trigram for CJK) | **DONE** (2026-08-11) — `migrations/0007_media_fts.sql` ✓ · `v_media_fts_source` view with Arabic normalization (Alef variants→ا, ى→ي, ة→ه, diacritics/tanween stripped) ✓ · `media_fts` (unicode61, 9 columns) + `media_fts_cjk` (trigram) — **stored content**, not contentless (FTS5 contentless `'delete'` either needs deleted values or silently orphans terms) ✓ · 21 refresh triggers (ins/del/upd on media + 6 feeder tables) + backfill ✓ · `PRAGMA recursive_triggers = ON` in `db::connect` + `test_support::in_memory_pool` so cascades re-fire FTS triggers ✓ · `infrastructure::fts::rebuild` (transactional wipe + repopulate) ✓ · 45/45 tests (schema/triggers, both indexes, CJK trigram substring, Arabic fold, alt-title refresh, cascade refresh, update refresh, rebuild) ✓ · clippy -D warnings ✓ |
| MISSION-017 `review`, `collection`, `collection_member`, `asset`, `activity`, `trash`, `settings` | **DONE** (2026-08-11) — `migrations/0006_user_aggregates.sql` ✓ · media cover/banner asset columns added via ALTER TABLE ADD COLUMN (deferred FK from 0002) ✓ · CHECKs: rating 1..10, favorites/spoiler/smart/restored booleans, asset kind/status, activity kind, trash kind ✓ · `idx_activity_media` ✓ · `provider_setting` deferred to MISSION-063 ✓ · 36/36 tests (asset SET NULL, review/collection/activity cascades, checks, settings roundtrip) ✓ · clippy -D warnings ✓ |
| MISSION-016 `tracking`, `status` (seeded core), `node_progress` + CHECKs | **DONE** (2026-08-11) — `migrations/0005_tracking.sql` ✓ · 7 core statuses seeded (is_system=1) ✓ · CHECKs: core_status, bucket, repeat_count>=0, node state, rating 1..10 ✓ · `idx_tracking_core_status`/`idx_tracking_updated_at` ✓ · 30/30 tests (seeds, checks, FK cascade, custom-status SET NULL) ✓ · clippy -D warnings ✓ |
| MISSION-015 `media_external_id`, `media_relation` + unique constraints | **DONE** (2026-08-11) — `migrations/0004_media_identity.sql` ✓ · PK(provider, ext_id) + UNIQUE(media_id, provider) exact-identity rules ✓ · relation CHECK + CHECK(from_id <> to_id) ✓ · 25/25 tests (uniques, cascades, self/missing/bad-relation rejection) ✓ · clippy -D warnings ✓ |
| MISSION-014 `content_node` tree + indexes; FK validation helpers | **DONE** (2026-08-11) — `migrations/0003_content_node.sql` (kind CHECK, `idx_node_media`/`idx_node_parent`) ✓ · `infrastructure::content_node` validators: parent-belongs-to-media + acyclic (SQLite can't express cross-row tree invariants) ✓ · `AppError::Validation` ✓ · shared test helpers moved to `test_support` ✓ · 21/21 tests (schema/indexes, media-cascade, kind CHECK, cross-media + cycle rejection) ✓ · clippy -D warnings ✓ |
| MISSION-013 `media`, `media_alt_title`, `person`/`media_person`, `genre`, `tag` (+ joins) + seeds | **DONE** (2026-08-11) — `migrations/0002_media.sql` ✓ · enum CHECKs (content_type, pub_status, role, scope) ✓ · seeds: 18 genres + 21 domain tags ✓ · asset FK columns deferred to MISSION-017 (SQLite rejects writes through an FK to a missing parent) ✓ · 16/16 tests (schema+seeds, media/person cascade, FK enforced) ✓ · clippy -D warnings ✓ |
| MISSION-012 Migration runner (`sqlx::migrate!`), versioned `migrations/`, transaction-wrapped | **DONE** (2026-08-11) — `db::migrate` wired into `init` ✓ · `migrations/0001_init.sql` (`app_meta`) ✓ · per-migration transaction verified in sqlx-sqlite 0.8.6 ✓ · `AppError::Migration` ✓ · idempotency + corrupt-db tests (Windows WAL/SHM lock fix) ✓ · clippy -D warnings ✓ |
| MISSION-011 sqlx pool + PRAGMAs + startup integrity check | **DONE** (2026-08-11) — `db::init` (FK/WAL/busy_timeout=5s/synchronous=Normal, max 5 conns) ✓ · `PRAGMA integrity_check` at startup ✓ · pool as managed state (`app.manage`) ✓ · `AppError::Database` ✓ · tests ✓ · clippy -D warnings ✓ |
| MISSION-010 Window shell: titlebar, min sizes, app id, CSP | **REVIEW** — productName/identifier `MyLore`/`com.mylore.app` ✓ · window 1100x760 min 900x600 centered ✓ · strict prod CSP (no unsafe-inline) + dev CSP ✓ · config guard test ✓ · cargo build validates ✓ · 7/7 tests ✓ |
| MISSION-009 Typed IPC boundary + codegen | **REVIEW** — contract in `scripts/ipc-contract.json` ✓ · `npm run codegen` → `src/api/ipc.generated.ts` ✓ · drift-guard (Rust #[command] scan + --check) ✓ · App.tsx uses typed `greet` ✓ · 4/4 tests ✓ · lint/build ✓ |
| MISSION-008 Logging (tracing → rolling file, no secrets) + `AppError` | **REVIEW** — clippy -D warnings ✓ · 6/6 tests ✓ · cargo build ✓ · rolling daily logs + stdout ✓ · AppError→frontend as string ✓ |
| MISSION-007 GitHub Actions CI (win/ubuntu/macos) | **REVIEW** — actionlint ✓ · prettier ✓ · all steps pass locally (fmt/clippy/test/build/lint) |
| MISSION-006 Rust unit-test harness + in-memory sqlite helper | **DONE** (2026-08-11) — `cargo test` 2/2 ✓ · clippy -D warnings ✓ · fmt ✓ |
| MISSION-005 Vitest + Testing Library harness + sample test | **DONE** (2026-08-11) — 2 tests pass · lint ✓ · format ✓ · build ✓ |
| MISSION-004 Rust rustfmt + Clippy + crate layout | **DONE** (2026-08-11) — fmt ✓ · clippy -D warnings ✓ · layered crate layout per PROJECT_MAP |
| MISSION-003 ESLint + Prettier + pre-commit hook | **DONE** (2026-08-11) — eslint ✓ · prettier ✓ · husky+lint-staged ✓ · .gitattributes ✓ |
| MISSION-002 TS strict + path aliases + editor config | **DONE** (2026-08-11) — strict flags ✓ · `@/` alias (tsc+vite) ✓ · .editorconfig ✓ |
| MISSION-001 Scaffold Tauri 2 + React + TS | **DONE** (2026-08-11) — npm build ✓ · cargo build ✓ · app launched & user-tested ✓ |

### M1 · Foundation (MISSION-001 … 010)

| Mission | Description | Deps | Pri | Cplx |
|---------|-------------|------|-----|------|
| MISSION-001 | Scaffold Tauri 2 + React + TS (Vite); baseline config (`tauri.conf.json`, capabilities). | — | Core | M |
| MISSION-002 | TypeScript strict config, path aliases, editor config. | 001 | Core | S |
| MISSION-003 | ESLint + Prettier (+ pre-commit hook via lint-staged). | 002 | Core | S |
| MISSION-004 | Rust: rustfmt + Clippy (deny warnings in CI), crate layout. | 001 | Core | S |
| MISSION-005 | Vitest + @testing-library/react harness; sample test. | 002 | Core | S |
| MISSION-006 | Rust unit-test harness (`cargo test`) with in-memory sqlite helper. | 004 | Core | S |
| MISSION-007 | GitHub Actions: build + lint + test (windows/ubuntu/macos-latest). | 003..006 | Core | M |
| MISSION-008 | Logging (tracing → rolling file, no secrets) + `AppError` skeleton. | 001 | Core | M |
| MISSION-009 | Typed IPC boundary: shared TS types for commands/events + codegen. | 002,008 | Core | M |
| MISSION-010 | Window shell: titlebar, min sizes, app identifier, CSP in config. | 001 | Core | S |

### M2 · Database (MISSION-011 … 021)

| Mission | Description | Deps | Pri | Cplx |
|---------|-------------|------|-----|------|
| MISSION-011 | sqlx pool + PRAGMAs (FK, WAL, busy_timeout) as managed state; startup integrity check. | 008 | Core | M |
| MISSION-012 | Migration runner (`sqlx::migrate!`), versioned `migrations/`, transaction-wrapped. | 011 | Core | S |
| MISSION-013 | `media`, `media_alt_title`, `person`/`media_person`, `genre`, `tag` tables + seeds. | 012 | Core | M |
| MISSION-014 | `content_node` tree + indexes; FK validation helpers. | 013 | Core | M |
| MISSION-015 | `media_external_id`, `media_relation` + unique constraints. | 014 | Core | M |
| MISSION-016 | `tracking`, `status` (seeded core), `node_progress` + CHECKs. | 013,014 | Core | M |
| MISSION-017 | `review`, `collection`, `collection_member`, `asset`, `activity`, `trash`, `settings`. | 013..016 | Core | M |
| MISSION-018 | FTS5 `media_fts` + triggers + rebuild; multilingual tokenization (unicode61 + trigram for CJK). | 013,015,016 | Core | L |
| MISSION-019 | Repositories: media, node, tracking, review, collection, asset, activity (sqlx typed). | 011..018 | Core | L |
| MISSION-020 | DB integration tests (CRUD, FKs, cascade, FTS query, transaction rollback). | 019 | Core | M |
| MISSION-021 | Benchmarks: insert 1k/10k, search 10k/50k/100k rows, bulk import timing. | 020 | Important | M |

### M3 · Domain Layer (MISSION-022 … 029)

| Mission | Description | Deps | Pri | Cplx |
|---------|-------------|------|-----|------|
| MISSION-022 | Domain types: `Media`, `ContentNode`, `Tracking`, `Review`, value objects; invariant guards. | 012 | Core | M |
| MISSION-023 | Progress engine: per-contentType templates, aggregates (pages/chapters/episodes); unit tests. | 022 | Core | M |
| MISSION-024 | Status engine: core statuses, custom statuses, auto-transition rules (reversible). | 023 | Core | M |
| MISSION-025 | Title normalization (case/unicode/diacritic fold, script-aware) + title matching. | 023 | Core | M |
| MISSION-026 | IdentityService: exact (provider, ext_id) + fuzzy scoring + candidate ranking. | 025 | Core | M |
| MISSION-027 | StatsService: pure computations (counts, hours, completion, avg rating, distributions). | 022 | Core | M |
| MISSION-028 | MergeService: merge plan, conflict report, re-parenting, before-image. | 022,026 | Important | L |
| MISSION-029 | Service unit tests: progress math, status, dedup, stats, merge. | 023..028 | Core | M |

### M4 · UI Foundation (MISSION-030 … 037)

| Mission | Description | Deps | Pri | Cplx |
|---------|-------------|------|-----|------|
| MISSION-030 Design tokens (colors/type/spacing/radius/elevation), light+dark themes, `data-theme` | **DONE** — see log | 010 | Core | M |
| MISSION-031 Tailwind + design-system primitives (Button, Input, Dialog, Popover, Toast, Skeleton…) on Radix | **DONE** — see log | 030 | Core | L |
| MISSION-032 Router + app shell: nav rail, topbar, status bar; empty-state pages | **DONE** — see log | 031 | Core | M |
| MISSION-033 i18n: i18next en/ar, ICU, RTL wiring (`dir`, logical props), locale switcher | **DONE** — see log | 032 | Core | M |
| MISSION-034 | Settings store (tauri-plugin-store) + preferences model; theme/lang persistence. | 033 | Core | S | **DONE** — see log |
| MISSION-035 | TanStack Query client + typed command wrappers (`api.ts`) + query keys. | 009,032 | Core | M | **DONE** — see log |
| MISSION-036 | Command palette skeleton (Ctrl/Cmd+K) + shortcut registry. | 032 | Important | M | **DONE** — see log |
| MISSION-037 | A11y baseline: focus ring, reduced-motion, semantic shell, screen-reader pass. | 031 | Important | M | **DONE** — see log |

### M5 · Library MVP (MISSION-038 … 045)

| Mission | Description | Deps | Pri | Cplx |
|---------|-------------|------|-----|------|
| MISSION-038 | Manual Add dialog (fast entry, validation with Zod) → MediaService command. | 019,022,032 | Core | M |
| MISSION-039 | Library query endpoint (filter/sort/group/paginate) + API. | 019 | Core | M |
| MISSION-040 | Library views: Grid / List / Compact list (virtualized, TanStack Virtual). | 035,039 | Core | L |
| MISSION-041 | Filter panel (type, format, status, genre, tag, year, favorite) + sort menu + group-by. | 039,040 | Core | L | **DONE** — see log |
| MISSION-042 | Media detail page: hero, meta tabs, actions (overview/detail/tracking/review shell). | 035,039 | Core | L | **DONE** — see log |
| MISSION-043 | Local search (FTS) in header + results page (local-first). | 018,039 | Core | M | **DONE** — see log |
| MISSION-044 | Trash/restore UI + undo toast for deletes. | 017,038 | Important | M | **DONE** — see log |
| MISSION-045 | Bulk-select mode + action bar (status, tag, list, delete, export later). | 040,044 | Important | M | **DONE** — see log |

### M6 · Tracking (MISSION-046 … 052)

| Mission | Description | Deps | Pri | Cplx |
|---------|-------------|------|-----|------|
| MISSION-046 | Node tree endpoint + UI (seasons→episodes, volumes→chapters; expand/collapse). | 014,019,042 | Core | L | **DONE** — see log |
| MISSION-047 | Per-node progress commands (mark read/watched/skipped, range-mark) + optimistic UI. | 023,046 | Core | M | **DONE** — see log |
| MISSION-048 | Status transitions + auto-complete rule; repeat counter. | 024,047 | Core | M | **DONE** — see log |
| MISSION-049 | Quick capture popover (global hotkey) + in-grid quick controls. | 047,036 | Core | M | **DONE** — see log |
| MISSION-050 | Dashboard widgets: Continue Reading/Watching, Recently Added/Completed, Quick Actions. | 047,048,039 | Core | M | **DONE** — see log |
| MISSION-051 | Activity log writes on all tracking actions. | 017,047 | Core | S | **DONE** — see log |
| MISSION-052 | **Novel/web-novel tracking UX** (research §2b): chapter-list read-state rows + "my status" markers; **Normal (autoTrack) vs Manual mode** per media; **DNF-with-progress** (dropped carries % / chapter). | 047,048 | Important | M | **DONE** — see log |

### M7 · Providers (MISSION-053 … 066)

| Mission | Description | Deps | Pri | Cplx |
|---------|-------------|------|-----|------|
| MISSION-053 | Provider trait + capabilities + Coordinator (rate limit, retry/backoff, timeout, cancel, error map). | 022 | Core | L | **DONE** — see log |
| MISSION-054 | AniList adapter (search/details/nodes/relations/external ids; LNs via MANGA/NOVEL) + fixtures. | 053 | Core | L | **DONE** — see log |
| MISSION-055 | TMDB adapter (movies+TV, seasons/episodes, images, attribution) + fixtures. | 053 | Core | L | **DONE** — see log |
| MISSION-056 | MangaDex adapter (manga/manhwa/manhua, chapters, covers) + fixtures. | 053 | Core | L | **DONE** — see log |
| MISSION-057 | OpenLibrary adapter (books, ISBN, covers) + fixtures. | 053 | Core | M | **DONE** — see log |
| MISSION-058 | Fallbacks: Jikan (anime), Google Books (books). | 054,057 | Optional | M | **DONE** — see log |
| MISSION-059 | External search UI (grouped by provider, "in library"/duplicate flags). | 053..057,026 | Core | L | **DONE** — see log |
| MISSION-060 | Import-from-provider flow (search → details → identity check → add). | 059,026 | Core | M | **DONE** — see log |
| MISSION-061 | Enrich metadata (refresh provider fields only; never touch user data) + diff report. | 054..057,042 | Core | M | **DONE** — see log |
| MISSION-062 | Image pipeline: download/cache covers, broken-url handling, cache policy. | 017,053 | Core | M | **DONE** — see log |
| MISSION-063 | Provider settings UI: enable/disable, API keys (keyring), test connection. | 053 | Core | M | **DONE** — see log |
| MISSION-064 | Hardcover adapter (books/LN, free GraphQL) — optional third book provider. | 057 | Optional | M | **DONE** — see log |
| MISSION-065 | **NovelUpdates adapter** (web novels/light novels: search/details/chapters/external id via HTML scraping, lnreader-plugins selectors) + fixtures. + Note: look in the folder C:\Users\malek\project\webtest to be more sure about the website  | 053 | Core | M | **DONE** — see log |
| MISSION-066 | Bangumi adapter (CN ACGN incl. LN/WN, open API, ~1 rps) — optional. | 053 | Optional | M | **DONE** — see log |

### M8 · Import / Export (MISSION-067 … 073)

| Mission | Description | Deps | Pri | Cplx |
|---------|-------------|------|-----|------|
| MISSION-067 | Import pipeline core: parser→validator→normalizer→dedup→preview→txn→report. | 026,019 | Core | L | **DONE** — see log |
| MISSION-068 | JSON import (app format) + CSV import (mapping UI). | 067 | Core | M | **DONE** — see log |
| MISSION-069 | Bulk-import preview UI (per-item outcome) + confirm/cancel. | 067,040 | Core | M | **DONE** — see log |
| MISSION-070 | Background TaskManager (progress, cancel, typed results) wired to import. | 067 | Core | M | **DONE** — see log |
| MISSION-071 | Export JSON / CSV / Markdown (streaming, dialog pick). | 019 | Core | M | **DONE** — see log |
| MISSION-072 | Provider imports: AniList user export, **Goodreads CSV**, **StoryGraph CSV** (MAL/Trakt user lists out of scope — Jikan has no user-list API, Trakt needs OAuth). | 067,054,058 | Optional | M | **DONE** — see log |
| MISSION-073 | Import/export integration tests + fixture data. | 068..071 | Core | M | **DONE** - see log |

> Note: novels/web novels/light novels have no clean *open* provider — NovelUpdates has no
> official API (ToS forbids automated scraping, but the LNReader plugin — maintained by an NU
> moderator — provides authoritative selectors we follow at a modest rate); AniList indexes LNs only.
> NovelUpdates (MISSION-065) + OpenLibrary/Google Books (+ optional Hardcover/Bangumi) cover
> metadata; adopt NovelUpdates' genre/tag taxonomy as conventions. Book imports come
> from Goodreads/StoryGraph CSV (user-owned data). `API_PROVIDERS.md` §12–17.

### M9 · Reviews & Collections (MISSION-074 … 079)

| Mission | Description | Deps | Pri | Cplx |
|---------|-------------|------|-----|------|
| MISSION-074 | Review/notes UI (rating, review, short review, spoiler, personal tags) + commands. | 017,042 | Core | M | **DONE** - see log |
| MISSION-075 | Favorites flag in grid/list + filter. | 039 | Core | S | **DONE** - see log |
| MISSION-076 | Collections CRUD + drag/drop membership + bulk add. | 017,040 | Core | M | **DONE** - see log |
| MISSION-077 | Smart collections: save filter as collection; query builder (basic). | 039,076 | Important | M | **DONE** - see log |
| MISSION-078 | Bulk operations on filtered selection (status/tag/list/delete) with summary. | 045 | Important | M | **DONE** — see log |
| MISSION-079 | **Mood / pace / content-warning badges** on detail page (StoryGraph); content warnings as acknowledged metadata. | 074,042 | Optional | M | **DONE** — see log |

### M10 · Stats & Calendar (MISSION-080 … 083)

| Mission | Description | Deps | Pri | Cplx |
|---------|-------------|------|-----|------|
| MISSION-080 | StatsService endpoints + Stats page (cards + small charts, tabular numbers). | 027,019 | Important | M | **DONE** — see log |
| MISSION-081 | Calendar: local air/release dates + activity; month grid + list. | 051,039 | Optional | M | **DONE** — see log |
| MISSION-082 | Year-in-review style recap. | 080 | Optional | M | **DONE** — see log |
| MISSION-083 | **Reading recap stats**: pages/chapters per month, mood/pace/format trends (StoryGraph) — local-data-only. | 080 | Optional | M | **DONE** — see log |

### M11 · Backup & Recovery (MISSION-084 … 089)

| Mission | Description | Deps | Pri | Cplx |
|---------|-------------|------|-----|------|
| MISSION-084 | BackupService: `VACUUM INTO` snapshot + assets + meta → `.mylore`; validate. | 011 | Core | M | **DONE** — see log |
| MISSION-085 | Restore: quarantine current, swap, verify, rollback-safe. | 084 | Core | M | **DONE** — see log |
| MISSION-086 | Automatic backup schedule + rotation (N + monthly) in preferences. | 084 | Core | M | **DONE** — see log |
| MISSION-087 | Pre-migration auto-backup hook. | 084,012 | Core | S | **DONE** — see log |
| MISSION-088 | Backups UI (list, restore, validate, delete) + recovery from corrupt DB (prompt restore). | 084,085 | Core | M | **DONE** — see log |
| MISSION-089 | Merge UI with conflict preview + restore-from-trash for merges. | 028 | Important | L | **DONE** — see log |

### M12 · UX Polish (MISSION-090 … 095)

| Mission | Description | Deps | Pri | Cplx |
|---------|-------------|------|-----|------|
| MISSION-090 | Complete shortcut map + command palette (add, mark complete, status, navigate, settings). | 036 | Important | M | **DONE** — see log |
| MISSION-091 | States audit: empty/loading/skeleton/error/retry on every data surface. | 040..050 | Core | M | **DONE** — see log |
| MISSION-092 | RTL pass: mirror nav, flip icons, mixed-direction titles, test AR+EN. | 033 | Core | M | **DONE** — see log |
| MISSION-093 | Accessibility pass (WCAG AA): focus, labels, dialogs, contrast, reduced-motion. | 037 | Important | M | **DONE** — see log |
| MISSION-094 | Performance pass: virtual lists, debounced search, image cache, startup timing. | 021,040 | Important | M | **DONE** — see log |
| MISSION-095 | Micro-interactions & density tiers (comfortable/compact). | 031 | Optional | M | **DONE** — see log |

### M13 · Testing & Release (MISSION-096 … 100)

| Mission | Description | Deps | Pri | Cplx |
|---------|-------------|------|-----|------|
| MISSION-096 | Integration tests: DB, providers (recorded fixtures), import/export, backup/restore. | all | Core | L | **DONE** — see log |
| MISSION-097 | E2E (Playwright + tauri-driver): add media, search, track progress, import, backup, restore. | 096 | Core | L |
| MISSION-098 | Provider mock harness + fixtures committed (offline CI). | 053..057 | Core | M |
| MISSION-099 | Release pipeline: build installers (Win/mac/Linux), signing (Win/mac), versioning, changelog. | 007 | Core | L |
| MISSION-100 | Alpha → Beta → Stable gates + `MILESTONE-REPORT.md` per milestone. | all | Core | M |

### FX · Future Scope (MISSION-101+)

Post-Stable, behind designed seams. Nothing here blocks M1–M13 (ADR-013 scope discipline).

| Mission | Description | Deps | Pri | Cplx |
|---------|-------------|------|-----|------|
| MISSION-101 | Cloud sync: aggregate-level last-write-wins + conflict resolution (`updatedAt` already designed). | 100 | Optional | L |
| MISSION-102 | Trakt import + scrobble integration. | 100 | Optional | M |
| MISSION-103 | SIMKL import source. | 100 | Optional | M |
| MISSION-104 | Plugins: provider-adapter plugin seam (first plugin surface). | 100 | Optional | L |
| MISSION-105 | AI features (optional, local, disable-able): auto-tag suggestions, summaries. | 100 | Optional | L |
| MISSION-106 | Mobile companion (read-only or lightweight tracking). | 100 | Optional | L |
| MISSION-107 | WN/LN chapter-release notifications + release-feed calendar (NovelUpdates-style Normal mode). | 100 | Optional | M |
| MISSION-108 | Buddy reads with progress-gated spoiler protection (StoryGraph-style; multi-user). | 100 | Optional | L |
| MISSION-109 | New content types: games, podcasts, music (data-only additions: contentType + progress template). | 100 | Optional | M |
| MISSION-110 | NoviList import source (WN/LN tracker with API docs; young, watch). | 100 | Optional | M |
| MISSION-111 | ISBNDB paid enrichment fallback (free tier: 100 req/mo). | 100 | Optional | S |
| MISSION-112 | SQLCipher encrypted database (opt-in). | 100 | Optional | L |
| MISSION-113 | Advanced visualizations/chart library for stats. | 100 | Optional | M |
| MISSION-114 | **Reading groups — local model**: `reading_group` / `group_member` / `group_note` tables (separate from personal aggregates per ADR-007), stable work identity across devices (provider id or normalized title+author+year hash reusing identity_candidates), manual export/import of `group_state.json` as the first "transport". Realizes MISSION-108's data model. | 100 | Optional | M |
| MISSION-115 | **Reading groups — CRDT + E2EE engine**: `yrs` behind a `p2p` cargo feature (default build untouched), StateVector sync handshake so only missing updates travel, snapshots/compaction (every N ops or 7 days), XChaCha20-Poly1305 group key in the OS keyring (ADR-011 pattern), QR/link invite carrying `(relays, group_id, group_key)` out-of-band. Ownership model: CRDT only for shared notes; each member's shelf is single-writer (no conflicts); group settings owner-only. | 114 | Optional | M |
| MISSION-116 | **Reading groups — async Nostr transport**: `nostr-sdk` behind the same feature flag, 3–5 pinned public relays + optional self-hosted relay, outbox-first writes (SQLite before broadcast), event dedup, chunked updates (~64KB events), `TaskKind::GroupSync` on the existing TaskManager (`task-changed` progress). Tests: async member (write → close → later peer receives), offline → auto-sync on reconnect, via an in-memory mock transport and two real pools. | 115 | Optional | L |
| MISSION-117 | **Reading groups — UI + spoiler gates**: group page with per-member shelves, per-work discussion threads with chapter watermark blur (progress-gated spoilers — MISSION-108's UX), relay/E2EE status badges, explicit opt-in privacy screen stating exactly what leaves the device, manual "Sync now"; i18n EN+AR. | 114 | Optional | M |
| MISSION-118 | **Reading groups — hardening**: key rotation + epoch bump on member removal (forward secrecy), chaos tests (dropped chunks, duplicate events, clock skew), release-binary size budget check before/after the `p2p` feature. | 116 | Optional | M |

---

## 4. Execution workflow (per mission)

1. **Pick up** a mission in the current milestone (only READY/BACKLOG missions; never skip deps).
2. Create its checklist in the epic folder (from the template in `DEVELOPMENT_PLAN.md §5`).
3. **Implement → test → review → fix → update docs → set status** (spec §90, §91).
4. Run quality gates for the milestone (TypeScript PASS · Rust build PASS · Lint PASS · Tests
   PASS · Migrations PASS · no console/TS/lint errors · no broken deps).
5. Mark mission DONE only when its acceptance criteria pass; update this roadmap's status.

## 5. Definition of Done & Quality Gates

Implementation ✓ · Type safety ✓ · Tests ✓ · Error handling ✓ · UI validation ✓ · Docs updated ✓ ·
A11y where needed ✓ · Perf where needed ✓ · No console/TS/lint errors ✓ · No broken deps ✓.

Release gates (M13): TS PASS · Rust build PASS · Lint PASS · Tests PASS · Migrations PASS ·
Import/Export PASS · Backup/Restore PASS · Critical UX flows PASS · Security review PASS.

## 6. MVP scope (what ships as "Stable 1.0")

M1–M6 + M7 core providers (AniList, TMDB, MangaDex, OpenLibrary) + M8 (JSON/CSV import-export,
Goodreads/StoryGraph CSV) + M9 basics + M11 backups. Concretely: **Media CRUD · Library ·
Search(local) · Tracking (incl. novel chapter UX) · Provider import/search · Reviews · Tags ·
Collections · Backup/Restore** — fully offline, en/ar RTL.

## 7. Doc roles

| Doc | Role |
|-----|------|
| **ROADMAP.md** (this) | Master: milestones + complete mission list + status. |
| `DEVELOPMENT_PLAN.md` | Reference: per-task detail (files, tests, AC) + traceability. |
| `PROJECT_REQUIREMENTS.md` | What we build (requirements, priorities). |
| `PHASE0_REPORT.md` | Executive summary of Phase 0. |
| `RESEARCH.md` · `API_PROVIDERS.md` · `UX_RESEARCH.md` | Why we build it that way. |
| `ARCHITECTURE.md` · `DOMAIN_MODEL.md` · `DATABASE.md` · `DECISIONS.md` · `DESIGN_SYSTEM.md` | How. |
