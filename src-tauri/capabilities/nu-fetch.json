{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "nu-fetch",
  "description": "MISSION-129 — hidden NovelUpdates clearance harvester. May invoke only the clearance report command.",
  "windows": ["nu-fetch"],
  "permissions": ["core:default"]
}
