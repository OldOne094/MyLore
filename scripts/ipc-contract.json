{
  "$comment": "Single source of truth for the typed IPC boundary. Edit this file, then run `npm run codegen` to regenerate src/api/ipc.generated.ts. Type expressions: string | number | boolean | null | any | unknown | void | `T[]` | `Record<string, T>` | anything else passes through raw TS. Named interfaces shared across commands live in `types` (name + raw TS body, may reference each other for recursion).",
  "version": 1,
  "types": [
    {
      "name": "ContentNode",
      "shape": "id: string; kind: string; position: number; number: string | null; title: string | null; release_date: string | null; duration_min: number | null; page_count: number | null; synopsis: string | null; is_special: boolean; state: string | null; children: ContentNode[]"
    },
    {
      "name": "TrackingView",
      "shape": "media_id: string; core_status: string; custom_status_id: string | null; started_at: string | null; finished_at: string | null; repeat_count: number; auto_track: boolean; progress: ProgressSummary | null; updated_at: string"
    },
    {
      "name": "ProgressSummary",
      "shape": "percent: number | null; completed: number; total: number; next_label: string | null; next_node_id: string | null"
    },
    {
      "name": "ReviewView",
      "shape": "media_id: string; rating: number | null; review: string | null; short_review: string | null; notes: string | null; favorite: boolean; is_spoiler: boolean; moods: string[]; pace: string | null; content_warnings: string[]; warnings_acknowledged_at: string | null; created_at: string; updated_at: string"
    },
    {
      "name": "ReviewListItemView",
      "shape": "media_id: string; rating: number | null; review: string | null; short_review: string | null; notes: string | null; favorite: boolean; is_spoiler: boolean; moods: string[]; pace: string | null; content_warnings: string[]; warnings_acknowledged_at: string | null; created_at: string; updated_at: string; title: string; content_type: string; cover_asset_id: string | null"
    },
    {
      "name": "MediaTagView",
      "shape": "id: string; name: string; scope: string"
    },
    {
      "name": "NodeProgressNextView",
      "shape": "media_id: string; summary: ProgressSummary"
    },
    {
      "name": "MediaListItem",
      "shape": "id: string; content_type: string; title: string; pub_status: string; release_year: number | null; cover_asset_id: string | null; updated_at: string; favorite: boolean; progress: ProgressSummary"
    },
    {
      "name": "CollectionView",
      "shape": "id: string; name: string; is_smart: boolean; filter: SmartFilter | null; member_count: number; created_at: string"
    },
    {
      "name": "SmartFilter",
      "shape": "content_type: string | null; format: string | null; pub_status: string | null; genre: string | null; tag: string | null; year: number | null; favorite: boolean | null; sort: string | null; ascending: boolean | null"
    },
    {
      "name": "CollectionMemberView",
      "shape": "position: number; media: MediaListItem"
    },
    {
      "name": "BulkFilter",
      "shape": "content_type: string | null; format: string | null; pub_status: string | null; genre: string | null; tag: string | null; year: number | null; favorite: boolean | null"
    },
    {
      "name": "BulkFailure",
      "shape": "media_id: string; reason: string"
    },
    {
      "name": "BulkResult",
      "shape": "total: number; succeeded: number; failed: number; failures: BulkFailure[]"
    },
    {
      "name": "BulkDeleteResult",
      "shape": "summary: BulkResult; trash_ids: string[]"
    },
    {
      "name": "DashboardSummary",
      "shape": "continue_watching: MediaListItem[]; recently_completed: MediaListItem[]; recently_added: MediaListItem[]"
    },
    {
      "name": "ExternalSearchView",
      "shape": "local: MediaListItem[]; groups: ExternalProviderGroup[]; failures: ExternalProviderFailure[]"
    },
    {
      "name": "ExternalProviderGroup",
      "shape": "provider: string; name: string; hits: ExternalHit[]"
    },
    {
      "name": "ExternalHit",
      "shape": "provider: string; provider_id: string; title: string; content_type: string; release_year: number | null; cover_url: string | null; synopsis: string | null; url: string | null; identity: ExternalIdentityFlag"
    },
    {
      "name": "ExternalIdentityFlag",
      "shape": "kind: string; media_id: string | null; score: number | null"
    },
    {
      "name": "ExternalProviderFailure",
      "shape": "provider: string; message: string"
    },
    {
      "name": "ProviderImportView",
      "shape": "media_id: string; created: boolean; identity_kind: string; title: string; content_type: string"
    },
    {
      "name": "EnrichChange",
      "shape": "field: string; before: string | null; after: string | null"
    },
    {
      "name": "EnrichView",
      "shape": "media_id: string; provider: string; refreshed_at: string; changed: boolean; changes: EnrichChange[]"
    },
    {
      "name": "AssetView",
      "shape": "id: string; kind: string; status: string; local_path: string | null; remote_url: string | null; mime_type: string | null"
    },
    {
      "name": "ProviderSettingsView",
      "shape": "provider: string; name: string; enabled: boolean; requires_key: boolean; has_key: boolean; content_types: string[]"
    },
    {
      "name": "ProviderTestView",
      "shape": "ok: boolean; message: string; results: number"
    },
    {
      "name": "CsvMapping",
      "shape": "title: string | null; title_original: string | null; alt_titles: string | null; content_type: string | null; default_content_type: string | null; format: string | null; pub_status: string | null; start_date: string | null; end_date: string | null; release_year: string | null; language: string | null; country: string | null; content_rating: string | null; pages: string | null; duration_min: string | null; ep_count: string | null; ch_count: string | null; synopsis: string | null; author: string | null; artist: string | null; studio: string | null; genres: string | null; tags: string | null; external_id: string | null; cover_url: string | null; banner_url: string | null; delimiter: string; separator: string"
    },
    {
      "name": "Issue",
      "shape": "severity: string; field: string; message: string"
    },
    {
      "name": "PreviewItem",
      "shape": "source_row: number; title: string | null; outcome: string; matched_media_id: string | null; match_kind: string | null; match_score: number | null; issues: Issue[]"
    },
    {
      "name": "ImportPreview",
      "shape": "total: number; valid: number; invalid: number; new: number; in_library: number; duplicates: number; items: PreviewItem[]"
    },
    {
      "name": "ImportPlan",
      "shape": "rows: number[]"
    },
    {
      "name": "ReportItem",
      "shape": "source_row: number; title: string; status: string; media_id: string | null; message: string | null"
    },
    {
      "name": "ImportReport",
      "shape": "total: number; committed: number; skipped: number; failed: number; items: ReportItem[]"
    },
    {
      "name": "ExportReport",
      "shape": "format: string; total: number; path: string"
    },
    {
      "name": "AssetManifestEntry",
      "shape": "id: string; file: string"
    },
    {
      "name": "BackupMeta",
      "shape": "format_version: number; app_version: string; created_at: string; media_count: number; asset_count: number; assets: AssetManifestEntry[]"
    },
    {
      "name": "BackupReport",
      "shape": "path: string; size_bytes: number; media_count: number; asset_count: number"
    },
    {
      "name": "RestoreReport",
      "shape": "media_count: number; asset_count: number; quarantined_to: string; restart_required: boolean"
    },
    {
      "name": "BackupPrefs",
      "shape": "auto_enabled: boolean; interval_hours: number; keep_count: number"
    },
    {
      "name": "BackupEntry",
      "shape": "file_name: string; path: string; size_bytes: number; created_at: string"
    },
    {
      "name": "HealthStatus",
      "shape": "database_ok: boolean"
    },
    {
      "name": "RecoveryOutcome",
      "shape": "quarantined_to: string; restart_required: boolean"
    },
    {
      "name": "MergeConflict",
      "shape": "field: string; survivor: string; duplicate: string"
    },
    {
      "name": "MergePreview",
      "shape": "survivor_id: string; duplicate_id: string; survivor_title: string; duplicate_title: string; merged_title: string; conflicts: MergeConflict[]; nodes_to_move: number; move_review: boolean; move_tracking: boolean; collections_to_move: number"
    },
    {
      "name": "MergeResult",
      "shape": "trash_id: string"
    },
    {
      "name": "TaskSnapshot",
      "shape": "id: string; kind: string; title: string; state: string; progress: number | null; message: string | null; error: string | null; result: unknown | null; created_at: string; updated_at: string"
    },
    {
      "name": "StatCount",
      "shape": "key: string; count: number"
    },
    {
      "name": "StatsView",
      "shape": "total: number; status_counts: StatCount[]; content_type_counts: StatCount[]; rating_counts: StatCount[]; avg_rating: number | null; favorites: number; completed_media: number; completion_rate: number | null; avg_percent: number | null; consumed_minutes: number; consumed_hours: number; consumed_pages: number; year_counts: StatCount[]"
    },
    {
      "name": "CalendarItem",
      "shape": "media_id: string | null; title: string; content_type: string | null; label: string | null; kind: string | null; time: string | null"
    },
    {
      "name": "CalendarDay",
      "shape": "date: string; airs: CalendarItem[]; activity: CalendarItem[]"
    },
    {
      "name": "CalendarMonth",
      "shape": "year: number; month: number; days: CalendarDay[]"
    },
    {
      "name": "GenreCount",
      "shape": "name: string; count: number"
    },
    {
      "name": "RecapMedia",
      "shape": "media_id: string | null; title: string; content_type: string | null; activity_count: number"
    },
    {
      "name": "RecapTotals",
      "shape": "added: number; started: number; completed: number; reviewed: number; progress: number"
    },
    {
      "name": "YearRecap",
      "shape": "year: number; totals: RecapTotals; by_month: number[]; top_genres: GenreCount[]; top_media: RecapMedia[]; longest_streak: number; best_month: number | null"
    },
    {
      "name": "MonthReading",
      "shape": "pages: number; chapters: number"
    },
    {
      "name": "ReadingTotals",
      "shape": "pages: number; chapters: number; finished: number"
    },
    {
      "name": "ReadingRecap",
      "shape": "year: number; by_month: MonthReading[]; totals: ReadingTotals; mood_counts: StatCount[]; pace_counts: StatCount[]; format_counts: StatCount[]"
    },
    {
      "name": "GroupMemberView",
      "shape": "member_id: string; display_name: string; role: string; joined_at: string"
    },
    {
      "name": "GroupShelfEntryView",
      "shape": "group_id: string; member_id: string; work_key: string; title: string; content_type: string; status: string; progress: number; updated_at: string"
    },
    {
      "name": "GroupNoteView",
      "shape": "id: string; group_id: string; work_key: string; author_id: string; body: string; created_at: string; updated_at: string"
    },
    {
      "name": "GroupView",
      "shape": "id: string; name: string; owner_id: string; epoch: number; created_at: string; updated_at: string; members: GroupMemberView[]; shelf_count: number; note_count: number"
    },
    {
      "name": "GroupPrefs",
      "shape": "enabled: boolean; member_id: string; display_name: string"
    },
    {
      "name": "GroupExportReport",
      "shape": "path: string; groups: number; members: number; shelf: number; notes: number"
    },
    {
      "name": "GroupImportReport",
      "shape": "groups: number; members: number; shelf: number; notes: number; skipped: number"
    },
    {
      "name": "GroupKeyStatus",
      "shape": "has_key: boolean; key_id: string | null"
    },
    {
      "name": "GroupInviteView",
      "shape": "group_id: string; group_name: string; epoch: number; relays: string[]; link: string; qr_payload: string; key_id: string"
    },
    {
      "name": "GroupNoteEntry",
      "shape": "note_id: string; body: string"
    },
    {
      "name": "GroupNoteSyncView",
      "shape": "group_id: string; work_key: string; state_vector: string; update: string; notes: GroupNoteEntry[]; compacted: boolean"
    },
    {
      "name": "GroupRelayView",
      "shape": "relays: string[]; pending: number"
    },
    {
      "name": "GroupSyncReport",
      "shape": "published: number; failed: number; received: number; merged: number; skipped: number; pending: number"
    }
  ],
  "commands": [
    {
      "name": "media_create",
      "args": {
        "title": "string",
        "content_type": "string",
        "format": "string | null",
        "pub_status": "string | null",
        "synopsis": "string | null",
        "release_year": "number | null",
        "language": "string | null",
        "country": "string | null",
        "pages": "number | null",
        "duration_min": "number | null",
        "ep_count": "number | null",
        "ch_count": "number | null",
        "genres": "string[]"
      },
      "returns": "string",
      "doc": "Create a media entry from manual input. Resolves with the new media id or rejects with an AppError string."
    },
    {
      "name": "media_list",
      "args": {
        "content_type": "string | null",
        "format": "string | null",
        "pub_status": "string | null",
        "genre": "string | null",
        "tag": "string | null",
        "year": "number | null",
        "favorite": "boolean | null",
        "search": "string | null",
        "sort": "string | null",
        "ascending": "boolean | null",
        "limit": "number | null",
        "offset": "number | null"
      },
      "returns": "MediaListItem[]",
      "doc": "List library entries with optional filters. Resolves with summary rows (each carrying its progress summary for the in-grid quick controls) or rejects with an AppError string."
    },
    {
      "name": "media_facets",
      "args": {},
      "returns": "{ formats: string[]; genres: { id: string; name: string }[]; tags: { id: string; name: string }[]; years: number[] }",
      "doc": "Distinct filter values present in the library. Resolves with facet options or rejects with an AppError string."
    },
    {
      "name": "media_count",
      "args": {},
      "returns": "number",
      "doc": "Total number of titles in the library - the live status-bar count (MISSION-146). Resolves with the count or rejects with an AppError string."
    },
    {
      "name": "media_get",
      "args": { "id": "string" },
      "returns": "{ id: string; content_type: string; format: string | null; title_main: string; title_original: string | null; synopsis: string | null; pub_status: string; start_date: string | null; end_date: string | null; release_year: number | null; language: string | null; country: string | null; content_rating: string | null; pages: number | null; duration_min: number | null; ep_count: number | null; ch_count: number | null; cover_asset_id: string | null; banner_asset_id: string | null; provider: string | null; provider_url: string | null; metadata_refreshed_at: string | null; created_at: string; updated_at: string; alt_titles: { lang: string; title: string }[]; people: string[]; genres: string[]; tags: string[]; external_ids: { provider: string; ext_id: string; url: string | null }[]; relations: { to_id: string; relation: string }[] } | null",
      "doc": "Read the full aggregate for one media. Resolves with the record or null when not found; rejects with an AppError string."
    },
    {
      "name": "media_search",
      "args": { "query": "string", "content_type": "string | null" },
      "returns": "MediaListItem[]",
      "doc": "Local full-text search over the library. When content_type is provided, only media of that type are returned. Resolves with summary rows (each carrying its progress summary) or rejects with an AppError string."
    },
    {
      "name": "media_tags",
      "args": { "media_id": "string" },
      "returns": "MediaTagView[]",
      "doc": "The personal tags linked to one media. Resolves with tag rows (id + name + scope) or rejects with an AppError string."
    },
    {
      "name": "media_add_tag",
      "args": {
        "media_id": "string",
        "tag": "string"
      },
      "returns": "MediaTagView[]",
      "doc": "Add a personal tag to one media (reused or created as needed). Resolves with the updated personal-tag list or rejects with an AppError string."
    },
    {
      "name": "media_remove_tag",
      "args": {
        "media_id": "string",
        "tag_id": "string"
      },
      "returns": "MediaTagView[]",
      "doc": "Remove a personal tag from one media (the tag row is kept for other media). Resolves with the updated personal-tag list or rejects with an AppError string."
    },
    {
      "name": "review_get",
      "args": { "media_id": "string" },
      "returns": "ReviewView | null",
      "doc": "Read a media's review. Resolves with the row or null when the user hasn't reviewed it; rejects with an AppError string."
    },
    {
      "name": "review_list",
      "returns": "ReviewListItemView[]",
      "doc": "Every review in the library with its media's display fields (title, content type, cover), most recently updated first - the aggregate Reviews hub (MISSION-144). Resolves with the list or rejects with an AppError string."
    },
    {
      "name": "review_save",
      "args": {
        "media_id": "string",
        "rating": "number | null",
        "review": "string | null",
        "short_review": "string | null",
        "notes": "string | null",
        "favorite": "boolean",
        "is_spoiler": "boolean",
        "moods": "string[]",
        "pace": "string | null",
        "content_warnings": "string[]"
      },
      "returns": "ReviewView",
      "doc": "Save (create or update) a media's review. Resolves with the stored row (an entirely empty review clears the row and resolves with an empty view) or rejects with an AppError string."
    },
    {
      "name": "review_acknowledge_warnings",
      "args": { "media_id": "string" },
      "returns": "ReviewView",
      "doc": "Acknowledge a media's current content-warning set (MISSION-079) — stamps warnings_acknowledged_at now and resolves with the updated row, or rejects when there is no review / no warnings to acknowledge."
    },
    {
      "name": "review_delete",
      "args": { "media_id": "string" },
      "returns": "void",
      "doc": "Delete a media's review row. Resolves or rejects with an AppError string."
    },
    {
      "name": "search_external",
      "args": {
        "query": "string",
        "content_type": "string | null"
      },
      "returns": "ExternalSearchView",
      "doc": "External (provider) search grouped by provider, with identity flags. `content_type` narrows the fan-out when provided; null searches every enabled provider (domain-agnostic). Resolves with local hits + provider groups + per-provider failures, or rejects with an AppError string."
    },
    {
      "name": "provider_get_details",
      "args": { "provider": "string", "id": "string" },
      "returns": "unknown",
      "doc": "Fetch full details for one title from a specific provider (MISSION-127). Returns a JSON object with all normalized metadata (synopsis, cover_url, authors, genres, tags, status, dates, counts, external links) or rejects with an AppError string."
    },
    {
      "name": "import_provider",
      "args": {
        "provider": "string",
        "provider_id": "string"
      },
      "returns": "ProviderImportView",
      "doc": "Import one provider title into the library (details → identity check → add). Resolves with the media that owns the title — newly created (created: true) or an existing library row the identity check matched (created: false), or rejects with an AppError string."
    },
    {
      "name": "import_file_detect",
      "args": {
        "source": "string"
      },
      "returns": "string",
      "doc": "Sniff a file's import format from its content (MISSION-072): `json` vs `anilist` for JSON files, `csv` vs `goodreads` vs `storygraph` for CSV files. The frontend calls this after reading a file to pick the parser and, for the profile kinds (anilist/goodreads/storygraph), to skip the column-mapping step. Resolves with the kind string or rejects with an AppError string."
    },
    {
      "name": "import_file_preview",
      "args": {
        "kind": "string",
        "source": "string",
        "mapping": "CsvMapping | null"
      },
      "returns": "ImportPreview",
      "doc": "Parse + dedup a file (kind `json` = MyLore JSON format, `csv` = CSV with a column mapping, `anilist` = AniList export, `goodreads` = Goodreads CSV, `storygraph` = StoryGraph CSV) into the per-item preview. `mapping` is required for `csv` and ignored for the other kinds. Read-only. Resolves with the preview (per-row outcomes + issues) or rejects with an AppError string."
    },
    {
      "name": "import_commit",
      "args": {
        "kind": "string",
        "source": "string",
        "mapping": "CsvMapping | null",
        "plan": "ImportPlan | null"
      },
      "returns": "TaskSnapshot",
      "doc": "Import a file's rows as a background task (MISSION-070): spawns the commit on the TaskManager and resolves with the initial (queued) snapshot; progress + terminal state stream as `task_changed` events and the task can be cancelled. The commit runs in one transaction, savepoint per row. `plan` selects which source rows to import; null imports every `New` row of the preview. Non-new / invalid / unselected rows are reported as skipped; a row that fails to insert rolls back its own savepoint and is reported as failed. On success the task's `result` is the per-item `ImportReport`."
    },
    {
      "name": "export_media",
      "args": {
        "format": "string",
        "path": "string"
      },
      "returns": "TaskSnapshot",
      "doc": "Export the whole library as a background task (MISSION-071): streams rows to `path` as json / csv / markdown (`format`) and resolves with the initial (queued) snapshot; progress + terminal state stream as `task_changed` events and the task can be cancelled (a cancelled export drops its partial file). The file is written to a `*.partial` sibling and renamed into place on success. On success the task's `result` is the `ExportReport` (`{ format, total, path }`)."
    },
    {
      "name": "backup_create",
      "args": {},
      "returns": "TaskSnapshot",
      "doc": "Create a validated `.mylore` backup of the whole library as a background task (MISSION-084): a consistent `VACUUM INTO` database snapshot, every cached asset file, and a meta.json manifest zipped under `{data_dir}/backups`. Resolves with the initial (queued) snapshot; progress + terminal state stream as `task_changed` events and the task can be cancelled (a cancelled backup leaves no `.partial` archive). The archive is re-opened and validated before success. On success the task's `result` is the `BackupReport` (`{ path, size_bytes, media_count, asset_count }`)."
    },
    {
      "name": "backup_validate",
      "args": { "path": "string", "passphrase": "string | null" },
      "returns": "BackupMeta",
      "doc": "MISSION-112-aware validate: the returned meta carries encrypted=true when the embedded snapshot is SQLCipher-encrypted. passphrase unlocks archives from other machines."
    },
    {
      "name": "backup_restore",
      "args": { "path": "string", "passphrase": "string | null" },
      "returns": "TaskSnapshot",
      "doc": "Restore a `.mylore` backup archive as a background task (MISSION-085): validates the archive, quarantines the current database + cached images under `{data_dir}/quarantine-…`, swaps the restored data into place, repoints asset paths at the restored files, and verifies the result - rolling back the previous data on any failure. The live pool is closed to unlock the files, so the app MUST restart after success (`restart_required` in the RestoreReport). Not cancelable mid-restore by design. Resolves with the initial (queued) snapshot; progress + terminal state stream as `task_changed` events."
    },
    {
      "name": "backup_prefs_get",
      "args": {},
      "returns": "BackupPrefs",
      "doc": "Load the backup preferences (MISSION-086): automatic backups on/off, the interval in hours, and how many recent archives to keep (plus one per older month). Resolves with the BackupPrefs or rejects with an AppError string."
    },
    {
      "name": "backup_prefs_set",
      "args": { "auto_enabled": "boolean", "interval_hours": "number", "keep_count": "number" },
      "returns": "BackupPrefs",
      "doc": "Validate and persist the backup preferences (MISSION-086): the interval must be 1-8760 hours and the keep count 1-100. Every backup (manual or automatic) applies the retention policy afterwards - keeping the newest N archives plus the newest of every older month. Resolves with the stored BackupPrefs or rejects with an AppError string."
    },
    {
      "name": "backup_list",
      "args": {},
      "returns": "BackupEntry[]",
      "doc": "List every `.mylore` archive in the backups folder, newest first (MISSION-088). Entries carry file name, full path, size and the creation stamp parsed from the name; contents are not validated - use backup_validate per archive. Resolves with the list or rejects with an AppError string."
    },
    {
      "name": "backup_delete",
      "args": { "path": "string" },
      "returns": "void",
      "doc": "Delete one archive from the backups folder (MISSION-088). Only files inside that folder whose names match the archive pattern can be deleted - the path is re-derived server-side, so foreign paths are rejected. Resolves when deleted or rejects with an AppError string."
    },
    {
      "name": "app_health",
      "args": {},
      "returns": "HealthStatus",
      "doc": "Startup health of the local database (MISSION-088). When integrity_check failed at startup the app launches in recovery mode with database_ok false and the UI shows the recovery screen instead of the normal shell. Resolves with the HealthStatus."
    },
    {
      "name": "recover_start_fresh",
      "args": {},
      "returns": "RecoveryOutcome",
      "doc": "Move the corrupt database (and its WAL sidecars) aside into `{data_dir}/quarantine-corrupt-…` so the next startup creates a fresh one (MISSION-088 recovery). Closes the pool to unlock the files - restart the app afterwards. Resolves with the RecoveryOutcome or rejects with an AppError string."
    },
    {
      "name": "recover_restore",
      "args": { "path": "string" },
      "returns": "RecoveryOutcome",
      "doc": "Validate and restore a `.mylore` archive over the corrupt database (MISSION-088 recovery) - the same rollback-safe quarantine/swap/verify flow as backup_restore. Closes the pool to unlock the files - restart the app afterwards. Resolves with the RecoveryOutcome or rejects with an AppError string."
    },
    {
      "name": "import_csv_headers",
      "args": {
        "source": "string",
        "delimiter": "string"
      },
      "returns": "string[]",
      "doc": "Read the header row of a CSV file for the mapping UI's column pickers. Resolves with the trimmed column names or rejects with an AppError string."
    },
    {
      "name": "media_enrich",
      "args": {
        "media_id": "string"
      },
      "returns": "EnrichView",
      "doc": "Refresh a media's provider-owned metadata from its provider and report what changed (per-field before → after). Never touches user data (tracking, review, collections, personal tags, asset ids). Resolves with the diff view or rejects with an AppError string."
    },
    {
      "name": "providers_list",
      "args": {},
      "returns": "ProviderSettingsView[]",
      "doc": "Snapshot every registered provider for the settings UI. Resolves with the rows in registration order, or rejects with an AppError string."
    },
    {
      "name": "provider_set_enabled",
      "args": {
        "provider": "string",
        "enabled": "boolean"
      },
      "returns": "ProviderSettingsView",
      "doc": "Toggle one provider on/off. Persists the flag and takes effect immediately (routing rebuilds the coordinator). Resolves with the updated row or rejects with an AppError string."
    },
    {
      "name": "provider_set_key",
      "args": {
        "provider": "string",
        "api_key": "string"
      },
      "returns": "ProviderSettingsView",
      "doc": "Store (or clear, when blank) a provider's API key in the OS keyring. The key is never persisted in settings files and never returned to the webview. Resolves with the updated row or rejects with an AppError string."
    },
    {
      "name": "provider_test_connection",
      "args": {
        "provider": "string"
      },
      "returns": "ProviderTestView",
      "doc": "Ping one provider with a probe search. Runs even when the provider is disabled so a key can be verified before enabling. Resolves with the test outcome (never rejects for a provider failure) or rejects with an AppError."
    },
    {
      "name": "anilist_oauth_start",
      "args": {},
      "returns": "void",
      "doc": "MISSION-130 - start the AniList OAuth (authorization-code + loopback) flow: binds 127.0.0.1:24110, opens the system browser at the authorize page, then exchanges the returned code and stores the token as the anilist provider key. Completion arrives via the anilist-oauth event. Rejects only if the loopback port is unavailable or the browser cannot be launched."
    },
    {
      "name": "db_security_status",
      "args": {},
      "returns": "{ available: boolean; encrypted: boolean }",
      "doc": "MISSION-112 - snapshot of at-rest encryption: available=true when this build ships SQLCipher support; encrypted=true when the on-disk library is currently encrypted."
    },
    {
      "name": "db_enable_encryption",
      "args": { "passphrase": "string" },
      "returns": "void",
      "doc": "MISSION-112 - encrypt the live database in place (SQLCipher rekey) and store the passphrase in the secret pipeline. Requires a build with db-encryption and a passphrase of at least 8 characters. Restart the app afterwards."
    },
    {
      "name": "db_get_passphrase",
      "args": {},
      "returns": "string | null",
      "doc": "MISSION-112 - resolve the stored database passphrase for copy-to-clipboard affordances when moving an encrypted archive to another machine. Local only; resolves null when no passphrase is stored."
    },
    {
      "name": "db_disable_encryption",
      "args": {},
      "returns": "void",
      "doc": "MISSION-112 - decrypt the live database back to plaintext and drop the stored passphrase. Requires a build with db-encryption. Restart the app afterwards."
    },
    {
      "name": "dashboard_summary",
      "args": { "limit": "number | null" },
      "returns": "DashboardSummary",
      "doc": "Resolve the dashboard widget lists (continue watching, recently completed, recently added). `limit` is optional and clamped per widget (1..=20). Resolves with the DashboardSummary or rejects with an AppError string."
    },
    {
      "name": "asset_resolve",
      "args": { "asset_id": "string" },
      "returns": "AssetView",
      "doc": "Resolve one cover/banner asset to a cached local file, downloading per the cache policy when needed. `status` is `cached` (local_path usable via `convertFileSrc`), `failed` (transient, retried after a cooldown) or `missing` (permanent broken URL). Resolves with the asset view or rejects with an AppError string."
    },
    {
      "name": "assets_resolve",
      "args": { "asset_ids": "string[]" },
      "returns": "AssetView[]",
      "doc": "Resolve many cover/banner assets in one call (deduped; unknown ids are skipped). The library grid calls this once per visible page so covers resolve as a batch. Resolves with the resolved asset views or rejects with an AppError string."
    },
    {
      "name": "media_nodes",
      "args": { "id": "string" },
      "returns": "ContentNode[]",
      "doc": "Read the full content tree for one media (seasons→episodes, volumes→chapters) with per-node progress state. Resolves with the nested tree, roots ordered by position, or rejects with an AppError string."
    },
    {
      "name": "node_progress_set",
      "args": {
        "node_id": "string",
        "node_state": "string"
      },
      "returns": "void",
      "doc": "Set the progress state of one node (read/watched/skipped/unread). Completed states stamp read_at. Resolves or rejects with an AppError string."
    },
    {
      "name": "node_progress_range",
      "args": {
        "media_id": "string",
        "from_id": "string",
        "to_id": "string",
        "node_state": "string"
      },
      "returns": "string[]",
      "doc": "Set the progress state of every node between two nodes in the media's display order. Resolves with the affected node ids (for optimistic UI) or rejects with an AppError string."
    },
    {
      "name": "node_progress_next",
      "args": { "media_id": "string" },
      "returns": "NodeProgressNextView | null",
      "doc": "Mark the next not-yet-consumed countable node of a media done (watched for episodes, read otherwise) and run the auto-status rule. Resolves with the refreshed progress summary, null when nothing is left to mark, or rejects with an AppError string."
    },
    {
      "name": "tracking_get",
      "args": { "media_id": "string" },
      "returns": "TrackingView | null",
      "doc": "Read the tracking row for one media. Resolves with the row or null when the media is untracked; rejects with an AppError string."
    },
    {
      "name": "tracking_set_status",
      "args": {
        "media_id": "string",
        "core_status": "string"
      },
      "returns": "TrackingView",
      "doc": "Apply a status transition for one media (status engine applies, incl. the Repeat guard and started/finished stamps). Resolves with the updated row or rejects with an AppError string."
    },
    {
      "name": "tracking_set_auto_track",
      "args": {
        "media_id": "string",
        "auto_track": "boolean"
      },
      "returns": "TrackingView",
      "doc": "Toggle Normal (autoTrack) vs Manual tracking mode for one media. Resolves with the updated row (turning Normal back on re-syncs the status to the current progress) or rejects with an AppError string."
    },
    {
      "name": "media_delete",
      "args": { "id": "string" },
      "returns": "string",
      "doc": "Soft-delete a media: store its before-image in trash, cascade the row away. Resolves with the trash id (accepted by trash_restore for undo) or rejects with an AppError string."
    },
    {
      "name": "trash_list",
      "args": {},
      "returns": "{ id: string; kind: string; title: string; deleted_at: string }[]",
      "doc": "List active (not restored) trash entries. Resolves with trash items or rejects with an AppError string."
    },
    {
      "name": "trash_restore",
      "args": { "id": "string" },
      "returns": "void",
      "doc": "Restore a soft-deleted aggregate from its trash before-image - a plain media entry, or a merge entry, which reverses the whole merge (re-creates the duplicate, pulls its nodes back, moves a borrowed review/tracking home and re-adds its collection memberships). Resolves or rejects with an AppError string."
    },
    {
      "name": "merge_plan",
      "args": { "survivor_id": "string", "duplicate_id": "string" },
      "returns": "MergePreview",
      "doc": "Preview what merging the duplicate into the survivor would change (MISSION-089): field-level conflicts (different non-empty values), the merged title, and what will move - content nodes, review/tracking when the survivor lacks one, collection memberships. Resolves with the MergePreview or rejects with an AppError string."
    },
    {
      "name": "merge_apply",
      "args": { "survivor_id": "string", "duplicate_id": "string" },
      "returns": "MergeResult",
      "doc": "Apply a merge (MISSION-089): snapshots the duplicate into trash (kind `merge`, restorable from the Trash page), folds its metadata into the survivor per the MISSION-028 policy, re-keys its nodes / review / tracking / collections onto the survivor and deletes it. Resolves with the trash id for undo or rejects with an AppError string."
    },
    {
      "name": "trash_purge",
      "args": { "id": "string" },
      "returns": "void",
      "doc": "Permanently forget a trash entry. Resolves or rejects with an AppError string."
    },
    {
      "name": "tracking_bulk_set_status",
      "args": {
        "ids": "string[]",
        "core_status": "string",
        "filter": "BulkFilter | null"
      },
      "returns": "BulkResult",
      "doc": "Set the tracking status for many media at once (status engine applies). An optional filter resolves the media set server-side (apply to the whole filtered selection). Resolves with a per-item summary — media that can't reach the target are in `failures`, not an error — or rejects with an AppError string."
    },
    {
      "name": "media_bulk_add_tag",
      "args": {
        "ids": "string[]",
        "tag": "string",
        "filter": "BulkFilter | null"
      },
      "returns": "BulkResult",
      "doc": "Add a personal tag to many media at once (reused or created as needed). An optional filter resolves the media set server-side. Resolves with a per-item summary or rejects with an AppError string."
    },
    {
      "name": "media_bulk_delete",
      "args": {
        "ids": "string[]",
        "filter": "BulkFilter | null"
      },
      "returns": "BulkDeleteResult",
      "doc": "Soft-delete many media. An optional filter resolves the media set server-side. Resolves with a per-item summary plus a trash id per deleted media (for group undo) or rejects with an AppError string."
    },
    {
      "name": "collection_list",
      "args": {},
      "returns": "CollectionView[]",
      "doc": "List collections with member counts, for the Collections page and the add-to-list picker. Resolves with the rows or rejects with an AppError string."
    },
    {
      "name": "collection_create",
      "args": { "name": "string" },
      "returns": "CollectionView",
      "doc": "Create a manual collection; resolves with its view or rejects with an AppError string."
    },
    {
      "name": "collection_create_smart",
      "args": { "name": "string", "filter": "SmartFilter" },
      "returns": "CollectionView",
      "doc": "Create a smart collection from a saved filter; membership is computed live. Resolves with its view or rejects with an AppError string."
    },
    {
      "name": "collection_update_smart",
      "args": { "collection_id": "string", "filter": "SmartFilter" },
      "returns": "CollectionView",
      "doc": "Replace a smart collection's filter. Resolves with the updated view or rejects with an AppError string."
    },
    {
      "name": "collection_rename",
      "args": { "collection_id": "string", "name": "string" },
      "returns": "CollectionView",
      "doc": "Rename a collection; resolves with the updated view or rejects with an AppError string."
    },
    {
      "name": "collection_delete",
      "args": { "collection_id": "string" },
      "returns": "string",
      "doc": "Delete a collection (members cascade). Resolves with the removed name or rejects with an AppError string."
    },
    {
      "name": "collection_members",
      "args": { "collection_id": "string" },
      "returns": "CollectionMemberView[]",
      "doc": "A collection's members in display order. Resolves with the rows or rejects with an AppError string."
    },
    {
      "name": "collection_bulk_add",
      "args": {
        "collection_id": "string",
        "media_ids": "string[]",
        "filter": "BulkFilter | null"
      },
      "returns": "BulkResult",
      "doc": "Add many media to one collection (idempotent append). An optional filter resolves the media set server-side. Resolves with a per-item summary or rejects with an AppError string."
    },
    {
      "name": "collection_remove_member",
      "args": { "collection_id": "string", "media_id": "string" },
      "returns": "string",
      "doc": "Remove one media from a collection; resolves with the removed media id or rejects with an AppError string."
    },
    {
      "name": "collection_reorder",
      "args": { "collection_id": "string", "media_ids": "string[]" },
      "returns": "void",
      "doc": "Persist a drag/drop reorder of a collection's members (the media ids must be exactly the current members). Resolves or rejects with an AppError string."
    },
    {
      "name": "task_list",
      "args": {},
      "returns": "TaskSnapshot[]",
      "doc": "Every background task snapshot, newest first. Resolves with the list or rejects with an AppError string."
    },
    {
      "name": "task_get",
      "args": { "id": "string" },
      "returns": "TaskSnapshot",
      "doc": "The current snapshot of one background task. Resolves with the snapshot or rejects with an AppError string when the id is unknown."
    },
    {
      "name": "task_cancel",
      "args": { "id": "string" },
      "returns": "TaskSnapshot",
      "doc": "Request cancellation of a background task. The runner observes the flag at its next checkpoint (dropping its in-flight transaction). Resolves with the current snapshot or rejects with an AppError string when the id is unknown."
    },
    {
      "name": "stats_summary",
      "args": {},
      "returns": "StatsView",
      "doc": "Resolve the library statistics overview: counts per status and content type, hours and pages consumed, completion rate, average rating, favorites, and the rating + release-year distributions. Resolves with the StatsView or rejects with an AppError string."
    },
    {
      "name": "calendar_month",
      "args": {
        "year": "number",
        "month": "number"
      },
      "returns": "CalendarMonth",
      "doc": "Resolve one calendar month: content-node air/release dates plus the user activity trail, bucketed per local day. Resolves with the CalendarMonth or rejects with an AppError string."
    },
    {
      "name": "recap_year",
      "args": { "year": "number" },
      "returns": "YearRecap",
      "doc": "Resolve the year-in-review recap for one year: headline totals, a monthly completion chart, top genres of finished media, the most-active media, and the longest streak of consecutive active days - all bucketed by local time. Resolves with the YearRecap or rejects with an AppError string."
    },
    {
      "name": "reading_recap",
      "args": { "year": "number" },
      "returns": "ReadingRecap",
      "doc": "Resolve the reading recap for one year: pages and chapters consumed per month (book pages weighed by page count, all bucketed by local time), the year totals including distinct finished reading media, plus all-time taste distributions - mood set, pace and format - built from review metadata and tracked reading media. Resolves with the ReadingRecap or rejects with an AppError string."
    },
    {
      "name": "reading_group_prefs_get",
      "args": {},
      "returns": "GroupPrefs",
      "doc": "MISSION-114 - the local reading-group opt-in flag and this install's identity (a member id is minted and persisted on first read). Resolves with the prefs or rejects with an AppError string."
    },
    {
      "name": "reading_group_prefs_set",
      "args": { "enabled": "boolean", "display_name": "string" },
      "returns": "GroupPrefs",
      "doc": "MISSION-114 - persist the reading-group opt-in flag and display name. Resolves with the updated prefs or rejects with an AppError string."
    },
    {
      "name": "reading_group_list",
      "args": {},
      "returns": "GroupView[]",
      "doc": "MISSION-114 - every reading group with its members and row counts. Resolves with the list or rejects with an AppError string."
    },
    {
      "name": "reading_group_create",
      "args": { "name": "string" },
      "returns": "GroupView",
      "doc": "MISSION-114 - create a reading group (seeds the local owner membership). Resolves with the group view or rejects with an AppError string."
    },
    {
      "name": "reading_group_rename",
      "args": { "group_id": "string", "name": "string" },
      "returns": "GroupView",
      "doc": "MISSION-114 - rename a reading group. Resolves with the updated group view or rejects with an AppError string."
    },
    {
      "name": "reading_group_delete",
      "args": { "group_id": "string" },
      "returns": "void",
      "doc": "MISSION-114 - delete a reading group; its members, shelf rows and notes cascade. Resolves or rejects with an AppError string."
    },
    {
      "name": "reading_group_view",
      "args": { "group_id": "string" },
      "returns": "GroupView",
      "doc": "MISSION-114 - one reading group with its members and row counts. Resolves with the group view or rejects with an AppError string."
    },
    {
      "name": "reading_group_add_member",
      "args": {
        "group_id": "string",
        "member_id": "string",
        "display_name": "string",
        "role": "string"
      },
      "returns": "GroupView",
      "doc": "MISSION-114 - add (or update) a group member. Role is `owner` or `member`. Resolves with the updated group view or rejects with an AppError string."
    },
    {
      "name": "reading_group_remove_member",
      "args": { "group_id": "string", "member_id": "string" },
      "returns": "GroupView",
      "doc": "MISSION-114 - remove a member (the owner can never be removed); bumps the group epoch. Resolves with the updated group view or rejects with an AppError string."
    },
    {
      "name": "reading_group_shelf",
      "args": { "group_id": "string", "member_id": "string | null" },
      "returns": "GroupShelfEntryView[]",
      "doc": "MISSION-114 - a group's shelf rows, optionally narrowed to one member. Resolves with the rows or rejects with an AppError string."
    },
    {
      "name": "reading_group_set_shelf",
      "args": {
        "group_id": "string",
        "member_id": "string",
        "work_key": "string",
        "title": "string",
        "content_type": "string",
        "status": "string",
        "progress": "number"
      },
      "returns": "GroupShelfEntryView",
      "doc": "MISSION-114 - upsert one member's shelf entry for a work (single-writer). Status is a core status string. Resolves with the row or rejects with an AppError string."
    },
    {
      "name": "reading_group_notes",
      "args": { "group_id": "string", "work_key": "string | null" },
      "returns": "GroupNoteView[]",
      "doc": "MISSION-114 - a group's shared notes, optionally narrowed to one work. Resolves with the rows or rejects with an AppError string."
    },
    {
      "name": "reading_group_add_note",
      "args": {
        "group_id": "string",
        "work_key": "string",
        "author_id": "string",
        "body": "string"
      },
      "returns": "GroupNoteView",
      "doc": "MISSION-114 - add a shared note (the author must be a member). Resolves with the note or rejects with an AppError string."
    },
    {
      "name": "reading_group_update_note",
      "args": { "note_id": "string", "body": "string" },
      "returns": "GroupNoteView",
      "doc": "MISSION-114 - update a shared note's body. Resolves with the updated note or rejects with an AppError string."
    },
    {
      "name": "reading_group_delete_note",
      "args": { "note_id": "string" },
      "returns": "void",
      "doc": "MISSION-114 - delete a shared note. Resolves or rejects with an AppError string."
    },
    {
      "name": "reading_group_export",
      "args": { "path": "string", "group_id": "string | null" },
      "returns": "GroupExportReport",
      "doc": "MISSION-114 - write group_state.json (one group when group_id is given, else all) to a caller-chosen path. Resolves with the report or rejects with an AppError string."
    },
    {
      "name": "reading_group_import",
      "args": { "source": "string" },
      "returns": "GroupImportReport",
      "doc": "MISSION-114 - merge a group_state.json payload (read in the webview) into the library; idempotent last-write-wins by updated_at. Resolves with the report or rejects with an AppError string."
    },
    {
      "name": "reading_group_work_key",
      "args": {
        "title": "string",
        "author": "string | null",
        "year": "number | null",
        "provider": "string | null",
        "external_id": "string | null"
      },
      "returns": "string",
      "doc": "MISSION-117 - the stable cross-device work key for a work: `{provider}:{value}` when a provider id is given, else a fold+hash of title/author/year. Exposed so the UI never re-implements (and drifts from) the domain's derivation. Resolves with the key or rejects with an AppError string."
    },
    {
      "name": "reading_group_key_status",
      "args": { "group_id": "string" },
      "returns": "GroupKeyStatus",
      "doc": "MISSION-115 (p2p) - whether this group has a shared key on this device, plus a short fingerprint. Requires a build with the `p2p` feature. Resolves with the status or rejects with an AppError string."
    },
    {
      "name": "reading_group_key_rotate",
      "args": { "group_id": "string" },
      "returns": "GroupKeyStatus",
      "doc": "MISSION-118 (p2p) - replace the group key with a fresh one and bump the group's epoch, so anything sealed from now on is unreadable to whoever held the previous key. Owner-only; queued envelopes are dropped. Resolves with the new status or rejects with an AppError string (including the unsupported-p2p error)."
    },
    {
      "name": "reading_group_invite_create",
      "args": { "group_id": "string", "relays": "string[]" },
      "returns": "GroupInviteView",
      "doc": "MISSION-115 (p2p) - create an out-of-band invite (generates the group key on first use) carrying the relays, group id and key. Resolves with the link + QR payload or rejects with an AppError string."
    },
    {
      "name": "reading_group_invite_accept",
      "args": { "link": "string" },
      "returns": "string",
      "doc": "MISSION-115 (p2p) - accept an invite: create the local group replica and import the shared key. Resolves with the group id or rejects with an AppError string."
    },
    {
      "name": "reading_group_note_state",
      "args": { "group_id": "string", "work_key": "string" },
      "returns": "GroupNoteEntry[]",
      "doc": "MISSION-115 (p2p) - a group's notes for a work, materialized from the conflict-free document. Resolves with the notes or rejects with an AppError string."
    },
    {
      "name": "reading_group_note_edit",
      "args": { "group_id": "string", "work_key": "string", "note_id": "string", "body": "string" },
      "returns": "GroupNoteSyncView",
      "doc": "MISSION-115 (p2p) - apply a local note edit to the CRDT document; returns the encrypted update envelope to hand to peers plus the materialized notes. Resolves with the sync view or rejects with an AppError string."
    },
    {
      "name": "reading_group_note_sync",
      "args": {
        "group_id": "string",
        "work_key": "string",
        "remote_state_vector": "string | null",
        "remote_update": "string | null"
      },
      "returns": "GroupNoteSyncView",
      "doc": "MISSION-115 (p2p) - one sync round-trip: merge an optional encrypted remote update and produce the update the peer is missing (diffed against its state vector). Resolves with the sync view or rejects with an AppError string."
    },
    {
      "name": "reading_group_relays_get",
      "args": { "group_id": "string" },
      "returns": "GroupRelayView",
      "doc": "MISSION-116 (p2p) - a group's relays plus how many envelopes are still waiting in the outbox. Resolves with the view or rejects with an AppError string."
    },
    {
      "name": "reading_group_relays_set",
      "args": { "group_id": "string", "relays": "string[]" },
      "returns": "GroupRelayView",
      "doc": "MISSION-116 (p2p) - replace a group's relay set (owner-only; trimmed, deduped, capped at 8). Resolves with the new view or rejects with an AppError string."
    },
    {
      "name": "reading_group_sync_now",
      "args": { "group_id": "string" },
      "returns": "TaskSnapshot",
      "doc": "MISSION-116 (p2p) - run one relay sync pass as a GroupSync background task: flush the outbox, then pull and merge unseen envelopes. Progress streams over task_changed; resolves with the task snapshot or rejects with an AppError string."
    }
  ],
  "events": [
    {
      "name": "anilist-oauth",
      "payload": "{ ok: boolean; message?: string }",
      "doc": "MISSION-130 - emitted when the AniList OAuth loopback flow finishes: ok=true means the token was stored as the anilist provider key (callers should invalidate the providers list); otherwise message carries a user-safe failure reason."
    },
    {
      "name": "task-changed",
      "payload": "TaskSnapshot",
      "doc": "Emitted by the TaskManager whenever a background task changes state or reports progress."
    }
  ]
}
