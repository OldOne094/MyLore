{
  "$comment": "Single source of truth for the typed IPC boundary. Edit this file, then run `npm run codegen` to regenerate src/api/ipc.generated.ts. Type expressions: string | number | boolean | null | any | unknown | void | `T[]` | `Record<string, T>` | anything else passes through raw TS.",
  "version": 1,
  "commands": [
    {
      "name": "greet",
      "args": { "name": "string" },
      "returns": "string",
      "doc": "Placeholder greeting command (create-tauri-app scaffold). Resolves with the greeting or rejects with an AppError string."
    },
    {
      "name": "media_create",
      "args": {
        "title": "string",
        "content_type": "string",
        "format": "string | null",
        "pub_status": "string | null",
        "synopsis": "string | null",
        "release_year": "number | null",
        "language": "string | null",
        "country": "string | null",
        "pages": "number | null",
        "duration_min": "number | null",
        "ep_count": "number | null",
        "ch_count": "number | null",
        "genres": "string[]"
      },
      "returns": "string",
      "doc": "Create a media entry from manual input. Resolves with the new media id or rejects with an AppError string."
    },
    {
      "name": "media_list",
      "args": {
        "content_type": "string | null",
        "pub_status": "string | null",
        "genre": "string | null",
        "tag": "string | null",
        "favorite": "boolean | null",
        "search": "string | null",
        "sort": "string | null",
        "ascending": "boolean | null",
        "limit": "number | null",
        "offset": "number | null"
      },
      "returns": "{ id: string; content_type: string; title: string; pub_status: string; release_year: number | null; cover_asset_id: string | null; updated_at: string }[]",
      "doc": "List library entries with optional filters. Resolves with summary rows or rejects with an AppError string."
    }
  ],
  "events": []
}
